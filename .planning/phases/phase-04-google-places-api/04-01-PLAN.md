---
phase: 4
plan: 04-01
wave: 1
depends_on: []
files_modified:
  - lib/score.ts
  - components/audit/ScoreHero.tsx
  - components/audit/AuditDashboard.tsx
autonomous: true
requirements: [REQ-2]
---

# Plan 04-01 — Wire Google Places into Score

## Objective
Ajouter `computeBlendedScore` dans `lib/score.ts`, modifier `ScoreHero` pour accepter une prop `google` et afficher un libellé conditionnel, câbler `DiagnosticGoogle` dans `AuditDashboard` avec state partagé.

## Tasks

### Task 1 — Add `computeBlendedScore` to `lib/score.ts`
<read_first>
- lib/score.ts
- types/audit.ts (pour le type GoogleData)
</read_first>

<action>
Ajouter à la fin de `lib/score.ts` (après `scoreBadge`) :

```ts
export function computeBlendedScore(
  tauxNoshow: number,
  google?: { rating: number | null; user_ratings_total: number } | null
): number {
  const base = computeScore(tauxNoshow);
  if (!google || typeof google.rating !== "number" || !Number.isFinite(google.rating)) {
    return base;
  }
  const reviews = Number.isFinite(google.user_ratings_total) ? google.user_ratings_total : 0;
  const confidence = Math.min(1, Math.max(0, reviews / 50));
  const delta = Math.round((google.rating - 4.0) * 5 * confidence);
  return Math.max(0, Math.min(100, base + delta));
}
```
</action>

<acceptance_criteria>
- `grep -n "export function computeBlendedScore" lib/score.ts` → 1 match
- `npm run build` exit 0
- Mental test : `computeBlendedScore(10, null)` === `computeScore(10)` ; `computeBlendedScore(10, {rating:4.8, user_ratings_total:100})` === `computeScore(10) + 4` ; `computeBlendedScore(10, {rating:5, user_ratings_total:0})` === `computeScore(10)` ; clamp à [0,100]
</acceptance_criteria>

### Task 2 — Update `ScoreHero` with `google` prop + conditional label
<read_first>
- components/audit/ScoreHero.tsx
- types/audit.ts
- lib/score.ts (après Task 1)
</read_first>

<action>
1. Changer la signature : `export default function ScoreHero({ stats, google }: { stats: AuditStats; google?: GoogleData | null })`.
2. Importer `computeBlendedScore` depuis `@/lib/score` et `GoogleData` depuis `@/types/audit`.
3. Remplacer `const score = computeScore(tauxNoshow);` par :
   ```ts
   const score = computeBlendedScore(tauxNoshow, google);
   const hasGoogle = !!(google && typeof google.rating === "number");
   ```
4. Sous la ligne `<div className="mt-1 text-sm text-gray-500">sur 100</div>`, ne rien changer. Par contre, AJOUTER sous le paragraphe narratif (`<p>...</p>` ligne ~100), avant la fermeture du `</div>` du bloc de droite, un nouveau paragraphe :
   ```tsx
   <p className="mt-3 text-[12px] font-medium uppercase tracking-wider text-gray-400">
     {hasGoogle
       ? "Score global (no-shows + réputation Google)"
       : "Score no-shows (hors Google)"}
   </p>
   ```
</action>

<acceptance_criteria>
- `grep -n "google" components/audit/ScoreHero.tsx` → matches pour la prop et le conditional label
- `grep -n "computeBlendedScore" components/audit/ScoreHero.tsx` → 1 match (import + usage)
- `grep -n "hors Google" components/audit/ScoreHero.tsx` → 1 match
- `npm run build` exit 0
</acceptance_criteria>

### Task 3 — Wire `DiagnosticGoogle` into `AuditDashboard` section 4
<read_first>
- components/audit/AuditDashboard.tsx
- components/audit/DiagnosticGoogle.tsx
- types/audit.ts
</read_first>

<action>
1. Convertir le composant en client component : ajouter `"use client";` reste en place (déjà). Ajouter `import { useState } from "react";` en tête.
2. Importer `DiagnosticGoogle` : `import DiagnosticGoogle from "./DiagnosticGoogle";`.
3. Importer le type : `import type { AuditStats, GoogleData } from "@/types/audit";` (remplacer l'import existant).
4. Dans le corps de la fonction, ajouter : `const [google, setGoogle] = useState<GoogleData | null>(null);`
5. Dans la section `<AuditSection id="score" ...>`, remplacer `<ScoreHero stats={stats} />` par :
   ```tsx
   <div className="space-y-5">
     <ScoreHero stats={stats} google={google} />
     <DiagnosticGoogle
       nomCabinet={stats.nom_cabinet ?? ""}
       onSuccess={(data) => setGoogle(data)}
     />
   </div>
   ```
</action>

<acceptance_criteria>
- `grep -n "DiagnosticGoogle" components/audit/AuditDashboard.tsx` → ≥ 2 matches (import + render)
- `grep -n "useState" components/audit/AuditDashboard.tsx` → 1 match
- `grep -n "google={google}" components/audit/AuditDashboard.tsx` → 1 match
- `npm run build` exit 0
- `npm run lint` sans erreur bloquante
</acceptance_criteria>

## Must-haves (goal-backward)
- `computeBlendedScore` exportée et pure
- ScoreHero affiche libellé distinct selon présence de `google`
- DiagnosticGoogle rendu dans le dashboard, state partagé avec ScoreHero
- Build + lint verts
- Aucune regression : sans cliquer "Analyser", le comportement actuel est préservé
