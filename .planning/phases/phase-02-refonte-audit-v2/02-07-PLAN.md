---
phase: 02-refonte-audit-v2
plan: 07
type: execute
wave: 5
depends_on: ["02-06"]
files_modified: []
autonomous: false
requirements: [REQ-2]
must_haves:
  truths:
    - "Visual review vs sketches 005–009 confirms layout + palette fidelity"
    - "Scrollspy updates aria-current correctly on active link as user scrolls"
    - "No horizontal overflow detected at 375, 768, 1280 viewport widths in resultats state"
    - "axe-core audit returns 0 blocking violations on /audit resultats state"
    - "ca_perdu value never re-multiplied (checked in all 4 surfaces: Argent KPI, MoneyBuildCard breakdown total, Argent KPI in PDF, violet hero in PDF)"
---

<objective>
Human verification checkpoint: end-to-end visual + a11y + business-rule audit of the refondu /audit dashboard before the phase ships.

Purpose: cover requirement 8 (A11y AA + responsive) and the `ca_perdu` invariant with a final human sign-off.
Output: approval or issue list; no code changes in this plan (gap-closure plans follow if issues found).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/phase-02-refonte-audit-v2/02-SPEC.md
@.planning/phases/phase-02-refonte-audit-v2/02-CONTEXT.md
@.claude/skills/sketch-findings-audit-system-audit-noshows/SKILL.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual + scrollspy + responsive UAT</name>
  <read_first>
    - .planning/phases/phase-02-refonte-audit-v2/02-SPEC.md (all 8 requirements + acceptance criteria)
    - .claude/skills/sketch-findings-audit-system-audit-noshows/SKILL.md (visual contract)
  </read_first>
  <what-built>
    Dashboard clinique-claire v2 rendering on `/audit` in `resultats` state:
    - Sidebar 240px desktop / top-bar sticky mobile
    - 5 sections scrollables (Synthèse, Manque à gagner, Où & Quand, Score, Plan + CTA)
    - Scrollspy with `aria-current="location"` on active link
    - Pastel KPI cards + violet-plein hero + dual DOM bars charts + primary-dark score hero + timeline + Calendly
    - PDF refondé en light palette
    - 3 legacy v1 components supprimés
  </what-built>
  <how-to-verify>
    1. **Run dev server:** `npm run dev`, open `http://localhost:3000/audit`
    2. **Upload a sample CSV** (or mock the n8n response) to reach `resultats` state
    3. **Visual parity vs sketches:**
       - Compare Section 1 vs sketch 006 A — 4 KPI pastels grid égal
       - Compare Section 2 vs sketch 007 A — violet plein + breakdown glass
       - Compare Section 3 vs sketch 008 B — dual charts côte à côte
       - Compare Section 4 vs sketch 009 C — primary-dark ring blanc
       - Compare Section 5 vs sketch 009 C — timeline 3 items + Calendly
    4. **Scrollspy:** scroll slowly, verify the sidebar active link updates as each section enters viewport center; active link has a vertical 3px bar + emerald-50 bg + `aria-current="location"` (inspect DOM)
    5. **Responsive:**
       - Chrome devtools → 1280px: sidebar fixed 240px visible, charts side-by-side
       - 768px: sidebar becomes sticky top-bar scrollable, charts may stack
       - 375px: top-bar scrolls horizontally for 5 links + CTA icon button, no horizontal page overflow
       - Run `document.documentElement.scrollWidth === document.documentElement.clientWidth` at each width → must be `true`
    6. **axe-core audit:** install axe DevTools extension, run on `/audit` in resultats state → 0 blocking violations (moderate/minor acceptable, note in summary)
    7. **`ca_perdu` invariant — 4 surfaces:** with mocked `stats.ca_perdu = 47200` and `stats.nb_mois = 3`:
       - Argent KPI shows `47 200 €` (NOT `566 400 €`, NOT `188 800 €`)
       - MoneyBuildCard "Total CA perdu annualisé" line shows `47 200 €`
       - PDF Argent KPI cell shows `47 200 €`
       - PDF violet hero shows `47 200 €`
    8. **Calendly env toggle:**
       - With `NEXT_PUBLIC_CALENDLY_URL` unset → placeholder vert sapin visible, NOT empty screen
       - With env set → iframe loads
    9. **Legacy states untouched:** reload the page (back to `formulaire`), submit with broken data to trigger `erreur`, confirm these two states look identical to pre-Phase-2 UI.
    10. **Build check:** `npm run build` exits 0.
  </how-to-verify>
  <acceptance_criteria>
    - Visual fidelity confirmed vs sketches 005–009 (user confirms each)
    - Scrollspy behavior matches spec (aria-current toggles, visual active indicator correct)
    - No horizontal overflow at 375 / 768 / 1280
    - axe-core blocking violations = 0
    - ca_perdu verbatim in all 4 surfaces
    - Calendly placeholder renders when env absent
    - formulaire / loading / erreur states unchanged
    - npm run build exits 0
  </acceptance_criteria>
  <resume-signal>Type "approved" to ship, or list issues by requirement number (e.g., "R5: charts stack at 1100px instead of 1024px; R7: Calendly iframe has no rounded corners").</resume-signal>
</task>

</tasks>

<verification>
Covered by the checkpoint's `how-to-verify` steps.
</verification>

<success_criteria>
- All 8 SPEC requirements visually and behaviorally verified.
- No regressions on legacy states.
- Ready for ship or well-scoped gap-closure list for follow-up plan.
</success_criteria>

<output>
Create `.planning/phases/phase-02-refonte-audit-v2/02-07-SUMMARY.md` documenting the UAT outcome (approved or list of gaps with requirement IDs for potential `--gaps` replan).
</output>
