---
phase: 09-monitoring-analytics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/analytics.ts
  - lib/__tests__/analytics.test.ts
autonomous: true
requirements: [R2, R3, R4]
must_haves:
  truths:
    - "lib/analytics.ts exporte 11 fonctions typées (1 par event)"
    - "Aucun helper n'accepte de string libre PII (email, nom_cabinet, etc.)"
    - "Chaque helper enveloppe track() dans un try/catch silencieux"
    - "Vitest couvre les 11 helpers avec mock @vercel/analytics + test fail-soft"
  artifacts:
    - path: "lib/analytics.ts"
      provides: "11 typed track helpers + fail-soft wrappers (D-02, D-03, D-04)"
      exports: ["trackLandingView", "trackLandingCtaAuditClick", "trackAuditView", "trackCsvPreviewLoaded", "trackCsvRejected", "trackAuditSubmitted", "trackAuditSuccess", "trackAuditFailed", "trackCtaCalendlyClick", "trackGoogleDiagnosticTriggered", "trackPdfDownloaded"]
    - path: "lib/__tests__/analytics.test.ts"
      provides: "Vitest mock + assertions name/properties + fail-soft (D-06)"
      contains: "vi.mock(\"@vercel/analytics\""
  key_links:
    - from: "lib/analytics.ts"
      to: "@vercel/analytics"
      via: "import { track }"
      pattern: "from\\s+[\"']@vercel/analytics[\"']"
    - from: "lib/analytics.ts"
      to: "types/audit-errors.ts"
      via: "import type { AuditErrorCode }"
      pattern: "AuditErrorCode"
---

<objective>
Créer la couche typée centralisée `lib/analytics.ts` (D-02, D-03, D-04, D-05) qui expose les 11 helpers `track*()` consommés par les call-sites de Plan 09-03, et la suite Vitest associée (D-06) couvrant nom d'event + properties + fail-soft (R2, R3, R4, AC-3).

Purpose: Centraliser tous les `track()` derrière des signatures typées rend l'injection accidentelle de PII statiquement impossible (AC-3) et garantit la convention snake_case + fail-soft uniformément. Un fichier unique, testé, avant tout wiring.

Output: 1 fichier source + 1 fichier de test (~15-20 tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-09-monitoring-analytics/09-SPEC.md
@.planning/phases/phase-09-monitoring-analytics/09-CONTEXT.md
@.planning/phases/phase-09-monitoring-analytics/09-RESEARCH.md
@types/audit-errors.ts
@lib/audit-thresholds.ts
@lib/audit-validation.ts
@lib/__tests__

<interfaces>
<!-- API track() de @vercel/analytics (RESEARCH §Q3) -->
From @vercel/analytics:
```ts
export function track(
  name: string,
  properties?: Record<string, string | number | boolean | null>
): void;
```
Properties acceptent UNIQUEMENT des primitives (string | number | boolean | null). Pas d'objects, arrays, undefined, Date.

<!-- Type AuditErrorCode existant (D-05) -->
From types/audit-errors.ts:
```ts
export type AuditErrorCode =
  | "MISSING_COLUMNS"
  | "INVALID_DATE_FORMAT"
  | "EMPTY_AFTER_PARSING"
  | "ENCODING_ERROR"
  | "INSUFFICIENT_DATA";
```

<!-- Convention nommage events (RESEARCH gotchas — snake_case verrouillé) -->
Event names :
1.  landing_view             | properties: { referrer?: string }
2.  landing_cta_audit_click  | properties: aucune
3.  audit_view               | properties: aucune
4.  csv_preview_loaded       | properties: { nb_rdv: number, reco_rate: number }
5.  csv_rejected             | properties: { error_code: AuditErrorCode }
6.  audit_submitted          | properties: { degraded: boolean }
7.  audit_success            | properties: { score: number, taux_noshow: number }
8.  audit_failed             | properties: { error_code: string }
9.  cta_calendly_click       | properties: { location: 'hero' | 'footer' | 'audit-results' }
10. google_diagnostic_triggered | properties: aucune
11. pdf_downloaded           | properties: aucune
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Écrire la suite Vitest (failing) pour les 11 helpers</name>
  <files>lib/__tests__/analytics.test.ts</files>
  <read_first>
    - lib/__tests__/audit-thresholds.test.ts (style et structure des tests Phase 7 — mêmes conventions)
    - .planning/phases/phase-09-monitoring-analytics/09-RESEARCH.md §Q5 (pattern mock vitest exact)
    - types/audit-errors.ts (codes AuditErrorCode)
    - .planning/phases/phase-09-monitoring-analytics/09-CONTEXT.md §D-06 (~15-20 tests cible)
  </read_first>
  <behavior>
    Pour chaque helper, vérifier :
    - L'event name exact (ex: "audit_success", PAS "auditSuccess")
    - Les properties exactes (snake_case ; types primitifs ; pas de propriété supplémentaire)
    - Fail-soft : si `track()` throw, le helper ne propage PAS

    Structure attendue (exemples) :
    - trackLandingView() sans arg → track("landing_view", undefined) OU track("landing_view", {})
    - trackLandingView("https://google.com") → track("landing_view", { referrer: "https://google.com" })
    - trackLandingCtaAuditClick() → track("landing_cta_audit_click", undefined)
    - trackCsvPreviewLoaded(150, 0.85) → track("csv_preview_loaded", { nb_rdv: 150, reco_rate: 0.85 })
    - trackCsvRejected("MISSING_COLUMNS") → track("csv_rejected", { error_code: "MISSING_COLUMNS" })
    - trackAuditSubmitted(true) → track("audit_submitted", { degraded: true })
    - trackAuditSuccess(72, 18.5) → track("audit_success", { score: 72, taux_noshow: 18.5 })
    - trackAuditFailed("VALIDATION_ERROR") → track("audit_failed", { error_code: "VALIDATION_ERROR" })
    - trackCtaCalendlyClick("hero") → track("cta_calendly_click", { location: "hero" })
    - trackGoogleDiagnosticTriggered() → track("google_diagnostic_triggered", undefined)
    - trackPdfDownloaded() → track("pdf_downloaded", undefined)
    - Test fail-soft global : `vi.mocked(track).mockImplementationOnce(() => { throw new Error("blocked"); })` → `expect(() => trackAuditSuccess(72, 18.5)).not.toThrow()` (au moins 1 test fail-soft sur 1 helper représentatif)
  </behavior>
  <action>
    Créer `lib/__tests__/analytics.test.ts` avec :
    1. Imports : `import { describe, it, expect, vi, beforeEach } from "vitest";` + `import { track } from "@vercel/analytics";` + imports nommés des 11 helpers depuis `@/lib/analytics`.
    2. `vi.mock("@vercel/analytics", () => ({ track: vi.fn() }));` au top-level.
    3. `beforeEach(() => { vi.mocked(track).mockClear(); });`
    4. Un `describe(...)` par helper, avec 1-2 `it(...)` chacun. Total cible : 15-20 `it()`.
    5. Au moins 1 test "is fail-soft when track throws" sur un helper représentatif (ex: `trackAuditSuccess`).
    6. Pour les helpers sans args (`trackLandingCtaAuditClick`, `trackAuditView`, `trackGoogleDiagnosticTriggered`, `trackPdfDownloaded`), accepter soit `track("name", undefined)` soit `track("name")` selon implémentation Task 2 — utiliser `expect(track).toHaveBeenCalledWith("event_name")` ou `toHaveBeenCalledWith("event_name", undefined)` aligné avec Task 2.
    Le test DOIT échouer à ce stade (helpers pas encore implémentés) : c'est attendu (TDD red).
  </action>
  <verify>
    <automated>npx vitest run lib/__tests__/analytics.test.ts 2>&1 | tail -20 ; test -f lib/__tests__/analytics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `lib/__tests__/analytics.test.ts` existe
    - Le fichier contient `vi.mock("@vercel/analytics"` (mock du module)
    - `grep -c '  it(' lib/__tests__/analytics.test.ts` retourne ≥ 15
    - `grep -c 'describe(' lib/__tests__/analytics.test.ts` retourne ≥ 11 (un par helper)
    - Le fichier référence les 11 noms d'events snake_case (`grep -oE '"(landing_view|landing_cta_audit_click|audit_view|csv_preview_loaded|csv_rejected|audit_submitted|audit_success|audit_failed|cta_calendly_click|google_diagnostic_triggered|pdf_downloaded)"' lib/__tests__/analytics.test.ts | sort -u | wc -l` retourne 11
    - Au moins 1 occurrence de `mockImplementationOnce` ou `not.toThrow` (test fail-soft)
    - `npx vitest run lib/__tests__/analytics.test.ts` échoue avec module-not-found OU assertions non satisfaites (RED — `lib/analytics.ts` n'existe pas encore) — c'est OK
  </acceptance_criteria>
  <done>
    Fichier de test commit-ready. État RED attendu (Task 2 le fera passer GREEN). (D-06)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implémenter lib/analytics.ts avec 11 helpers typés + fail-soft</name>
  <files>lib/analytics.ts</files>
  <read_first>
    - lib/__tests__/analytics.test.ts (test écrit en Task 1 — la signature exacte des helpers se déduit de leurs assertions)
    - lib/audit-thresholds.ts (style typage strict + named exports — pattern Phase 7 à dupliquer per D-05)
    - lib/audit-validation.ts (style)
    - types/audit-errors.ts (réutiliser `AuditErrorCode`)
    - .planning/phases/phase-09-monitoring-analytics/09-CONTEXT.md §D-02, D-03, D-04
  </read_first>
  <action>
    Créer `lib/analytics.ts` avec EXACTEMENT cette structure :

    ```ts
    /**
     * Phase 9 — Couche typée d'analytics (Vercel Web Analytics).
     * Tous les call-sites passent par les helpers ci-dessous (D-03 :
     * import direct de `@vercel/analytics` interdit ailleurs).
     * Chaque helper est fail-soft (D-04) : si track() throw (adblock,
     * réseau), le funnel commercial n'est jamais interrompu.
     */
    import { track } from "@vercel/analytics";
    import type { AuditErrorCode } from "@/types/audit-errors";

    /** Wrapper fail-soft local (D-04). */
    function safeTrack(
      name: string,
      properties?: Record<string, string | number | boolean | null>,
    ): void {
      try {
        track(name, properties);
      } catch {
        // Silencieux — analytics ne doit JAMAIS bloquer le parcours commercial.
      }
    }

    export function trackLandingView(referrer?: string): void {
      safeTrack("landing_view", referrer ? { referrer } : undefined);
    }

    export function trackLandingCtaAuditClick(): void {
      safeTrack("landing_cta_audit_click");
    }

    export function trackAuditView(): void {
      safeTrack("audit_view");
    }

    export function trackCsvPreviewLoaded(nbRdv: number, recoRate: number): void {
      safeTrack("csv_preview_loaded", { nb_rdv: nbRdv, reco_rate: recoRate });
    }

    export function trackCsvRejected(errorCode: AuditErrorCode): void {
      safeTrack("csv_rejected", { error_code: errorCode });
    }

    export function trackAuditSubmitted(degraded: boolean): void {
      safeTrack("audit_submitted", { degraded });
    }

    export function trackAuditSuccess(score: number, tauxNoshow: number): void {
      safeTrack("audit_success", { score, taux_noshow: tauxNoshow });
    }

    export function trackAuditFailed(errorCode: string): void {
      safeTrack("audit_failed", { error_code: errorCode });
    }

    export type CalendlyCtaLocation = "hero" | "footer" | "audit-results";

    export function trackCtaCalendlyClick(location: CalendlyCtaLocation): void {
      safeTrack("cta_calendly_click", { location });
    }

    export function trackGoogleDiagnosticTriggered(): void {
      safeTrack("google_diagnostic_triggered");
    }

    export function trackPdfDownloaded(): void {
      safeTrack("pdf_downloaded");
    }
    ```

    Adapter localement les détails (par ex. si Task 1 a écrit `toHaveBeenCalledWith("landing_view", undefined)` au lieu de `toHaveBeenCalledWith("landing_view")`, alors `trackLandingView` sans referrer doit appeler `safeTrack("landing_view", undefined)` — aligner Task 1 et Task 2 strictement). Conserver tous les noms snake_case et la convention `taux_noshow`/`nb_rdv`/`reco_rate`/`error_code` verbatim.
  </action>
  <verify>
    <automated>npx vitest run lib/__tests__/analytics.test.ts 2>&1 | tail -10 | grep -E '(passed|failed)'</automated>
  </verify>
  <acceptance_criteria>
    - `lib/analytics.ts` existe et exporte 11 fonctions nommées : `grep -cE '^export function track' lib/analytics.ts` retourne 11
    - Le fichier contient un seul import depuis `@vercel/analytics` (le module, pas `/react`) : `grep 'from "@vercel/analytics"' lib/analytics.ts` matche 1 ligne
    - Le fichier importe `AuditErrorCode` depuis `@/types/audit-errors`
    - Le fichier contient `try {` et `catch` (fail-soft D-04)
    - Aucune signature n'accepte un type `string` libre nommé `email`, `nom_cabinet`, ou similaire (`grep -E '(email|nom_cabinet|cabinet|patient)' lib/analytics.ts` retourne 0 match)
    - `npx vitest run lib/__tests__/analytics.test.ts` retourne `passed` (tous les tests verts — TDD GREEN)
    - `npm run lint` exit 0
  </acceptance_criteria>
  <done>
    11 helpers typés, importés depuis `@/lib/analytics`, fail-soft, testés. Suite vitest GREEN. (R2, R3, R4, AC-3, D-02..D-06)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App code → Vercel Analytics | properties d'events sont émises depuis le client vers `_vercel/insights/event` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure (PII) | Helpers `track*()` | mitigate | Signatures TS strictes : aucun helper n'accepte de string libre représentant identité (email, nom_cabinet, contenu CSV) ; D-03 verrouille le passage obligatoire par cette couche ; test Vitest assert payload exact (AC-3) |
| T-09-05 | Tampering / DoS | `track()` throw (adblock, réseau, infra Vercel down) | mitigate | `safeTrack()` wrapper try/catch silencieux (D-04) ; test Vitest fail-soft assert `not.toThrow()` (R4) |
| T-09-06 | Repudiation (event drift) | Casing inconsistant (camelCase vs snake_case) entre helpers | mitigate | Convention snake_case verrouillée dans une seule source (`lib/analytics.ts`) ; tests assertent les noms exacts ; RESEARCH gotcha §3 documenté |
</threat_model>

<verification>
- [ ] AC-3 (PII): aucun helper n'accepte d'identifiant personnel ; test Vitest assert payload exact
- [ ] AC-5: `npm run lint` vert + `npx vitest run lib/__tests__/analytics.test.ts` vert
- [ ] R2: 11 helpers exportés
- [ ] R3: PII statiquement bloquée par typage
- [ ] R4: fail-soft testé

Final check :
```bash
npx vitest run lib/__tests__/analytics.test.ts && npm run lint && grep -c '^export function track' lib/analytics.ts  # doit afficher 11
```
</verification>

<success_criteria>
- 11 helpers `track*()` exportés depuis `lib/analytics.ts`
- 1 seul import de `@vercel/analytics` dans tout le repo (vérifié par Plan 09-03 lint task)
- 11 `describe()` Vitest, ≥15 `it()` au total, 1+ test fail-soft
- Suite vitest scopée verte en < 10s
- Lint vert
- Aucune modification hors scope (uniquement `lib/analytics.ts` + `lib/__tests__/analytics.test.ts`)
</success_criteria>

<output>
After completion, create `.planning/phases/phase-09-monitoring-analytics/09-02-SUMMARY.md` listant :
- Les 11 signatures de helpers (copier depuis `lib/analytics.ts`)
- Le nombre de tests (`describe` count + `it` count)
- Output `npx vitest run lib/__tests__/analytics.test.ts` final (lignes "passed" / durée)
- Confirmation D-03 : aucun autre fichier n'importera `@vercel/analytics` directement (sera vérifié en Plan 09-03)
</output>
