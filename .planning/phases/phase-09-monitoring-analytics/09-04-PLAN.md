---
phase: 09-monitoring-analytics
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - .planning/phases/phase-09-monitoring-analytics/09-04-bundle-after.txt
  - .planning/phases/phase-09-monitoring-analytics/09-04-bundle-delta.md
autonomous: true
requirements: [R5]
must_haves:
  truths:
    - "Le First Load JS de la route / après instrumentation est mesuré et capturé"
    - "Le delta gzipped vs baseline 09-01 est ≤ +5 KB"
    - "Build et lint sont verts"
  artifacts:
    - path: ".planning/phases/phase-09-monitoring-analytics/09-04-bundle-after.txt"
      provides: "Table de routes Next 14 post-instrumentation"
      min_lines: 1
    - path: ".planning/phases/phase-09-monitoring-analytics/09-04-bundle-delta.md"
      provides: "Comparaison baseline / after avec delta calculé"
      contains: "delta"
  key_links:
    - from: "09-04-bundle-delta.md"
      to: "09-01-bundle-baseline.txt"
      via: "comparaison route /"
      pattern: "First Load JS"
---

<objective>
Mesurer le bundle final post-instrumentation et calculer le delta vs baseline 09-01 pour valider AC-4 (≤ +5 KB gzipped sur la route `/`).

Purpose: AC-4 ne peut être vérifié qu'a posteriori, et le baseline pré-install (09-01) est la moitié du diff. Cette étape ferme le calcul.

Output: 2 fichiers de mesure dans `.planning/phases/phase-09-monitoring-analytics/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-09-monitoring-analytics/09-SPEC.md
@.planning/phases/phase-09-monitoring-analytics/09-RESEARCH.md
@.planning/phases/phase-09-monitoring-analytics/09-01-bundle-baseline.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mesurer le bundle après instrumentation et calculer le delta</name>
  <files>.planning/phases/phase-09-monitoring-analytics/09-04-bundle-after.txt, .planning/phases/phase-09-monitoring-analytics/09-04-bundle-delta.md</files>
  <read_first>
    - .planning/phases/phase-09-monitoring-analytics/09-01-bundle-baseline.txt (valeur de référence)
    - .planning/phases/phase-09-monitoring-analytics/09-RESEARCH.md §Q6 (commande exacte)
  </read_first>
  <action>
    1. Build propre et capture :
       ```bash
       rm -rf .next
       npm run build 2>&1 | sed -n '/Route (app)/,/^$/p' > .planning/phases/phase-09-monitoring-analytics/09-04-bundle-after.txt
       ```
       Si la sed ne match pas, fallback `tail -60`.

    2. Lire `09-01-bundle-baseline.txt` et `09-04-bundle-after.txt`. Extraire le `First Load JS` de la route `/` (en kB) dans chaque fichier.

    3. Créer `09-04-bundle-delta.md` avec ce template :
       ```markdown
       # Phase 9 — Bundle Delta (AC-4)

       | Metric | Baseline (09-01) | After (09-04) | Delta | AC-4 (≤ +5 KB) |
       |---|---|---|---|---|
       | Route `/` First Load JS | XXX kB | YYY kB | +Z kB | ✅ / ❌ |
       | Route `/audit` First Load JS | XXX kB | YYY kB | +Z kB | (info) |

       Mesure : `npm run build` (Next.js 14 — gzipped values).
       Date : 2026-04-26.

       ## Verdict
       AC-4 : [pass / fail]
       ```

    4. Si delta > +5 KB sur la route `/` : NE PAS commit le delta comme "pass" — créer une checkpoint dans le SUMMARY recommandant investigation (probablement import élargi indésirable). Sinon : pass et passer à Plan 09-05.
  </action>
  <verify>
    <automated>test -s .planning/phases/phase-09-monitoring-analytics/09-04-bundle-after.txt && test -s .planning/phases/phase-09-monitoring-analytics/09-04-bundle-delta.md && grep -E 'AC-4' .planning/phases/phase-09-monitoring-analytics/09-04-bundle-delta.md && npm run build 2>&1 | tail -3 | grep -E 'Compiled successfully|Generating' && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `09-04-bundle-after.txt` existe et contient une ligne pour route `/` avec valeur en kB
    - `09-04-bundle-delta.md` existe et contient une table avec baseline / after / delta
    - Le delta de la route `/` est explicitement calculé et le verdict pass/fail noté
    - `npm run build` exit 0
    - `npm run lint` exit 0
    - Si delta ≤ +5 KB → verdict "pass" écrit dans le markdown
    - Si delta > +5 KB → verdict "fail" écrit + section "Investigation" dans le markdown (ne pas masquer)
  </acceptance_criteria>
  <done>
    AC-4 mesuré et documenté. Build + lint verts. (R5, AC-4, AC-5)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build artifact → mesure | Mesure dépend de l'environnement npm install — le baseline et l'after doivent être faits sur la même machine, même Node version |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Repudiation (mesure non-reproductible) | `npm run build` outputs | accept | Pour Phase 9 (mesure one-shot), accepter la variabilité ; documenter Node version + date dans le delta.md ; si AC-4 borderline, refaire la mesure (rm -rf .next + clean install) |
</threat_model>

<verification>
- [ ] AC-4 : delta route `/` ≤ +5 KB gzipped (vérifié par lecture humaine du delta.md)
- [ ] AC-5 : `npm run build` + `npm run lint` verts (Task 1)
- [ ] R5 satisfaite

Final :
```bash
cat .planning/phases/phase-09-monitoring-analytics/09-04-bundle-delta.md
```
</verification>

<success_criteria>
- 2 fichiers de mesure existent
- Delta calculé explicitement
- AC-4 verdict tracé
- Build + lint verts
</success_criteria>

<output>
After completion, create `.planning/phases/phase-09-monitoring-analytics/09-04-SUMMARY.md` reprenant le tableau du `09-04-bundle-delta.md` + la commande `npm run build` complète + Node version (`node -v`) au moment de la mesure.
</output>
