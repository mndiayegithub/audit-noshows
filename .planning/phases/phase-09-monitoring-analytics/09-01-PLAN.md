---
phase: 09-monitoring-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - app/layout.tsx
  - .planning/phases/phase-09-monitoring-analytics/09-01-bundle-baseline.txt
autonomous: true
requirements: [R1, R5]
must_haves:
  truths:
    - "Le poids First Load JS de la route / AVANT install est capturé dans un fichier baseline"
    - "Le package @vercel/analytics est listé en dependencies dans package.json"
    - "Le composant <Analytics /> est monté dans <body> de RootLayout (sibling de Toaster/Agentation)"
    - "npm run build passe sans erreur après ajout"
  artifacts:
    - path: ".planning/phases/phase-09-monitoring-analytics/09-01-bundle-baseline.txt"
      provides: "Baseline First Load JS route / pour delta AC-4"
      min_lines: 1
    - path: "package.json"
      provides: "Dependency entry @vercel/analytics ^1.x"
      contains: "@vercel/analytics"
    - path: "app/layout.tsx"
      provides: "Mount du provider Analytics dans <body>"
      contains: "<Analytics />"
  key_links:
    - from: "app/layout.tsx"
      to: "@vercel/analytics/react"
      via: "import { Analytics }"
      pattern: "from\\s+[\"']@vercel/analytics/react[\"']"
---

<objective>
Capturer le baseline bundle (avant install) puis installer `@vercel/analytics` v1.x et monter `<Analytics />` dans `app/layout.tsx` (R1, R5 baseline, AC-1 prereq, AC-4 baseline, AC-5).

Purpose: Fondation. Sans le provider racine, aucun event custom (`track()`) ne peut être collecté en prod. Et sans baseline capturé AVANT install, le delta bundle d'AC-4 n'est plus mesurable a posteriori.

Output: fichier baseline texte + package.json/lockfile mis à jour + layout.tsx avec `<Analytics />` à côté de `<Toaster />`/`<Agentation />` (D-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/phase-09-monitoring-analytics/09-SPEC.md
@.planning/phases/phase-09-monitoring-analytics/09-CONTEXT.md
@.planning/phases/phase-09-monitoring-analytics/09-RESEARCH.md
@app/layout.tsx

<interfaces>
<!-- Le composant Analytics est exporté depuis @vercel/analytics/react (D-01, RESEARCH Q1) -->
From @vercel/analytics/react:
```ts
export function Analytics(props?: { mode?: 'auto' | 'production' | 'development'; debug?: boolean }): JSX.Element;
```
NE PAS passer `mode` — laisser les defaults (auto). Le script ne tracke pas en dev (RESEARCH Q4) ; c'est le comportement voulu.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capturer le bundle baseline AVANT install</name>
  <files>.planning/phases/phase-09-monitoring-analytics/09-01-bundle-baseline.txt</files>
  <read_first>
    - .planning/phases/phase-09-monitoring-analytics/09-RESEARCH.md §Q6 (commande exacte de mesure)
    - .planning/phases/phase-09-monitoring-analytics/09-VALIDATION.md §Manual-Only Verifications (ligne bundle delta)
  </read_first>
  <action>
    AVANT toute install, exécuter `npm run build` pour capturer le `First Load JS` actuel des routes `/` et `/audit`. Sauvegarder l'intégralité de la table de routes Next.js dans `.planning/phases/phase-09-monitoring-analytics/09-01-bundle-baseline.txt` :

    ```bash
    npm run build 2>&1 | sed -n '/Route (app)/,/^$/p' > .planning/phases/phase-09-monitoring-analytics/09-01-bundle-baseline.txt
    ```

    Si le pattern sed ne match pas (output Next.js variant), faire `npm run build 2>&1 | tail -60 > .planning/phases/phase-09-monitoring-analytics/09-01-bundle-baseline.txt`. Le fichier DOIT contenir au minimum les lignes `/` et `/audit` avec leur `First Load JS` exprimé en kB.

    NE PAS commit ce fichier en dehors de `.planning/` (déjà au bon endroit). NE PAS modifier `package.json` à ce stade.
  </action>
  <verify>
    <automated>test -s .planning/phases/phase-09-monitoring-analytics/09-01-bundle-baseline.txt && grep -E '(/| /audit) +' .planning/phases/phase-09-monitoring-analytics/09-01-bundle-baseline.txt</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `.planning/phases/phase-09-monitoring-analytics/09-01-bundle-baseline.txt` existe et est non vide
    - Le fichier contient une ligne pour la route `/` avec une valeur en kB
    - `package.json` n'a PAS encore été modifié (`grep '@vercel/analytics' package.json` retourne 0 match)
  </acceptance_criteria>
  <done>
    Baseline texte sauvegardé dans `.planning/phases/phase-09-monitoring-analytics/09-01-bundle-baseline.txt`. (R5 baseline, prerequisite for AC-4)
  </done>
</task>

<task type="auto">
  <name>Task 2: Installer @vercel/analytics (v1.x) en runtime dependency</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (dependencies actuelles)
    - .planning/phases/phase-09-monitoring-analytics/09-RESEARCH.md §Q1 (import paths + version)
  </read_first>
  <action>
    Exécuter `npm install @vercel/analytics` (sans `@latest`, sans `--save-dev`). Vérifier que `package.json` contient `"@vercel/analytics": "^1.x.x"` dans `dependencies` (pas `devDependencies`). NE PAS installer `@vercel/analytics/next` ni autre variante — RESEARCH Q1 lock le path canonique `/react`.

    Si `npm install` modifie d'autres entries (transitives), c'est attendu — ne pas les retirer du lockfile.
  </action>
  <verify>
    <automated>npm ls @vercel/analytics --depth=0 2>&1 | grep -E '@vercel/analytics@1\.'</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"@vercel/analytics"' package.json` retourne une match dans la section `dependencies` (pas `devDependencies`)
    - `package-lock.json` résout le package en version 1.x
    - `npm ls @vercel/analytics --depth=0` affiche `@vercel/analytics@1.x.x` sans warning UNMET / missing
  </acceptance_criteria>
  <done>
    Package installé en runtime dependency, version 1.x, lockfile à jour. (R1)
  </done>
</task>

<task type="auto">
  <name>Task 3: Monter <Analytics /> dans app/layout.tsx</name>
  <files>app/layout.tsx</files>
  <read_first>
    - app/layout.tsx (structure actuelle du RootLayout — lignes 1-46)
    - .planning/phases/phase-09-monitoring-analytics/09-RESEARCH.md §Q2 (placement diff exact)
    - .planning/phases/phase-09-monitoring-analytics/09-CONTEXT.md §D-01
  </read_first>
  <action>
    Dans `app/layout.tsx` :
    1. Ajouter l'import `import { Analytics } from "@vercel/analytics/react";` après l'import existant `import Footer from "@/components/Footer";` (avant `import "./globals.css";`).
    2. Dans le JSX du `<body>`, ajouter `<Analytics />` comme sibling, immédiatement après la ligne `{process.env.NODE_ENV === "development" && <Agentation />}` et avant `</body>`.
    3. NE PAS passer de prop `mode`, `debug`, ou autre (defaults Vercel = comportement voulu, RESEARCH Q4).
    4. NE PAS wrapper dans un Provider custom (D-01 lock).
    5. Conserver fonts (Inter/Fraunces), metadata, classes body, Toaster props identiques.

    Puis exécuter `npm run build` pour valider que le build passe et capturer le bundle POST-install (pour vérification rapide ; la mesure officielle AC-4 est faite en Plan 09-04).
  </action>
  <verify>
    <automated>grep -E 'import \{ Analytics \} from "@vercel/analytics/react"' app/layout.tsx && grep -E '<Analytics ?/>' app/layout.tsx && npm run build 2>&1 | tail -5 | grep -E '(Compiled successfully|Generating static pages)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'from "@vercel/analytics/react"' app/layout.tsx` matche exactement 1 ligne
    - `grep '<Analytics' app/layout.tsx` matche exactement 1 occurrence
    - `<Analytics />` apparaît dans la portion `<body>...</body>` (vérifier par lecture)
    - Aucune prop `mode=` ou `debug=` sur `<Analytics />`
    - `npm run build` exit 0 (pas d'erreur de compilation TS/Next)
    - `npm run lint` exit 0
  </acceptance_criteria>
  <done>
    Layout ressemble au diff exact de RESEARCH §Q2. Build prod et lint verts. (R1, AC-1 prereq, AC-5)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Vercel Analytics endpoint | POST `_vercel/insights/event` envoyé depuis le client (script auto-injecté en prod) |
| npm registry → repo | Nouvelle dépendance runtime tirée du registry public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering (supply chain) | `@vercel/analytics` package | mitigate | Lockfile (`package-lock.json`) verrouille la version résolue ; install via `npm install` sans `--no-save` ; pas d'install global ni script post-install custom |
| T-09-02 | Information Disclosure | `<Analytics />` script auto-injecté | accept | Vercel Web Analytics est cookieless, ne transmet pas d'IP en clair (CONSTRAINT SPEC.md) ; pas de PII émis par le script lui-même (page views uniquement) ; properties PII bloquées par helpers typés (Plan 09-02) |
| T-09-03 | Denial of Service | Vercel Analytics infra down ou bloquée par adblock | mitigate | `<Analytics />` est non-bloquant par design (script async fire-and-forget) ; AC-3 vérifie comportement adblock en smoke prod Plan 09-04 |
</threat_model>

<verification>
- [ ] AC-5: `npm run build` vert + `npm run lint` vert (Task 3)
- [ ] R1 satisfaite : provider monté + dépendance installée (Task 2 + 3)
- [ ] R5 baseline capturé pour delta AC-4 (Task 1)

Final check après Task 3 :
```bash
npm run build && npm run lint && grep -c '<Analytics' app/layout.tsx
```
</verification>

<success_criteria>
- Fichier baseline `09-01-bundle-baseline.txt` existe avec table de routes Next 14
- `@vercel/analytics` v1.x listé dans `package.json:dependencies`
- `<Analytics />` rendu dans `<body>` de RootLayout, sans prop custom, sibling de Toaster/Agentation
- `npm run build` exit 0
- `npm run lint` exit 0
- Aucun fichier hors scope modifié (Wave 1 fondation pure)
</success_criteria>

<output>
After completion, create `.planning/phases/phase-09-monitoring-analytics/09-01-SUMMARY.md` contenant :
- Version exacte de `@vercel/analytics` résolue (output `npm ls @vercel/analytics`)
- Diff exact de `app/layout.tsx` (avant/après)
- Référence vers `09-01-bundle-baseline.txt` (chemin) avec extrait de la ligne route `/`
- Output `npm run build` final (lignes "Compiled successfully" + table de routes post-install pour comparaison rapide)
</output>
