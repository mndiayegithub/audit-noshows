---
phase: 09-monitoring-analytics
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - app/page.tsx
  - components/landing/LandingViewTracker.tsx
  - components/landing/LandingHero.tsx
  - components/landing/LandingNav.tsx
  - components/landing/CTABand.tsx
  - components/landing/LandingFooter.tsx
  - app/audit/page.tsx
  - hooks/useCSVPreview.ts
  - components/audit/CSVErrorCard.tsx
  - components/audit/CTACalendly.tsx
  - components/audit/CalendlyEmbed.tsx
  - components/audit/DiagnosticGoogle.tsx
  - components/audit/AuditDashboard.tsx
autonomous: true
requirements: [R2, R3]
must_haves:
  truths:
    - "Les 11 events sont émis aux call-sites exacts du funnel"
    - "Aucun composant n'importe `track` directement de `@vercel/analytics` (D-03)"
    - "Aucun call-site ne passe d'identifiant personnel (email, nom_cabinet) aux helpers"
    - "Les events de mount (landing_view, audit_view) sont déclenchés via useEffect dans des composants client"
  artifacts:
    - path: "components/landing/LandingViewTracker.tsx"
      provides: "Wrapper client minimal qui appelle trackLandingView() au mount (app/page.tsx est RSC)"
      contains: "trackLandingView"
  key_links:
    - from: "app/audit/page.tsx"
      to: "@/lib/analytics"
      via: "import des helpers track*"
      pattern: "from\\s+[\"']@/lib/analytics[\"']"
    - from: "hooks/useCSVPreview.ts"
      to: "@/lib/analytics"
      via: "trackCsvPreviewLoaded sur succès"
      pattern: "trackCsvPreviewLoaded"
    - from: "components/audit/CSVErrorCard.tsx"
      to: "@/lib/analytics"
      via: "trackCsvRejected dans useEffect au mount"
      pattern: "trackCsvRejected"
---

<objective>
Câbler les 11 helpers de Plan 09-02 aux call-sites exacts du funnel commercial v2 (R2). Aucun composant ne doit importer `track` directement (D-03) ; tous passent par `@/lib/analytics`.

Purpose: Sans wiring, helpers = morts. Cette étape transforme la couche typée en instrumentation effective sur le funnel `landing → upload → audit → CTA Calendly → PDF`.

Output: 13 fichiers modifiés (12 wiring + 1 nouveau wrapper client `LandingViewTracker.tsx` car `app/page.tsx` est RSC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-09-monitoring-analytics/09-SPEC.md
@.planning/phases/phase-09-monitoring-analytics/09-CONTEXT.md
@.planning/phases/phase-09-monitoring-analytics/09-RESEARCH.md
@.planning/phases/phase-09-monitoring-analytics/09-01-SUMMARY.md
@.planning/phases/phase-09-monitoring-analytics/09-02-SUMMARY.md
@app/page.tsx
@app/audit/page.tsx
@hooks/useCSVPreview.ts
@components/audit/CSVErrorCard.tsx
@components/audit/CTACalendly.tsx
@components/audit/CalendlyEmbed.tsx
@components/audit/DiagnosticGoogle.tsx
@components/audit/AuditDashboard.tsx
@components/landing/LandingHero.tsx
@components/landing/LandingNav.tsx
@components/landing/CTABand.tsx
@components/landing/LandingFooter.tsx

<interfaces>
<!-- API consommée depuis Plan 09-02 — DOIT déjà exister avant cette wave -->
From @/lib/analytics:
```ts
export function trackLandingView(referrer?: string): void;
export function trackLandingCtaAuditClick(): void;
export function trackAuditView(): void;
export function trackCsvPreviewLoaded(nbRdv: number, recoRate: number): void;
export function trackCsvRejected(errorCode: AuditErrorCode): void;
export function trackAuditSubmitted(degraded: boolean): void;
export function trackAuditSuccess(score: number, tauxNoshow: number): void;
export function trackAuditFailed(errorCode: string): void;
export type CalendlyCtaLocation = "hero" | "footer" | "audit-results";
export function trackCtaCalendlyClick(location: CalendlyCtaLocation): void;
export function trackGoogleDiagnosticTriggered(): void;
export function trackPdfDownloaded(): void;
```

<!-- Contraintes RESEARCH §Gotchas -->
- track() depuis Server Component = no-op silencieux. Tous les call-sites DOIVENT être dans des fichiers `"use client"`.
- `app/page.tsx` (landing) est RSC (PAS `"use client"`) → on ne peut PAS y appeler track(). Solution : composant client minimal `LandingViewTracker` monté dedans, qui appelle `trackLandingView()` dans un `useEffect(..., [])`.
- Les events de mount utilisent `useEffect(() => { trackXxx(); }, [])` (pas d'appel inline qui re-fire à chaque render).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing — wrapper LandingViewTracker + 4 boutons CTA "/audit"</name>
  <files>components/landing/LandingViewTracker.tsx, app/page.tsx, components/landing/LandingHero.tsx, components/landing/LandingNav.tsx, components/landing/CTABand.tsx, components/landing/LandingFooter.tsx</files>
  <read_first>
    - app/page.tsx (RSC — confirmer absence de "use client" lignes 1-5)
    - components/landing/LandingHero.tsx (déjà "use client")
    - components/landing/LandingNav.tsx (déjà "use client")
    - components/landing/CTABand.tsx (vérifier directive "use client")
    - components/landing/LandingFooter.tsx (vérifier directive "use client")
    - .planning/phases/phase-09-monitoring-analytics/09-RESEARCH.md §Gotchas
  </read_first>
  <action>
    Étape 1 — Créer `components/landing/LandingViewTracker.tsx` :
    ```tsx
    "use client";
    import { useEffect } from "react";
    import { trackLandingView } from "@/lib/analytics";

    /** Phase 9 — déclenche `landing_view` au mount de `app/page.tsx` (RSC). */
    export default function LandingViewTracker() {
      useEffect(() => {
        const referrer =
          typeof document !== "undefined" && document.referrer ? document.referrer : undefined;
        trackLandingView(referrer);
      }, []);
      return null;
    }
    ```

    Étape 2 — Dans `app/page.tsx`, ajouter `import LandingViewTracker from "@/components/landing/LandingViewTracker";` puis `<LandingViewTracker />` au début du JSX retourné (avant `<LandingNav />`).

    Étape 3 — Dans `LandingHero.tsx`, `LandingNav.tsx`, `CTABand.tsx`, `LandingFooter.tsx` :
    - Ajouter `import { trackLandingCtaAuditClick } from "@/lib/analytics";`
    - Sur chaque `<Link href="/audit" ...>` ou `<a href="/audit">`, ajouter `onClick={() => trackLandingCtaAuditClick()}` SANS preventDefault. Si l'élément a déjà un onClick, composer (`onClick={() => { existing(); trackLandingCtaAuditClick(); }}`).
    - Si un fichier est RSC (pas de `"use client"` en tête), soit ajouter `"use client"` en tête (privilégié si fichier petit, sans fetch server-side), soit extraire le bouton dans un sous-composant client local. NE PAS introduire un `<Link onClick>` dans un fichier RSC sans `"use client"` (Next refusera de compiler).

    Étape 4 — Aucun import direct de `@vercel/analytics` dans aucun de ces fichiers (D-03).
  </action>
  <verify>
    <automated>test -f components/landing/LandingViewTracker.tsx && grep -l 'trackLandingCtaAuditClick' components/landing/LandingHero.tsx components/landing/LandingNav.tsx components/landing/CTABand.tsx components/landing/LandingFooter.tsx | wc -l | grep -E '^4$' && grep -c 'LandingViewTracker' app/page.tsx | grep -E '^[2-9]' && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `components/landing/LandingViewTracker.tsx` existe avec `"use client"` en première ligne
    - Le wrapper appelle `trackLandingView` dans un `useEffect` (pas appel inline) : `grep -E 'useEffect' components/landing/LandingViewTracker.tsx` matche
    - `app/page.tsx` importe ET utilise `LandingViewTracker` (≥ 2 matches `LandingViewTracker`)
    - Les 4 fichiers landing (Hero, Nav, CTABand, Footer) contiennent `trackLandingCtaAuditClick` (1 match chacun minimum)
    - Aucun de ces fichiers n'importe `@vercel/analytics` directement : `grep -lE 'from\s+"@vercel/analytics"' app/page.tsx components/landing/*.tsx` retourne vide
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>
    Events 1 (`landing_view`) et 2 (`landing_cta_audit_click`) câblés. Build vert. (R2 events 1+2)
  </done>
</task>

<task type="auto">
  <name>Task 2: Wiring app/audit/page.tsx (audit_view, audit_submitted, audit_success, audit_failed, pdf_downloaded)</name>
  <files>app/audit/page.tsx</files>
  <read_first>
    - app/audit/page.tsx lignes 1-160 (state machine + handleSubmit + handleDownloadPDF)
    - .planning/phases/phase-09-monitoring-analytics/09-CONTEXT.md §Integration Points
    - .planning/phases/phase-09-monitoring-analytics/09-SPEC.md §Requirements R2 tableau (events 3, 6, 7, 8, 11)
  </read_first>
  <action>
    Dans `app/audit/page.tsx` (`"use client"` déjà en tête) :

    1. Ajouter en haut :
       ```ts
       import { useEffect } from "react"; // ajouter useEffect aux imports React existants
       import {
         trackAuditView,
         trackAuditSubmitted,
         trackAuditSuccess,
         trackAuditFailed,
         trackPdfDownloaded,
       } from "@/lib/analytics";
       ```

    2. **Event 3 (`audit_view`)** — Dans le composant `AuditPage`, juste après les déclarations de state, ajouter :
       ```ts
       useEffect(() => {
         trackAuditView();
       }, []);
       ```

    3. **Event 6 (`audit_submitted`)** — Dans `handleSubmit(degradedConfirmed: boolean)`, AVANT le `await fetch("/api/audit", ...)` (ligne ~100), ajouter :
       ```ts
       trackAuditSubmitted(degradedConfirmed);
       ```

    4. **Events 7 / 8 (`audit_success` / `audit_failed`)** — Après `const data: AuditResponse = await response.json();` :
       - Si `response.ok` ET `data.success` ET `data.stats` : appeler `trackAuditSuccess(score, tauxNoshow)`. `score` se calcule via `100 - taux * 3.2` (cf `app/audit/page.tsx` rendu actuel) — réutiliser la même formule ou récupérer depuis `data.stats` selon ce qui est disponible. `tauxNoshow` = `data.stats.taux_noshow` (ou champ équivalent — vérifier `types/audit.ts`).
       - Si `!response.ok` : appeler `trackAuditFailed(errorCode)` où `errorCode` = `data.error_code` si présent, sinon `String(response.status)`.
       - Dans le `catch (err)` du try-catch global, appeler `trackAuditFailed("CLIENT_EXCEPTION")` (ou le message err.message si court et non-PII — préférer enum statique).

    5. **Event 11 (`pdf_downloaded`)** — Dans `handleDownloadPDF()`, juste après le clic effectif sur le `link.click()` (après `link.click();` ligne ~157) ou juste avant en début de fonction (au choix, mais une seule fois), ajouter :
       ```ts
       trackPdfDownloaded();
       ```
       Préférer en début de fonction (avant le rendu PDF qui peut throw) — comme ça l'event capture l'intention même si la génération échoue. Justifier ce choix dans le SUMMARY.

    6. **NE PAS** passer `email`, `nomCabinet`, ou contenu CSV à aucun helper. **NE PAS** importer `@vercel/analytics` directement.
  </action>
  <verify>
    <automated>grep -cE 'trackAuditView|trackAuditSubmitted|trackAuditSuccess|trackAuditFailed|trackPdfDownloaded' app/audit/page.tsx | grep -vE '^[01234]$' && grep -E 'from\s+"@vercel/analytics"' app/audit/page.tsx ; [ $? -ne 0 ] && echo "OK no direct import" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c trackAuditView app/audit/page.tsx` retourne ≥ 2 (1 import + 1 appel)
    - `grep -c trackAuditSubmitted app/audit/page.tsx` retourne ≥ 2
    - `grep -c trackAuditSuccess app/audit/page.tsx` retourne ≥ 2
    - `grep -c trackAuditFailed app/audit/page.tsx` retourne ≥ 2
    - `grep -c trackPdfDownloaded app/audit/page.tsx` retourne ≥ 2
    - Tous les imports viennent de `@/lib/analytics` : `grep -E 'from\s+"@vercel/analytics"' app/audit/page.tsx` retourne 0 match
    - Aucun call-site ne passe `email` ou `nomCabinet` : `grep -E 'track(Audit|Pdf)[A-Za-z]+\(.*(email|nomCabinet)' app/audit/page.tsx` retourne 0 match
    - `npm run build` exit 0 (Type-checking strict valide signatures)
    - `useEffect(() => { trackAuditView(); }, [])` existe (1 occurrence)
  </acceptance_criteria>
  <done>
    Events 3, 6, 7, 8, 11 câblés sur la page audit. Build vert. (R2 events 3+6+7+8+11)
  </done>
</task>

<task type="auto">
  <name>Task 3: Wiring useCSVPreview + CSVErrorCard (csv_preview_loaded, csv_rejected)</name>
  <files>hooks/useCSVPreview.ts, components/audit/CSVErrorCard.tsx</files>
  <read_first>
    - hooks/useCSVPreview.ts (signature complète, état "ready" et payload du résultat)
    - components/audit/CSVErrorCard.tsx (structure props + rendering)
    - lib/parseCSVForPreview.ts (forme du résultat preview — pour identifier `nb_rdv` et `reco_rate` exacts)
    - .planning/phases/phase-09-monitoring-analytics/09-SPEC.md §Requirements R2 events 4, 5
    - types/audit-errors.ts (AuditErrorCode)
  </read_first>
  <action>
    **Event 4 (`csv_preview_loaded`)** dans `hooks/useCSVPreview.ts` :
    1. Ajouter `import { trackCsvPreviewLoaded } from "@/lib/analytics";`
    2. Dans le `useEffect` qui parse le CSV, à la branche succès (status devient `"ready"` avec un `result` valide), ajouter `trackCsvPreviewLoaded(result.nb_rdv, result.reco_rate);`
    3. Identifier les noms exacts des champs dans le résultat (`result.nb_rdv` et `result.reco_rate` ou équivalents — lire `lib/parseCSVForPreview.ts` pour confirmer). Si les noms diffèrent, mapper vers les arguments numériques attendus par le helper. Le helper exige `(nbRdv: number, recoRate: number)` strict.
    4. Garde fail-soft : si une valeur n'est pas un `number` valide, ne pas appeler le helper (skip silencieux).

    **Event 5 (`csv_rejected`)** dans `components/audit/CSVErrorCard.tsx` :
    1. Ajouter `"use client"` en tête si absent.
    2. Ajouter `import { useEffect } from "react";` (si pas déjà importé) et `import { trackCsvRejected } from "@/lib/analytics";`
    3. Le composant reçoit `props.error` (cf signature `CSVErrorCardProps`). Identifier le champ qui contient l'`AuditErrorCode` (probablement `error.error_code` ou `error.code`).
    4. Au mount, déclencher l'event :
       ```ts
       useEffect(() => {
         if (props.error?.error_code) {
           trackCsvRejected(props.error.error_code);
         }
       }, [props.error?.error_code]);
       ```
       (Adapter l'accès au champ selon la forme réelle de `props.error`.)

    NE PAS importer `@vercel/analytics` directement dans aucun des deux fichiers.
  </action>
  <verify>
    <automated>grep -c trackCsvPreviewLoaded hooks/useCSVPreview.ts | grep -E '^[2-9]' && grep -c trackCsvRejected components/audit/CSVErrorCard.tsx | grep -E '^[2-9]' && grep -E 'from\s+"@vercel/analytics"' hooks/useCSVPreview.ts components/audit/CSVErrorCard.tsx ; [ $? -ne 0 ] && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c trackCsvPreviewLoaded hooks/useCSVPreview.ts` retourne ≥ 2 (1 import + 1 appel)
    - `grep -c trackCsvRejected components/audit/CSVErrorCard.tsx` retourne ≥ 2
    - `grep -E 'useEffect' components/audit/CSVErrorCard.tsx` matche (event au mount, pas inline)
    - Aucun import direct `@vercel/analytics` : `grep -E 'from\s+"@vercel/analytics"' hooks/useCSVPreview.ts components/audit/CSVErrorCard.tsx` retourne 0 match
    - `npm run build` exit 0 (signatures TS strictes valides — `AuditErrorCode` correctement résolu)
  </acceptance_criteria>
  <done>
    Events 4 (`csv_preview_loaded`) et 5 (`csv_rejected`) câblés. Build vert. (R2 events 4+5)
  </done>
</task>

<task type="auto">
  <name>Task 4: Wiring CTA Calendly (3 locations) + Google Diagnostic + PDF dashboard</name>
  <files>components/audit/CTACalendly.tsx, components/audit/CalendlyEmbed.tsx, components/audit/DiagnosticGoogle.tsx, components/audit/AuditDashboard.tsx</files>
  <read_first>
    - components/audit/CTACalendly.tsx (calendlyUrl, structure du bouton)
    - components/audit/CalendlyEmbed.tsx (variante A : 2 boutons — primaire blanc "Prendre rendez-vous" + secondaire)
    - components/audit/DiagnosticGoogle.tsx (handleSearch ligne ~65-75, fetch /api/google-places)
    - components/audit/AuditDashboard.tsx (bouton "Télécharger PDF" lignes 53-60 — onClick délègue à `onDownloadPDF` prop)
    - .planning/phases/phase-09-monitoring-analytics/09-SPEC.md §Requirements R2 events 9, 10, 11
  </read_first>
  <action>
    **Event 9 (`cta_calendly_click`)** — 3 locations attendues : `'hero' | 'footer' | 'audit-results'`. Mapping :
    - `components/audit/CTACalendly.tsx` → location `"audit-results"` (CTA principal post-audit)
    - `components/audit/CalendlyEmbed.tsx` → location `"audit-results"` (variante A, même section résultats — sketch 009)
    - Si un autre bouton Calendly existe en hero/footer landing, l'identifier via `grep -rn 'calendly\|NEXT_PUBLIC_CALENDLY' components/landing/ app/page.tsx` ; sinon, considérer que `'hero'` et `'footer'` sont enums autorisés mais non utilisés en Phase 9 (acceptable : le SPEC dit "pour chaque bouton/link Calendly").

    Dans CHAQUE bouton/link Calendly :
    1. `import { trackCtaCalendlyClick } from "@/lib/analytics";`
    2. Sur le `onClick` (ou ajouter un onClick si `<a href={calendlyUrl}>` simple) : `onClick={() => trackCtaCalendlyClick("audit-results")}` (ou la location appropriée). NE PAS preventDefault.

    **Event 10 (`google_diagnostic_triggered`)** — `components/audit/DiagnosticGoogle.tsx` :
    1. `import { trackGoogleDiagnosticTriggered } from "@/lib/analytics";`
    2. Dans la fonction qui déclenche le fetch Google Places (ex: `handleSearch` autour de la ligne 65, JUSTE AVANT le `await fetch('/api/google-places?...')`), ajouter `trackGoogleDiagnosticTriggered();`. Choisir le moment où l'utilisateur a réellement déclenché la recherche (clic bouton ou submit), pas un re-render passif.

    **Event 11 (`pdf_downloaded`)** — `components/audit/AuditDashboard.tsx` ligne 53-60 (bouton "Télécharger PDF") :
    - Le bouton délègue déjà à `onDownloadPDF` prop (qui est `handleDownloadPDF` dans `app/audit/page.tsx`, déjà câblé en Task 2).
    - DÉCISION : ne PAS dupliquer ici l'event `pdf_downloaded`. Task 2 l'émet déjà depuis `handleDownloadPDF`. **Aucune modification de `AuditDashboard.tsx` n'est requise pour event 11.** Le fichier est listé dans `files_modified` du plan uniquement parce que la décision d'instrumentation doit être documentée. SI l'event 11 n'apparaissait pas en smoke prod (Plan 09-04), envisager d'ajouter un fallback `trackPdfDownloaded()` directement dans le `onClick` du bouton ici. Pour l'instant : pas de changement.

    NE PAS importer `@vercel/analytics` directement dans aucun de ces 4 fichiers.
  </action>
  <verify>
    <automated>grep -c trackCtaCalendlyClick components/audit/CTACalendly.tsx components/audit/CalendlyEmbed.tsx | grep -vE ':[01]$' | wc -l | grep -E '^[2-9]' && grep -c trackGoogleDiagnosticTriggered components/audit/DiagnosticGoogle.tsx | grep -E '^[2-9]' && grep -E 'from\s+"@vercel/analytics"' components/audit/CTACalendly.tsx components/audit/CalendlyEmbed.tsx components/audit/DiagnosticGoogle.tsx components/audit/AuditDashboard.tsx ; [ $? -ne 0 ] && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c trackCtaCalendlyClick components/audit/CTACalendly.tsx` ≥ 2
    - `grep -c trackCtaCalendlyClick components/audit/CalendlyEmbed.tsx` ≥ 2 (au moins 1 location, jusqu'à 2 si 2 boutons)
    - `grep -c trackGoogleDiagnosticTriggered components/audit/DiagnosticGoogle.tsx` ≥ 2
    - Aucun import direct `@vercel/analytics` dans les 4 fichiers
    - Les arguments de `trackCtaCalendlyClick` sont des string-littéraux du union (`"hero"`, `"footer"`, `"audit-results"`) — pas de variable libre
    - `npm run build` exit 0
    - `npm run lint` exit 0
  </acceptance_criteria>
  <done>
    Events 9 (`cta_calendly_click` ≥ 1 location), 10 (`google_diagnostic_triggered`) câblés. Event 11 documenté comme couvert par Task 2 (single source). (R2 events 9+10 ; event 11 confirmed)
  </done>
</task>

<task type="auto">
  <name>Task 5: Vérification globale anti-PII + anti-import-direct + build/lint</name>
  <files>(verification only — aucune modification)</files>
  <read_first>
    - lib/analytics.ts (rappel des 11 helpers)
    - .planning/phases/phase-09-monitoring-analytics/09-SPEC.md §AC-3 (PII guardrail)
    - .planning/phases/phase-09-monitoring-analytics/09-CONTEXT.md §D-03
  </read_first>
  <action>
    Tâche de vérification statique globale (aucune modification de code) :

    1. **D-03 — Aucun import direct de `@vercel/analytics` hors de `lib/analytics.ts`** :
       ```bash
       grep -rE 'from\s+"@vercel/analytics"' app/ components/ hooks/ | grep -v 'lib/analytics.ts'
       ```
       Doit retourner ZÉRO ligne. Si ≥1 ligne → corriger en remplaçant par un import depuis `@/lib/analytics`.

    2. **R3 — Aucun call-site ne passe de PII** :
       ```bash
       grep -rE 'track[A-Z][A-Za-z]+\([^)]*\b(email|nomCabinet|cabinet|patient|nom)\b' app/ components/ hooks/
       ```
       Doit retourner ZÉRO ligne.

    3. **R2 — Les 11 helpers sont effectivement appelés** :
       ```bash
       for fn in trackLandingView trackLandingCtaAuditClick trackAuditView trackCsvPreviewLoaded trackCsvRejected trackAuditSubmitted trackAuditSuccess trackAuditFailed trackCtaCalendlyClick trackGoogleDiagnosticTriggered trackPdfDownloaded; do
         count=$(grep -rE "\b$fn\(" app/ components/ hooks/ | wc -l)
         echo "$fn: $count call-sites"
       done
       ```
       Chaque helper DOIT apparaître ≥ 1 fois (excepté que `trackPdfDownloaded` peut être 1 seule fois — dans `app/audit/page.tsx`).

    4. **Build + lint + tests verts** :
       ```bash
       npm run build && npm run lint && npm test
       ```
       Tous exit 0.

    5. Documenter les counts par helper dans le SUMMARY de plan.
  </action>
  <verify>
    <automated>set -e; ! grep -rE 'from\s+"@vercel/analytics"' app/ components/ hooks/ 2>/dev/null | grep -v 'lib/analytics.ts' | grep . ; ! grep -rE 'track[A-Z][A-Za-z]+\([^)]*\b(email|nomCabinet|cabinet)\b' app/ components/ hooks/ 2>/dev/null | grep . ; npm run build 2>&1 | tail -3 ; npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE 'from\s+"@vercel/analytics"' app/ components/ hooks/` (excluant lib/analytics.ts) retourne 0 ligne
    - Aucun appel `trackXxx(...)` ne référence `email`/`nomCabinet`/`cabinet`/`patient`
    - Les 11 helpers ont ≥ 1 call-site dans `app/`, `components/`, ou `hooks/`
    - `npm run build` exit 0
    - `npm run lint` exit 0
    - `npm test` (suite vitest complète) exit 0
  </acceptance_criteria>
  <done>
    AC-3 (PII-free) vérifiée par grep. AC-5 (build+lint) vert. D-03 (import barrier) vérifié. R2 (11 events instrumentés) vérifiée. (R2, R3, AC-3, AC-5)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composants React → couche `lib/analytics.ts` | tous les call-sites traversent cette barrière typée |
| Composants React → `@vercel/analytics` directement | INTERDIT (D-03) — vérifié par grep en Task 5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Information Disclosure (PII leak via call-site) | `app/audit/page.tsx`, `components/audit/CSVErrorCard.tsx`, `hooks/useCSVPreview.ts` | mitigate | Signatures helpers TS-strict refusent strings PII ; Task 5 grep-check pour `email`/`nomCabinet` ; review code en SUMMARY |
| T-09-08 | Tampering (event drift via import direct) | n'importe quel composant client | mitigate | Task 5 grep barrier `from "@vercel/analytics"` hors `lib/analytics.ts` doit retourner vide ; D-03 |
| T-09-09 | Repudiation (event manqué pour mount RSC) | `app/page.tsx` | mitigate | `LandingViewTracker` client wrapper monté explicitement ; RESEARCH gotcha §1 documenté |
</threat_model>

<verification>
- [ ] R2 : 11 events instrumentés (Task 1 events 1+2 ; Task 2 events 3+6+7+8+11 ; Task 3 events 4+5 ; Task 4 events 9+10)
- [ ] R3 : aucun PII dans aucun call-site (Task 5 grep)
- [ ] AC-3 : PII-free vérifié par revue grep statique
- [ ] AC-5 : build + lint + vitest verts (Task 5)
- [ ] D-03 : aucun import direct `@vercel/analytics` hors `lib/analytics.ts` (Task 5 grep)

Final check (Task 5) :
```bash
npm run build && npm run lint && npm test && \
  ! grep -rE 'from "@vercel/analytics"' app/ components/ hooks/ | grep -v 'lib/analytics.ts' | grep .
```
</verification>

<success_criteria>
- 11 helpers ont ≥ 1 call-site fonctionnel
- 0 import direct de `@vercel/analytics` hors `lib/analytics.ts`
- 0 PII (email, nomCabinet, contenu CSV) dans les properties d'events
- `npm run build` + `npm run lint` + `npm test` tous verts
- Tous les events de mount déclenchés via `useEffect(() => {...}, [])` (pas inline render)
</success_criteria>

<output>
After completion, create `.planning/phases/phase-09-monitoring-analytics/09-03-SUMMARY.md` listant :
- Tableau des 11 events avec colonnes : event_name | helper | file:line du call-site | properties (extrait de l'argument réel)
- Output du grep Task 5 (counts par helper)
- Confirmation que `grep -rE 'from "@vercel/analytics"' app/ components/ hooks/ | grep -v lib/analytics.ts` est vide
- Output `npm run build` final (table de routes Next 14 — sera comparé au baseline en Plan 09-04)
- Output `npm test` final (X tests passed)
</output>
