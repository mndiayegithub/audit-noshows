---
phase: 09-monitoring-analytics
plan: 05
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03, 09-04]
files_modified:
  - .planning/phases/phase-09-monitoring-analytics/SUMMARY.md
  - .planning/phases/phase-09-monitoring-analytics/09-05-SMOKE-PROD-CHECKLIST.md
autonomous: false
requirements: [R6]
must_haves:
  truths:
    - "SUMMARY.md de phase liste les 11 events avec triggers, properties, file:line"
    - "Mesure bundle baseline/after est tracée dans SUMMARY"
    - "Action user 'vercel.com/status' est documentée"
    - "Checklist smoke prod manuel pour AC-1, AC-2, AC-6 est rédigée"
    - "User a confirmé manuellement les 11 events dans le dashboard Vercel < 30s post-parcours"
  artifacts:
    - path: ".planning/phases/phase-09-monitoring-analytics/SUMMARY.md"
      provides: "Documentation finale de phase (D-10)"
      contains: "11 events"
    - path: ".planning/phases/phase-09-monitoring-analytics/09-05-SMOKE-PROD-CHECKLIST.md"
      provides: "Checklist actionable pour le smoke prod manuel (D-08)"
      contains: "audit.perfiamatic.fr"
  key_links:
    - from: "SUMMARY.md"
      to: "09-04-bundle-delta.md"
      via: "section bundle"
      pattern: "delta"
    - from: "SUMMARY.md"
      to: "09-05-SMOKE-PROD-CHECKLIST.md"
      via: "lien vers procédure smoke"
      pattern: "SMOKE-PROD-CHECKLIST"
---

<objective>
Documenter Phase 9 (D-10) et exécuter le smoke prod manuel sur `audit.perfiamatic.fr` pour valider AC-1, AC-2, AC-6 (R6) — qui ne peuvent pas être testés en dev (RESEARCH §Q4).

Purpose: Sans smoke prod, les events sont câblés mais pas vérifiés en condition réelle. Le user est le seul à pouvoir cliquer le funnel sur le domaine prod et constater l'apparition dans le dashboard Vercel < 30s.

Output: SUMMARY.md de phase + checklist smoke + checkpoint user confirmé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-09-monitoring-analytics/09-SPEC.md
@.planning/phases/phase-09-monitoring-analytics/09-CONTEXT.md
@.planning/phases/phase-09-monitoring-analytics/09-RESEARCH.md
@.planning/phases/phase-09-monitoring-analytics/09-01-SUMMARY.md
@.planning/phases/phase-09-monitoring-analytics/09-02-SUMMARY.md
@.planning/phases/phase-09-monitoring-analytics/09-03-SUMMARY.md
@.planning/phases/phase-09-monitoring-analytics/09-04-SUMMARY.md
@.planning/phases/phase-09-monitoring-analytics/09-04-bundle-delta.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rédiger SUMMARY.md de phase + checklist smoke prod</name>
  <files>.planning/phases/phase-09-monitoring-analytics/SUMMARY.md, .planning/phases/phase-09-monitoring-analytics/09-05-SMOKE-PROD-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/phase-09-monitoring-analytics/09-CONTEXT.md §D-10 (4 sections requises)
    - .planning/phases/phase-09-monitoring-analytics/09-04-bundle-delta.md (chiffres bundle)
    - .planning/phases/phase-09-monitoring-analytics/09-03-SUMMARY.md (tableau call-sites)
    - lib/analytics.ts (rappel des 11 helpers)
  </read_first>
  <action>
    **Étape 1 — Créer `SUMMARY.md` de phase** avec les 4 sections lockées par D-10 :

    ```markdown
    # Phase 9 — Monitoring & Analytics (SUMMARY)

    Status: ✅ Code livré, en attente smoke prod manuel.
    Closed: 2026-04-26 (date à mettre à jour à la validation finale)

    ## 1. Tableau des 11 events instrumentés

    | # | Event name | Helper | Trigger (file:line) | Properties |
    |---|---|---|---|---|
    | 1 | landing_view | trackLandingView | components/landing/LandingViewTracker.tsx:8 | { referrer? } |
    | 2 | landing_cta_audit_click | trackLandingCtaAuditClick | components/landing/{LandingHero,LandingNav,CTABand,LandingFooter}.tsx | — |
    | 3 | audit_view | trackAuditView | app/audit/page.tsx:~50 | — |
    | 4 | csv_preview_loaded | trackCsvPreviewLoaded | hooks/useCSVPreview.ts:~40 | { nb_rdv, reco_rate } |
    | 5 | csv_rejected | trackCsvRejected | components/audit/CSVErrorCard.tsx:~25 | { error_code } |
    | 6 | audit_submitted | trackAuditSubmitted | app/audit/page.tsx:~95 | { degraded } |
    | 7 | audit_success | trackAuditSuccess | app/audit/page.tsx:~120 | { score, taux_noshow } |
    | 8 | audit_failed | trackAuditFailed | app/audit/page.tsx:~110 | { error_code } |
    | 9 | cta_calendly_click | trackCtaCalendlyClick | components/audit/{CTACalendly,CalendlyEmbed}.tsx | { location } |
    | 10 | google_diagnostic_triggered | trackGoogleDiagnosticTriggered | components/audit/DiagnosticGoogle.tsx:~65 | — |
    | 11 | pdf_downloaded | trackPdfDownloaded | app/audit/page.tsx:~145 (handleDownloadPDF) | — |

    Lignes ~ approximatives — voir 09-03-SUMMARY.md pour file:line exacts post-edit.

    ## 2. Mesure bundle (AC-4)

    Voir `09-04-bundle-delta.md` pour le détail. Synthèse :
    - Route `/` First Load JS : baseline XXX kB → after YYY kB → delta +Z kB
    - Verdict AC-4 : [pass / fail]

    ## 3. Action user — abonnement infra Vercel

    **À faire manuellement** : s'abonner aux notifications de `https://www.vercel-status.com/` (subscribe email) pour être notifié si l'infra Vercel Analytics elle-même est down. Ce n'est PAS gérable côté app (par définition, si Analytics est down, on ne peut pas l'observer depuis l'app).

    ## 4. Ouverture vers Phase 9-bis et Phase 10

    - **Phase 9-bis (à roadmaper)** : Sentry / error tracking server-side sur `/api/audit`. Déclencheur : ≥1 incident prod silencieux ou volume erreurs ≥ 1%/sem observé via les events `audit_failed`.
    - **Phase 10 (à roadmaper)** : dashboard funnel custom `/admin` (auth + Vercel Analytics API) + tracking server-side de `nb_rdv_valides`/`reco_rate` côté n8n.

    ## 5. ACs

    - [x] AC-3 PII-free (vérifié par grep, Plan 09-03 Task 5)
    - [x] AC-4 bundle ≤ +5 KB (Plan 09-04)
    - [x] AC-5 build + lint + tests verts (Plan 09-03 Task 5)
    - [ ] AC-1, AC-2, AC-6 — smoke prod manuel (voir `09-05-SMOKE-PROD-CHECKLIST.md`)

    ## 6. Smoke prod

    Voir `09-05-SMOKE-PROD-CHECKLIST.md` pour la procédure pas-à-pas.
    ```

    **Étape 2 — Créer `09-05-SMOKE-PROD-CHECKLIST.md`** :

    ```markdown
    # Phase 9 — Smoke Prod Checklist (D-08, AC-1, AC-2, AC-6)

    **Cible :** `https://audit.perfiamatic.fr` (prod uniquement — Vercel Analytics ne tracke PAS en dev, cf RESEARCH §Q4)
    **Effectué par :** user (mndiayepro97@gmail.com)
    **Quand :** après merge Plan 09-03 + redéploiement Vercel auto

    ## Pré-requis
    - [ ] Plan 09-01 → 09-04 mergés sur `main`
    - [ ] Déploiement Vercel auto-déclenché et vert (vérifier `vercel.com/dashboard`)
    - [ ] Onglet Vercel Analytics → projet `audit-no-shows` → "Custom Events" ouvert dans un onglet

    ## Parcours 1 — Golden path (events 1, 2, 3, 4, 6, 7, 9, 10, 11)

    1. [ ] Ouvrir `https://audit.perfiamatic.fr` (window incognito recommandé) → vérifier dashboard Vercel "Custom Events" voit `landing_view` < 30s
    2. [ ] Cliquer le bouton "Démarrer l'audit" depuis hero (ou nav, ou CTA band, ou footer) → `landing_cta_audit_click`
    3. [ ] Sur `/audit`, mount → `audit_view`
    4. [ ] Drag-drop un CSV valide (ex: fixture golden de Phase 7) → `csv_preview_loaded`
    5. [ ] Remplir nom_cabinet, email, lancer l'audit → `audit_submitted`
    6. [ ] Attendre la réponse (~30-50s) → `audit_success`
    7. [ ] Cliquer "Télécharger PDF" → `pdf_downloaded`
    8. [ ] Cliquer le CTA Calendly principal → `cta_calendly_click`
    9. [ ] Cliquer "Diagnostic Google" et lancer une recherche → `google_diagnostic_triggered`

    ## Parcours 2 — Reject (events 5, 8)

    1. [ ] Refaire `/audit`
    2. [ ] Drag-drop un CSV invalide (ex: missing-columns.csv de la fixture Phase 7) → `csv_rejected`
    3. [ ] Si l'audit reach handleSubmit avec erreur API → `audit_failed`

    ## Vérification finale dashboard Vercel (AC-1, AC-2, AC-6)

    - [ ] Les 11 events apparaissent dans "Custom Events" du projet `audit-no-shows` (AC-1)
    - [ ] Le funnel `landing_view → landing_cta_audit_click → audit_view → audit_submitted → audit_success → cta_calendly_click` est visualisable via les filtres natifs Vercel Analytics (AC-2)
    - [ ] Les 11 events apparaissent < 30s après la fin du parcours (AC-6)
    - [ ] Aucun event ne contient de PII (vérifier le détail des properties dans le dashboard) (AC-3 confirm)

    ## Si un event manque

    1. Ouvrir DevTools → Console → vérifier message `[Vercel Web Analytics] Track "..."` (devrait apparaître en debug même si l'event est rejetté côté serveur)
    2. Network tab → POST `_vercel/insights/event` doit succeed (200/204)
    3. Si l'event apparaît en console mais pas en dashboard : attendre 1-2 min de propagation Vercel
    4. Si l'event n'apparaît pas en console : grep le call-site, vérifier qu'il est bien dans un composant `"use client"`
    ```
  </action>
  <verify>
    <automated>test -s .planning/phases/phase-09-monitoring-analytics/SUMMARY.md && test -s .planning/phases/phase-09-monitoring-analytics/09-05-SMOKE-PROD-CHECKLIST.md && grep -c 'landing_view\|landing_cta_audit_click\|audit_view\|csv_preview_loaded\|csv_rejected\|audit_submitted\|audit_success\|audit_failed\|cta_calendly_click\|google_diagnostic_triggered\|pdf_downloaded' .planning/phases/phase-09-monitoring-analytics/SUMMARY.md | grep -E '^[1-9][0-9]+$' && grep -E 'vercel-status\.com|vercel\.com/status' .planning/phases/phase-09-monitoring-analytics/SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `SUMMARY.md` existe avec les 4 sections D-10 (events table + bundle + action user vercel-status + ouverture 9-bis/10)
    - `SUMMARY.md` mentionne les 11 event names verbatim (snake_case)
    - `09-05-SMOKE-PROD-CHECKLIST.md` existe et contient un parcours golden + parcours reject
    - La checklist mentionne `audit.perfiamatic.fr` (cible prod)
    - La checklist explicite les 3 ACs vérifiées (AC-1, AC-2, AC-6)
    - SUMMARY contient un lien vers `09-04-bundle-delta.md`
  </acceptance_criteria>
  <done>
    Documentation D-10 complète + checklist smoke prête pour exécution user. (R6 doc, prep AC-1/AC-2/AC-6)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke prod manuel par le user (AC-1, AC-2, AC-6)</name>
  <what-built>
    11 events client-side instrumentés via Vercel Web Analytics, `<Analytics />` monté dans `app/layout.tsx`, helpers typés dans `lib/analytics.ts`, Vitest vert (15-20 tests), build + lint verts, bundle delta mesuré (AC-4 ≤ +5 KB).
  </what-built>
  <how-to-verify>
    Suivre `09-05-SMOKE-PROD-CHECKLIST.md` :
    1. Vérifier que le déploiement Vercel auto post-merge est vert sur `vercel.com/dashboard`.
    2. Ouvrir `https://audit.perfiamatic.fr` en navigation privée.
    3. Exécuter le parcours golden complet (8 events attendus côté funnel principal).
    4. Exécuter le parcours reject (CSV invalide → `csv_rejected` ; éventuellement `audit_failed` si soumission via dégradé).
    5. Ouvrir Vercel dashboard → projet `audit-no-shows` → onglet Analytics → Custom Events.
    6. Confirmer que les 11 events apparaissent < 30s après la fin du parcours.
    7. Confirmer que le funnel `landing_view → landing_cta_audit_click → audit_view → audit_submitted → audit_success → cta_calendly_click` est filtrable.
    8. Vérifier dans les détails de chaque event : aucune valeur PII (pas d'email, pas de nom_cabinet, pas de contenu CSV) — uniquement `referrer` (URL), `nb_rdv` (number), `reco_rate` (number), `error_code` (enum), `degraded` (bool), `score`/`taux_noshow` (numbers), `location` (enum).

    Si un event manque ou un PII apparaît : voir section "Si un event manque" de la checklist + ouvrir un gap.
  </how-to-verify>
  <resume-signal>
    Type "approved" si les 11 events sont visibles < 30s sans PII et le funnel est filtrable. Sinon, décrire le gap (event manquant, PII détecté, latence > 30s).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → prod `audit.perfiamatic.fr` | Smoke effectué en condition réelle ; user observe dashboard Vercel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Repudiation (event silencieusement perdu en prod) | Vercel Analytics endpoint | mitigate | Smoke prod < 30s confirme la propagation ; section troubleshoot dans checklist (DevTools console + Network tab) |
| T-09-12 | Information Disclosure (PII apparu malgré tests) | Dashboard Vercel | mitigate | Étape 8 de la checklist demande explicitement à l'user de vérifier les properties détaillées de chaque event ; AC-3 review par humain en plus du grep statique |
</threat_model>

<verification>
- [ ] AC-1 : 11 events ≥ 1 occurrence dans dashboard Vercel < 30s post-parcours (Task 2)
- [ ] AC-2 : funnel filterable dans Vercel Analytics (Task 2)
- [ ] AC-6 : smoke prod parcours complet → tous events visibles < 30s (Task 2)
- [ ] R6 : dashboard Vercel exploitable
- [ ] D-10 : SUMMARY.md + lien `vercel.com/status` (Task 1)
</verification>

<success_criteria>
- SUMMARY.md de phase complet (4 sections D-10)
- Checklist smoke prod actionable
- User confirme manuellement les 3 ACs prod (AC-1, AC-2, AC-6) après déploiement
- Aucune PII observée dans le dashboard détaillé
</success_criteria>

<output>
After completion, mettre à jour `.planning/STATE.md` (Phase 9 → ✅ Validée 2026-04-26) et `.planning/ROADMAP.md` §220-233 (status badge), puis créer commit final `docs(phase-9): close-out SUMMARY + smoke prod confirmé`.
</output>
