---
phase: 01-refonte-landing-v2
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - app/page.tsx
  - archive/landings-v1/landing.html
  - archive/landings-v1/landing-preview.html
  - archive/landings-v1/mockup.html
  - archive/landings-v1/README.md
autonomous: false
requirements: [REQ-1.1, REQ-1.9, REQ-1.10, REQ-1.11, REQ-1.12, REQ-1.13]
tags: [landing, orchestration, archive, build, lighthouse]

must_haves:
  truths:
    - "`app/page.tsx` is a thin orchestration shell that imports and composes LandingNav, LandingHero, LandingHowItWorks, LandingSocialProof, LandingFaqCta, LandingFooter in that order"
    - "All three legacy HTML files (landing.html, landing-preview.html, mockup.html) are moved into archive/landings-v1/ and no longer live at repo root"
    - "archive/landings-v1/README.md documents date + reason"
    - "`npm run build` succeeds, /audit still builds, no new TS warnings"
    - "All CTAs in the landing point to /audit; no Calendly, no upload form on landing"
    - "Page works at 375px, 768px, 1440px with no horizontal overflow"
  artifacts:
    - path: "app/page.tsx"
      provides: "Thin orchestration of 6 landing components"
      contains: "LandingNav"
      min_lines: 15
    - path: "archive/landings-v1/landing.html"
      provides: "Archived v1 landing"
    - path: "archive/landings-v1/landing-preview.html"
      provides: "Archived v1 landing preview"
    - path: "archive/landings-v1/mockup.html"
      provides: "Archived v1 mockup"
    - path: "archive/landings-v1/README.md"
      provides: "Date + rationale for archival"
      contains: "2026-04"
  key_links:
    - from: "app/page.tsx"
      to: "components/landing/LandingNav.tsx"
      via: "import"
      pattern: "import \\{ LandingNav \\}"
    - from: "app/page.tsx"
      to: "components/landing/LandingFooter.tsx"
      via: "import"
      pattern: "import \\{ LandingFooter \\}"
---

<objective>
Finaliser la phase 1 en 3 mouvements : (1) réécrire `app/page.tsx` en coquille d'orchestration mince qui compose les 6 sections livrées par plans 02-04, dans l'ordre du sketch 005→008 ; (2) archiver `landing.html`, `landing-preview.html`, `mockup.html` dans `archive/landings-v1/` avec un README expliquant le snapshot (REQ-1.13) ; (3) verrouiller la phase via un checkpoint humain qui valide responsive (REQ-1.10), absence de régression sur /audit (REQ-1.12), et Lighthouse Performance ≥ 85 (REQ-1.11).

Purpose : ce plan convertit les composants isolés en page fonctionnelle livrable, nettoie la racine du repo, et fait passer les acceptance gates non-automatisables.

Output : `app/page.tsx` réduit à ~15-25 lignes, 3 HTML archivés + README, build Next.js vert, preview Vercel ou dev local validé visuellement par l'utilisateur.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/phase-01-refonte-landing-v2/01-SPEC.md
@.planning/phases/phase-01-refonte-landing-v2/01-CONTEXT.md
@.planning/phases/phase-01-refonte-landing-v2/01-RESEARCH.md
@.planning/phases/phase-01-refonte-landing-v2/01-PATTERNS.md
@.planning/phases/phase-01-refonte-landing-v2/01-VALIDATION.md
@CLAUDE.md
@app/page.tsx
@.planning/phases/phase-01-refonte-landing-v2/01-01-SUMMARY.md
@.planning/phases/phase-01-refonte-landing-v2/01-02-SUMMARY.md
@.planning/phases/phase-01-refonte-landing-v2/01-03-SUMMARY.md
@.planning/phases/phase-01-refonte-landing-v2/01-04-SUMMARY.md

<interfaces>
Components available from plans 02-04 (verified via their SUMMARYs):
- `LandingNav` from `@/components/landing/LandingNav`
- `LandingHero` from `@/components/landing/LandingHero`
- `LandingHowItWorks` from `@/components/landing/LandingHowItWorks`
- `LandingSocialProof` from `@/components/landing/LandingSocialProof`
- `LandingFaqCta` from `@/components/landing/LandingFaqCta`
- `LandingFooter` from `@/components/landing/LandingFooter`

All are named exports. None requires props.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite app/page.tsx as a thin orchestration shell composing the 6 landing sections</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (current ~891 lines — to be fully replaced)
    - .planning/phases/phase-01-refonte-landing-v2/01-PATTERNS.md (app/page.tsx (full rewrite) section)
    - All four plan-0X-SUMMARY.md (verify component names + exports)
  </read_first>
  <action>
    Replace the entire contents of `app/page.tsx` with the orchestration shell per PATTERNS.md §"app/page.tsx (full rewrite)":

    ```tsx
    import { LandingNav } from "@/components/landing/LandingNav";
    import { LandingHero } from "@/components/landing/LandingHero";
    import { LandingHowItWorks } from "@/components/landing/LandingHowItWorks";
    import { LandingSocialProof } from "@/components/landing/LandingSocialProof";
    import { LandingFaqCta } from "@/components/landing/LandingFaqCta";
    import { LandingFooter } from "@/components/landing/LandingFooter";

    export default function HomePage() {
      return (
        <div className="min-h-screen font-sans overflow-x-hidden bg-landing-bg text-landing-text">
          <LandingNav />
          <main>
            <LandingHero />
            <LandingHowItWorks />
            <LandingSocialProof />
            <LandingFaqCta />
          </main>
          <LandingFooter />
        </div>
      );
    }
    ```

    CRITICAL:
    - No `"use client"` at this level — each child carries its own directive where needed (mirrors audit-page architecture).
    - No inline `CountUpNumber`, no inline `FAQItem`, no Framer variants definitions — those are now in their respective components or already extracted in plan 01.
    - `overflow-x-hidden` on root div (REQ-1.10 fallback) — prevents any accidental horizontal scroll on small viewports.
    - Sections ordered per SPEC: Nav → Hero → How-it-works → Social proof → FAQ+Final CTA → Footer.
    - `<main>` wrap improves semantics (nav + footer stay outside, the 4 content sections inside).
    - File SHOULD be under 30 lines total. If longer, something went wrong.
  </action>
  <verify>
    <automated>wc -l app/page.tsx | awk '$1 <= 30 {exit 0} {exit 1}' && grep -q "import { LandingNav }" app/page.tsx && grep -q "import { LandingHero }" app/page.tsx && grep -q "import { LandingHowItWorks }" app/page.tsx && grep -q "import { LandingSocialProof }" app/page.tsx && grep -q "import { LandingFaqCta }" app/page.tsx && grep -q "import { LandingFooter }" app/page.tsx && grep -q "export default function HomePage" app/page.tsx && grep -q "overflow-x-hidden" app/page.tsx && grep -q "bg-landing-bg" app/page.tsx && ! grep -q "\"use client\"" app/page.tsx && ! grep -q "ContainerScroll" app/page.tsx && ! grep -q "ShuffleTestimonials" app/page.tsx && ! grep -q "FaqSection" app/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/page.tsx` is ≤ 30 lines total (thin shell)
    - 6 named imports from `@/components/landing/*` present
    - Single default export `HomePage`
    - No `"use client"` at top (server component shell)
    - No imports of legacy `ContainerScroll`, `ShuffleTestimonials`, `FaqSection` (REQ-1.1 acceptance)
    - Root wrapper has `bg-landing-bg text-landing-text overflow-x-hidden`
    - Sections in exact order: Nav → Hero → HowItWorks → SocialProof → FaqCta → Footer
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Shell d'orchestration ≤ 30 lignes, 6 sections composées dans l'ordre, typecheck vert.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Move legacy landing*.html + mockup.html into archive/landings-v1/ + write archival README (REQ-1.13)</name>
  <files>archive/landings-v1/landing.html, archive/landings-v1/landing-preview.html, archive/landings-v1/mockup.html, archive/landings-v1/README.md</files>
  <read_first>
    - landing.html (confirm exists at repo root)
    - landing-preview.html (confirm exists at repo root)
    - mockup.html (confirm exists at repo root)
    - .planning/phases/phase-01-refonte-landing-v2/01-SPEC.md (REQ-1.13 acceptance)
  </read_first>
  <action>
    1. Create directory `archive/landings-v1/` if not existing:
       ```bash
       mkdir -p archive/landings-v1
       ```
    2. Use `git mv` for each file (preserves history in the commit diff):
       ```bash
       git mv landing.html archive/landings-v1/landing.html
       git mv landing-preview.html archive/landings-v1/landing-preview.html
       git mv mockup.html archive/landings-v1/mockup.html
       ```
       If `git mv` fails because files aren't tracked, fall back to `mv` then `git add -A` — preserves content even if not history.

    3. Create `archive/landings-v1/README.md` with the following exact content:

       ```markdown
       # Landings v1 — Archivées

       **Date d'archivage :** 2026-04-22
       **Raison :** Refonte complète de la landing dans la phase 1 du milestone v2.0 (`/gsd-plan-phase 1`). La nouvelle landing vit désormais dans `app/page.tsx` + `components/landing/*` avec toggle dark/light via `next-themes`.

       Ces 3 fichiers HTML sont conservés pour référence visuelle et historique :

       - `landing.html` — direction dark premium originale (inspiration pour la v2)
       - `landing-preview.html` — variante preview statique
       - `mockup.html` — mockup exploratoire

       Ils n'ont AUCUN impact sur le build Next.js (non référencés par `app/`, `components/`, `next.config.js`).

       **Ne pas ré-importer dans la build** — utilisez `app/page.tsx` comme source de vérité.

       **Contexte complet :** voir `.planning/phases/phase-01-refonte-landing-v2/01-SPEC.md` (REQ-1.13).
       ```

    4. Verify no build dependency references these files:
       ```bash
       grep -r "landing.html\|landing-preview.html\|mockup.html" app components --include='*.ts' --include='*.tsx' --include='*.js' --include='*.json' 2>/dev/null | grep -v "archive/" || true
       ```
       The grep should return nothing (no references outside archive/). If it returns matches, investigate before proceeding.
  </action>
  <verify>
    <automated>[ -f archive/landings-v1/landing.html ] && [ -f archive/landings-v1/landing-preview.html ] && [ -f archive/landings-v1/mockup.html ] && [ -f archive/landings-v1/README.md ] && [ ! -f landing.html ] && [ ! -f landing-preview.html ] && [ ! -f mockup.html ] && grep -q "2026-04" archive/landings-v1/README.md && grep -q "Refonte complète" archive/landings-v1/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `archive/landings-v1/landing.html` exists
    - `archive/landings-v1/landing-preview.html` exists
    - `archive/landings-v1/mockup.html` exists
    - `archive/landings-v1/README.md` exists and contains date `2026-04` + rationale
    - `landing.html`, `landing-preview.html`, `mockup.html` NO LONGER exist at repo root
    - No code file under `app/` or `components/` references any of those 3 HTML filenames
  </acceptance_criteria>
  <done>3 fichiers archivés, README daté + justifié, racine du repo nettoyée (REQ-1.13).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full build + regression smoke on /audit + grep CTA uniqueness + copy audit</name>
  <files>(no file modifications — verification pass only; runs checks over the whole phase output)</files>
  <read_first>
    - .planning/phases/phase-01-refonte-landing-v2/01-SPEC.md (REQ-1.8, REQ-1.11, REQ-1.12, REQ-1.14 acceptance)
    - app/api/audit/route.ts (verify unchanged)
    - types/audit.ts (verify unchanged)
  </read_first>
  <action>
    Run the following verifications and report each outcome. Any failure blocks the checkpoint.

    1. **Build clean:**
       ```bash
       npm run build
       ```
       Expected: exit 0. Zero new TypeScript errors. Zero new ESLint warnings. `/` and `/audit` both appear in the build output route summary.

    2. **No regression on /audit (REQ-1.12):**
       ```bash
       git diff HEAD -- app/audit/page.tsx app/api/audit/route.ts types/audit.ts components/audit/ components/GaugeBenchmark.tsx components/GraphiqueParJour.tsx
       ```
       Expected: empty diff. If non-empty, investigate — only `app/audit/layout.tsx` is a NEW file (not a modification of existing ones), and ONLY `app/globals.css` changes should NOT have removed any `.mesh-bg`, `.glass-dark*`, `.glass-panel`, `.btn-glow`, `.text-gradient-*`, `.animate-float` utilities (plan 01 task 2 preserved them).

    3. **CTA uniqueness to /audit (REQ-1.8):**
       ```bash
       grep -rn "href=" components/landing/ app/page.tsx --include='*.tsx' | grep -vE "(landing-page\.md|\.md)" | sort -u
       ```
       Every `href=` starting with `/` (internal link) must be one of: `/` (logo), `/audit` (CTAs), `/mentions-legales`, `/rgpd`. Any `href` pointing elsewhere (especially `calendly` or external form URLs) is a REQ-1.8 violation.
       Also:
       ```bash
       grep -rni "calendly" components/landing/ app/page.tsx
       ```
       Expected: empty.

    4. **Copy audit (REQ-1.14):**
       ```bash
       grep -rniE "\b(leader|meilleur|révolutionnaire|optimal|#1|le numéro un)\b" components/landing/ app/page.tsx
       ```
       Expected: empty. If any match, rewrite the copy to remove the superlative.

    5. **Token hygiene — no hard-coded legacy colors (REQ-1.1):**
       ```bash
       grep -rnE "\bbg-(white|ink|black)\b|\btext-(white|black|ink)(\b|/)" components/landing/ app/page.tsx
       ```
       Expected: empty. Landing components must use `bg-landing-*` / `text-landing-*` tokens (the CTA intentionally uses `bg-landing-text text-landing-bg` which is a token, not hard-coded).

    6. **/audit regression fonctionnel (REQ-1.12 — manual smoke in checkpoint below):** a `curl` / build-only proxy check isn't sufficient here; the human checkpoint in task 4 covers the functional smoke.

    Produce a short report in the summary artifact listing PASS/FAIL for each of the 5 automated checks above.
  </action>
  <verify>
    <automated>npm run build && git diff HEAD -- app/audit/page.tsx app/api/audit/route.ts types/audit.ts components/audit/ components/GaugeBenchmark.tsx components/GraphiqueParJour.tsx | wc -l | awk '$1 == 0 {exit 0} {exit 1}' && ! grep -rni "calendly" components/landing/ app/page.tsx && ! grep -rniE "\b(leader|meilleur|révolutionnaire|optimal)\b" components/landing/ app/page.tsx && ! grep -rnE "\bbg-(white|ink|black)\b" components/landing/ app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with `/` and `/audit` both in the route manifest
    - `git diff HEAD` against audit-related files produces empty output (no regression source code touched)
    - `grep calendly` returns empty across `components/landing/` and `app/page.tsx`
    - `grep` for superlatives returns empty
    - No hard-coded `bg-white`, `bg-ink`, `bg-black` in landing components
    - All automated checks PASS; report embedded in final SUMMARY
  </acceptance_criteria>
  <done>Build vert, 0 régression /audit, CTA discipline OK, copy sans superlatif, tokens propres.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Landing v2 complète : Nav sticky + Hero split + HowItWorks 3 steps + SocialProof (marquee + 4 stats bêta) + FAQ 4 questions + Final CTA full-bleed + Footer.
    - Toggle dark/light fonctionnel (icônes soleil/système/lune) persistant via `next-themes`.
    - `/audit` protégée par `app/audit/layout.tsx` avec `forcedTheme="light"`.
    - 3 anciens HTML (`landing.html`, `landing-preview.html`, `mockup.html`) archivés dans `archive/landings-v1/` avec README.
    - Build Next.js vert, 0 régression sur /audit, 0 CTA parasite.
  </what-built>
  <how-to-verify>
    Lance le dev server (ou déploie une preview Vercel) puis vérifie les points suivants :

    ### Vérifications fonctionnelles (REQ-1.2, 1.8, 1.12)
    1. `npm run dev` — ouvre http://localhost:3000
    2. **Theme system** (REQ-1.3) : sur first load, vérifie que la page respecte le `prefers-color-scheme` de l'OS (dark par défaut sur Chrome avec DevTools > Rendering > prefers-color-scheme: dark).
    3. **Theme toggle** (REQ-1.2) : clique sur chaque icône (soleil, système, lune) dans la nav — la palette bascule instantanément sans reload. Recharge la page : le choix persiste.
    4. **No-flash** (REQ-1.2) : DevTools > Network > Slow 3G > hard reload en `dark` puis en `light` → aucun flash blanc/noir parasite au first paint.
    5. **Audit scope** (REQ-1.12) : toggle en dark sur `/`, puis navigue vers `/audit` → la page reste light. Retour sur `/` → toggle dark restauré. Dans DevTools Elements, `<html class="light">` sur `/audit`.
    6. **CTAs** (REQ-1.8) : clique chaque bouton/lien CTA visible (nav, hero primary, hero ghost "Voir un exemple", final CTA) → tous arrivent sur `/audit`. Aucun popup Calendly. Aucun formulaire d'upload visible sur la landing.
    7. **FAQ** (REQ-1.7) : vérifie exactement 4 questions, la 1re ouverte par défaut, `+` tourne 45° à l'ouverture, animation height+opacity smooth.
    8. **Marquee** (REQ-1.5) : défile smoothly, pause au hover, DevTools > Rendering > prefers-reduced-motion: reduce → marquee s'arrête complètement.
    9. **Regression /audit** : upload d'un CSV d'exemple → résultats s'affichent (KPI cards, score, gauge, chart, rapport IA, PDF téléchargeable). Aucune régression visuelle vs v1.

    ### Vérifications responsive (REQ-1.10)
    10. DevTools > Toggle device toolbar :
        - **375px** (iPhone SE) : nav compressée OK, hero en 1 col, ReportPreview stacké sous le message, marquee fluide, grid stats en 1 col, pas de scroll horizontal.
        - **768px** (iPad) : how-it-works peut rester 1 col ou 3 col selon le breakpoint `min-[860px]:`, reste lisible.
        - **1440px** (desktop large) : max-width respectés (1120-1280px), hero en 2 col avec preview en rotation, pas de contenu coupé.

    ### Vérifications performance (REQ-1.11)
    11. Lighthouse desktop — soit sur preview Vercel, soit local via Chrome DevTools > Lighthouse > Mode: Navigation > Device: Desktop > Categories: Performance only → **Score Performance ≥ 85**.
        Si < 85, causes probables : `backdrop-filter: blur` trop épais (réduire à 12-14px), mesh radial trop coûteux (retirer le 2e radial), fonts mal chargées (vérifier `next/font` variable `--font-plus-jakarta` dans computed styles).

    ### Vérifications visuelles (REQ-1.4, 1.5, 1.6, 1.7)
    12. Hero : H1 gradient (3 lignes serif/gradient), ReportPreview affiche `42 380 €` en serif gradient + score ring + 3 mini-KPIs + bar chart 7 jours, card en rotation 0.6deg.
    13. HowItWorks : 3 cards avec `01/02/03` en serif 72px gradient, tag pills, flèches entre cards en desktop.
    14. SocialProof : mention "Bêta · Données d'exemple" visible en haut, marquee avec mask fade bords, 4 stats avec barres de progression.
    15. Final CTA : halo radial violet/rose visible, H2 géant avec "en 60 secondes." en serif italic gradient, CTA pill blanc centré.

    ### Vérifications repo (REQ-1.13)
    16. `ls archive/landings-v1/` montre exactement `landing.html`, `landing-preview.html`, `mockup.html`, `README.md`. Les 3 HTML ne sont plus à la racine.

    Si tout passe : répondre "approved". Si un point échoue : lister le(s) REQ(s) concerné(s) pour déclencher un plan de gap-closure.
  </how-to-verify>
  <resume-signal>Type "approved" ou liste les problèmes (ex : "REQ-1.11 Lighthouse = 78, backdrop-filter trop lourd").</resume-signal>
</task>

</tasks>

<verification>
Full phase acceptance:
- [x] REQ-1.1 dark par défaut (verified build + visual)
- [x] REQ-1.2 toggle sans flash (checkpoint manual)
- [x] REQ-1.3 nav sticky + blur (checkpoint visual)
- [x] REQ-1.4 hero split (checkpoint visual)
- [x] REQ-1.5 social proof marquee+stats+bêta (checkpoint visual)
- [x] REQ-1.6 3 steps (checkpoint visual)
- [x] REQ-1.7 FAQ 4Q + CTA final (checkpoint visual)
- [x] REQ-1.8 CTA unique /audit (grep + checkpoint)
- [x] REQ-1.9 ReportPreview dans hero (checkpoint visual)
- [x] REQ-1.10 responsive 3 breakpoints (checkpoint)
- [x] REQ-1.11 Lighthouse ≥ 85 (checkpoint — Lighthouse run)
- [x] REQ-1.12 zéro regression /audit (grep diff + smoke)
- [x] REQ-1.13 archive HTML (auto grep)
- [x] REQ-1.14 copy sans superlatif (auto grep)
</verification>

<success_criteria>
- `app/page.tsx` ≤ 30 lignes, 6 sections composées dans l'ordre spec
- 3 HTML archivés + README 2026-04-22
- Build Next.js vert
- Diff `/audit` sources intacte (hors nouveau `app/audit/layout.tsx`)
- Pas de CTA parasite, pas de superlatif, pas de hard-coded `bg-white`/`bg-ink`
- Checkpoint humain "approved" (Lighthouse ≥ 85 + responsive OK + regression /audit OK)
</success_criteria>

<output>
After completion, create `.planning/phases/phase-01-refonte-landing-v2/01-05-SUMMARY.md` documenting:
- `wc -l app/page.tsx` result
- List of files in `archive/landings-v1/`
- Build output summary (route manifest line with `/` and `/audit`)
- Pass/fail table for the 5 automated checks from task 3
- Lighthouse score captured during checkpoint
- User's "approved" signal or list of gap-closure items
</output>
