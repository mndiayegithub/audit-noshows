---
phase: 01-refonte-landing-v2
plan: 06
type: execute
wave: 4
depends_on: [05]
files_modified: []
autonomous: false
requirements: [REQ-15]
tags: [landing, uat, verify]
must_haves:
  truths:
    - "User has visually inspected all 5 sections at mobile, tablet, and desktop breakpoints"
    - "User has clicked every primary CTA and confirmed navigation to /audit"
    - "User has opened/closed each FAQ card and confirmed chevron rotation + emerald-50 open state"
    - "User has confirmed Fraunces renders on H1s, Inter on body, and the 4 KPI pastels match sémantique"
    - "Lighthouse ≥ 90 × 4 confirmed (either from Plan 05 report or re-run)"
    - "User signs off via /gsd-verify-work that all acceptance items are verified"
  artifacts: []
  key_links: []
---

<objective>
Hand the landing to the user for visual + functional UAT using `/gsd-verify-work`. This plan is a single human checkpoint: no code changes, just structured verification of the 10 SPEC acceptance items. Any issue raised here triggers a gap-closure plan (per `/gsd-plan-phase --gaps`).

Purpose: REQ-15 requires explicit user sign-off. Without this plan, the phase cannot close.
Output: user confirms each verification item or raises gaps to close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-01-refonte-landing-v2/01-SPEC.md
@.planning/phases/phase-01-refonte-landing-v2/01-01-SUMMARY.md
@.planning/phases/phase-01-refonte-landing-v2/01-02-SUMMARY.md
@.planning/phases/phase-01-refonte-landing-v2/01-03-SUMMARY.md
@.planning/phases/phase-01-refonte-landing-v2/01-04-SUMMARY.md
@.planning/phases/phase-01-refonte-landing-v2/01-05-SUMMARY.md
@.planning/phases/phase-01-refonte-landing-v2/01-05-LIGHTHOUSE.md
@.planning/sketches/001-landing-hero-nav/index.html
@.planning/sketches/002-landing-stats-target/index.html
@.planning/sketches/003-landing-how-value/index.html
@.planning/sketches/004-landing-testimonial-faq-cta/index.html
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT — /gsd-verify-work sign-off on 5 sections, CTAs, responsive, Lighthouse</name>
  <what-built>
Landing page at `/` (http://localhost:3000/) refondue clinique-claire v2:
- Nav sticky + Hero (sketch 001 C) with framer-motion mount
- StatsBar + TargetGrid (sketch 002 B)
- HowItWorksTimeline + ValueProps + ScorePill (sketch 003 C)
- Testimonial + FAQCards (native `<details>`) + CTABand (sketch 004 B)
- LandingFooter
- Global tokens: Inter + Fraunces via next/font, bg-gray-50, primary `#064E3B`, 4 KPI pastels
- Single CTA `/audit` (no form)
- Lighthouse report captured in `01-05-LIGHTHOUSE.md`
  </what-built>
  <how-to-verify>
1. **Start dev server** (or production): `npm run dev` → open http://localhost:3000/

2. **Visual match sketches** (open each sketch HTML in a second tab for side-by-side):
   - [ ] Hero layout matches `001-landing-hero-nav/index.html` variant C: sticky nav, interrogative badge pill emerald, Fraunces H1, 2 CTAs, mini dashboard right
   - [ ] Stats bar matches `002-landing-stats-target/index.html` variant B: 3 Fraunces numbers vert sapin, vertical dividers
   - [ ] "Pour qui" grid: 4 cards (Omnipratique / Orthodontie / Implantologie / Groupes)
   - [ ] Timeline matches `003-landing-how-value/index.html` variant C: 3 steps tricolor Volume → Signal → Taux with vertical connector
   - [ ] ScorePill 72/100 with SVG ring + Fraunces score
   - [ ] Testimonial card + 4 FAQ cards + primary-dark CTA band matches `004-landing-testimonial-faq-cta/index.html` variant B

3. **Typography check** (DevTools Computed Styles):
   - [ ] On an H1: `font-family` starts with `"__Fraunces_...` (Fraunces loaded via next/font)
   - [ ] On a `<p>`: `font-family` starts with `"__Inter_...` (Inter loaded via next/font)

4. **Body background + no dark**:
   - [ ] `document.body.className` contains `bg-gray-50` and NOT `bg-ink`, `dark:`
   - [ ] No black fullscreen background — everything reads on light gray

5. **CTA navigation**:
   - [ ] Click nav "Lancer mon audit" → lands on `/audit`
   - [ ] Click hero primary CTA → lands on `/audit`
   - [ ] Click hero ghost CTA → scrolls to `#comment-ca-marche`
   - [ ] Click CTA band white button → lands on `/audit`
   - [ ] Nav link "Comment ça marche" → scrolls to `#comment-ca-marche`
   - [ ] Nav link "Pour qui" → scrolls to `#pour-qui`
   - [ ] Nav link "FAQ" → scrolls to `#faq`

6. **FAQ interaction**:
   - [ ] Click a FAQ card → expands, background becomes `bg-emerald-50`, chevron rotates 180°
   - [ ] Click again → collapses

7. **Responsive** (DevTools device toolbar):
   - [ ] Mobile 375 px: no horizontal scroll, CTA always visible, hero stacks, timeline vertical
   - [ ] Tablet 768 px: hero starts to split, FAQ stays full width, stats bar possibly stacked
   - [ ] Desktop 1440 px: hero split 2-col, stats bar horizontal, CTA band 2-col

8. **Brand**:
   - [ ] "GetLostRevenue" visible in nav logo
   - [ ] "GetLostRevenue" visible in footer (logo + copyright — 2 occurrences)

9. **Lighthouse** (DevTools → Lighthouse → Desktop → Analyze):
   - [ ] Performance ≥ 90
   - [ ] Accessibility ≥ 90
   - [ ] Best Practices ≥ 90
   - [ ] SEO ≥ 90
   - If any score < 90, note which audits failed

10. **Zero form**:
    - [ ] View source (Ctrl+U) and search for `<form` or `<input` → zero matches in the landing HTML
  </how-to-verify>
  <resume-signal>
Type "approved" if all 10 checklists pass, or describe specific gaps (e.g., "FAQ chevron doesn't rotate on Safari", "Lighthouse Performance = 84 — LCP image issue"). Gaps will be closed via `/gsd-plan-phase --gaps`.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — this plan only reads the page. No code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Repudiation | UAT sign-off | mitigate | `/gsd-verify-work` records the approval event in git history; the SUMMARY for this plan captures which items were signed. |
</threat_model>

<verification>
All 10 checklists in Task 1 must pass. Failures become gap items for `/gsd-plan-phase --gaps`.
</verification>

<success_criteria>
- REQ-15 provable: `/gsd-verify-work` returns "all items verified" OR the user explicitly approves the checkpoint
- Any gap found is logged for gap-closure planning
</success_criteria>

<output>
After completion, create `.planning/phases/phase-01-refonte-landing-v2/01-06-SUMMARY.md` recording:
- Verification timestamp
- Which of the 10 checklists passed on first try
- Any gap raised (with proposed remediation plan number)
- Final Lighthouse scores at sign-off time
</output>
