---
phase: 01-refonte-landing-v2
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - components/landing/LandingNav.tsx
  - components/landing/LandingHero.tsx
  - components/landing/MiniDashboard.tsx
autonomous: true
requirements: [REQ-05, REQ-11]
tags: [landing, nav, hero, framer-motion]
must_haves:
  truths:
    - "Sticky nav renders on page load with GetLostRevenue logo + 3 links + primary-dark CTA"
    - "Hero shows interrogative badge pill + Fraunces H1 + 2 CTAs (primary-dark filled + ghost)"
    - "Mini dashboard mockup renders 4 KPI pastel cards in correct sémantique (Volume bleu / Signal émeraude / Taux orange / Argent violet)"
    - "Hero CTA primary-dark links to /audit; ghost CTA links to #comment-ca-marche"
    - "Framer Motion drives only the hero mount (staggered fade-up, 0.4s ease-out, no spring)"
  artifacts:
    - path: components/landing/LandingNav.tsx
      provides: "Sticky nav (72px) with scroll shadow"
      contains: "GetLostRevenue"
    - path: components/landing/LandingHero.tsx
      provides: "Split 2-col hero with framer-motion mount animation"
      contains: "framer-motion"
    - path: components/landing/MiniDashboard.tsx
      provides: "4 KPI pastel mini-cards DOM mockup, no Chart.js"
      contains: "kpiVolume"
  key_links:
    - from: components/landing/LandingHero.tsx
      to: components/landing/MiniDashboard.tsx
      via: "default import, rendered in right column of hero grid"
      pattern: "import MiniDashboard"
    - from: components/landing/LandingNav.tsx
      to: "/audit"
      via: "Next Link on primary-dark CTA"
      pattern: "href=\"/audit\""
---

<objective>
Build the top of the landing: sticky nav (sketch 001 C) and hero split 2-col (sketch 001 C) with a DOM-only mini dashboard mockup showing the 4 signature KPI pastels. Framer Motion drives the hero mount only (D-01). The nav gains a scroll shadow via a scroll listener (client component).

Purpose: the hero is the first visual contract of the clinique-claire direction — KPI pastels must appear above the fold to establish the "rapport d'expertise comptable" signature.
Output: 3 new components under `components/landing/`; no page wiring yet (orchestrator comes in Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-01-refonte-landing-v2/01-SPEC.md
@.planning/phases/phase-01-refonte-landing-v2/01-CONTEXT.md
@.planning/phases/phase-01-refonte-landing-v2/01-01-SUMMARY.md
@.claude/skills/sketch-findings-landing-system-audit-noshows/SKILL.md
@.planning/sketches/001-landing-hero-nav/README.md
@.planning/sketches/001-landing-hero-nav/index.html
@new_design.md
@tailwind.config.ts

<interfaces>
<!-- Tokens exposed by Plan 01 (Tailwind utilities available): -->
- `bg-primaryDark` / `text-primaryDark` → #064E3B
- `bg-accentGreen` / `text-accentGreen` → #10B981
- `bg-kpiVolume` / `text-kpiVolume-fg` → #DFF3FF / #2563EB
- `bg-kpiSignal` / `text-kpiSignal-fg` → #DCF4E6 / #059669
- `bg-kpiTaux` / `text-kpiTaux-fg` → #FCEACC / #EA580C
- `bg-kpiArgent` / `text-kpiArgent-fg` / `text-kpiArgent-deep` → #ECCDF8 / #9333EA / #6B21A8
- `font-sans` (Inter via var), `font-serif` (Fraunces via var)
- `rounded-2xl` (20px), `rounded-3xl` (28px), `shadow-cta`

<!-- External deps available (no install needed): -->
- `framer-motion` ^12.34.3
- `lucide-react` ^1.7.0 (icons: Menu, ArrowRight, Sparkles, Check)
- `next/link`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create components/landing/MiniDashboard.tsx — 4 KPI pastel mini-cards mockup</name>
  <read_first>
    - .planning/sketches/001-landing-hero-nav/index.html (variant C markup for the KPI strip + mini dashboard fallback content)
    - .claude/skills/sketch-findings-landing-system-audit-noshows/SKILL.md (§ design_tokens, KPI sémantique figée)
    - tailwind.config.ts (confirm kpi* utilities available after Plan 01)
  </read_first>
  <files>components/landing/MiniDashboard.tsx</files>
  <action>
Create the file as an RSC (no `"use client"`). It is a pure DOM mockup of the audit dashboard — 4 KPI cards in a 2x2 grid with a placeholder mini-chart bar row. No Chart.js, no canvas, no image. Values are hardcoded sample values; the point is visual signature, not real data.

```tsx
// components/landing/MiniDashboard.tsx
// Pure DOM mockup — no charts, no data fetching.
// Render preview of the audit dashboard KPI signature inside the hero.

export default function MiniDashboard() {
  return (
    <div className="rounded-3xl border border-gray-200 bg-white p-6 shadow-sm">
      {/* Fake browser bar */}
      <div className="flex items-center gap-2 pb-4 border-b border-gray-100">
        <span className="h-2.5 w-2.5 rounded-full bg-gray-200" />
        <span className="h-2.5 w-2.5 rounded-full bg-gray-200" />
        <span className="h-2.5 w-2.5 rounded-full bg-gray-200" />
        <span className="ml-3 text-xs text-slate-500">audit.getlostrevenue.fr</span>
      </div>

      {/* 4 KPI pastels — grille 2x2, sémantique figée */}
      <div className="grid grid-cols-2 gap-3 pt-5">
        <KpiMini bg="bg-kpiVolume" fg="text-kpiVolume-fg" label="RDV analysés" value="2 148" />
        <KpiMini bg="bg-kpiSignal" fg="text-kpiSignal-fg" label="No-shows" value="164" />
        <KpiMini bg="bg-kpiTaux" fg="text-kpiTaux-fg" label="Taux" value="7,6 %" />
        <KpiMini bg="bg-kpiArgent" fg="text-kpiArgent-deep" label="CA perdu / an" value="27 k€" />
      </div>

      {/* Mini bar row émeraude — placeholder graphique, pas de lib */}
      <div className="mt-5 flex items-end gap-1.5 h-16">
        {[40, 65, 48, 78, 55, 70, 42].map((h, i) => (
          <span
            key={i}
            className="flex-1 rounded-t-sm bg-accentGreen/70"
            style={{ height: `${h}%` }}
          />
        ))}
      </div>
      <p className="mt-2 text-[11px] text-slate-500">No-shows par jour — aperçu</p>
    </div>
  );
}

function KpiMini({
  bg, fg, label, value,
}: { bg: string; fg: string; label: string; value: string }) {
  return (
    <div className={`${bg} rounded-2xl p-3`}>
      <div className={`font-serif text-xl ${fg}`}>{value}</div>
      <div className="text-[11px] text-slate-600 mt-0.5">{label}</div>
    </div>
  );
}
```

Sémantique KPI — intouchable (SKILL `<design_tokens>`):
- RDV analysés → Volume (bleu)
- No-shows → Signal (émeraude)
- Taux → Taux (orange)
- CA perdu → Argent (violet, deep on XXL values — here "27 k€" uses `text-kpiArgent-deep`)
  </action>
  <verify>
    <automated>test -f components/landing/MiniDashboard.tsx && grep -E 'kpiVolume|kpiSignal|kpiTaux|kpiArgent' components/landing/MiniDashboard.tsx | wc -l | awk '$1 >= 4 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `components/landing/MiniDashboard.tsx`
    - `grep -c "bg-kpiVolume" components/landing/MiniDashboard.tsx` returns 1
    - `grep -c "bg-kpiSignal" components/landing/MiniDashboard.tsx` returns 1
    - `grep -c "bg-kpiTaux" components/landing/MiniDashboard.tsx` returns 1
    - `grep -c "bg-kpiArgent" components/landing/MiniDashboard.tsx` returns 1
    - `grep 'text-kpiArgent-deep' components/landing/MiniDashboard.tsx` returns 1 match (CA perdu uses deep)
    - `grep -E "chart\.js|Chart\.js|canvas|<img" components/landing/MiniDashboard.tsx` returns 0 matches (pure DOM)
    - `grep '"use client"' components/landing/MiniDashboard.tsx` returns 0 matches (RSC)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    MiniDashboard renders 4 KPI pastel cards + placeholder émeraude bar row; semantic color-to-label mapping is locked.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create components/landing/LandingNav.tsx — sticky 72px nav with scroll shadow</name>
  <read_first>
    - .planning/sketches/001-landing-hero-nav/index.html (variant C header markup)
    - .claude/skills/sketch-findings-landing-system-audit-noshows/SKILL.md (§ landing_structure 1. Sticky Nav)
    - new_design.md (règles nav bg-white/90, bordure gray-200)
  </read_first>
  <files>components/landing/LandingNav.tsx</files>
  <action>
Create as client component. Adds a `shadow-sm` + `border-b` once the user has scrolled > 8 px. Uses `next/link`, `lucide-react` for the mobile menu icon (fallback if not installed: inline SVG — see notes). Height fixed at 72 px (`h-[72px]`).

```tsx
"use client";

import Link from "next/link";
import { useEffect, useState } from "react";

export default function LandingNav() {
  const [scrolled, setScrolled] = useState(false);

  useEffect(() => {
    const onScroll = () => setScrolled(window.scrollY > 8);
    onScroll();
    window.addEventListener("scroll", onScroll, { passive: true });
    return () => window.removeEventListener("scroll", onScroll);
  }, []);

  return (
    <header
      className={[
        "sticky top-0 z-40 h-[72px] bg-white/90 backdrop-blur supports-[backdrop-filter]:bg-white/75",
        "transition-shadow duration-200",
        scrolled ? "shadow-sm border-b border-gray-200" : "border-b border-transparent",
      ].join(" ")}
    >
      <nav className="mx-auto flex h-full max-w-6xl items-center justify-between px-6">
        {/* Logo */}
        <Link href="/" className="flex items-center gap-2">
          <span className="h-6 w-6 rounded-full bg-primaryDark" aria-hidden />
          <span className="font-semibold tracking-tight text-slate-900">GetLostRevenue</span>
        </Link>

        {/* Links desktop */}
        <ul className="hidden md:flex items-center gap-8 text-sm text-slate-600">
          <li><a href="#comment-ca-marche" className="hover:text-slate-900">Comment ça marche</a></li>
          <li><a href="#pour-qui" className="hover:text-slate-900">Pour qui</a></li>
          <li><a href="#faq" className="hover:text-slate-900">FAQ</a></li>
        </ul>

        {/* CTA */}
        <Link
          href="/audit"
          className="inline-flex items-center rounded-xl bg-primaryDark px-4 py-2 text-sm font-medium text-white transition-shadow hover:shadow-cta focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primaryDark focus-visible:ring-offset-2"
        >
          Lancer mon audit
        </Link>
      </nav>
    </header>
  );
}
```

Notes:
- No framer-motion here (D-01 restricts FM to hero mount only).
- Mobile: the 3 links hide under `md:`. A hamburger menu is out of scope for this phase — the CTA is always visible (SPEC REQ-12 focuses on no overflow + CTA accessible, not on a full mobile menu).
- Anchor links use `#comment-ca-marche`, `#pour-qui`, `#faq` — the sections in Plans 03 and 04 MUST set the matching `id` attributes (noted in their acceptance criteria).
  </action>
  <verify>
    <automated>test -f components/landing/LandingNav.tsx && grep -E 'sticky top-0|h-\[72px\]|GetLostRevenue|href="/audit"' components/landing/LandingNav.tsx | wc -l | awk '$1 >= 4 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File begins with `"use client"`
    - `grep 'sticky top-0' components/landing/LandingNav.tsx` returns 1 match
    - `grep 'h-\\[72px\\]' components/landing/LandingNav.tsx` returns 1 match
    - `grep 'GetLostRevenue' components/landing/LandingNav.tsx` returns 1 match
    - `grep 'href="/audit"' components/landing/LandingNav.tsx` returns 1 match
    - `grep 'bg-primaryDark' components/landing/LandingNav.tsx` returns ≥ 2 matches (pastille + CTA)
    - `grep -E '#comment-ca-marche|#pour-qui|#faq' components/landing/LandingNav.tsx` returns 3 matches
    - `grep 'framer-motion' components/landing/LandingNav.tsx` returns 0 matches (D-01)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    LandingNav is sticky 72px, shows logo + GetLostRevenue wordmark + 3 anchor links + primary-dark CTA to /audit, gains shadow/border on scroll.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create components/landing/LandingHero.tsx — split 2-col hero with framer-motion mount</name>
  <read_first>
    - .planning/sketches/001-landing-hero-nav/index.html (variant C markup for the hero)
    - .claude/skills/sketch-findings-landing-system-audit-noshows/SKILL.md (§ landing_structure 1 Hero + copy_guidelines)
    - components/landing/MiniDashboard.tsx (import target from Task 1)
    - .planning/phases/phase-01-refonte-landing-v2/01-CONTEXT.md (D-01, D-04 — FM scope & timing)
  </read_first>
  <files>components/landing/LandingHero.tsx</files>
  <action>
Create as client component (framer-motion requires it). Layout: `grid md:grid-cols-2 gap-12`, text on the left (stacked on mobile), MiniDashboard on the right. Staggered mount: badge → H1 → subtitle → CTAs → mini dashboard, each `y: 12 → 0`, `opacity: 0 → 1`, duration `0.4s`, ease `"easeOut"`, stagger `0.08s` (80 ms per D-04). Copy must follow the interrogative/factual tone (SKILL `<copy_guidelines>`).

```tsx
"use client";

import { motion } from "framer-motion";
import Link from "next/link";
import MiniDashboard from "./MiniDashboard";

const fadeUp = {
  hidden: { opacity: 0, y: 12 },
  show:   { opacity: 1, y: 0, transition: { duration: 0.4, ease: "easeOut" } },
};

const container = {
  show: { transition: { staggerChildren: 0.08 } },
};

export default function LandingHero() {
  return (
    <section className="relative overflow-hidden">
      <motion.div
        initial="hidden"
        animate="show"
        variants={container}
        className="mx-auto grid max-w-6xl gap-12 px-6 py-16 md:grid-cols-2 md:py-24 md:items-center"
      >
        {/* Left — text + CTAs */}
        <div>
          <motion.span
            variants={fadeUp}
            className="inline-flex items-center rounded-full bg-emerald-50 px-3 py-1 text-xs font-medium text-emerald-700"
          >
            Audit gratuit · 60 secondes
          </motion.span>

          <motion.h1
            variants={fadeUp}
            className="mt-5 font-serif text-4xl leading-[1.1] text-slate-900 md:text-6xl"
          >
            Combien les no-shows coûtent-ils vraiment à votre cabinet&nbsp;?
          </motion.h1>

          <motion.p
            variants={fadeUp}
            className="mt-5 max-w-xl text-base text-slate-600 md:text-lg"
          >
            Chargez votre export Doctolib. Recevez un rapport chiffré sur votre
            taux de no-shows, le CA perdu et les créneaux à risque — sans inscription.
          </motion.p>

          <motion.div variants={fadeUp} className="mt-8 flex flex-wrap items-center gap-3">
            <Link
              href="/audit"
              className="inline-flex items-center rounded-xl bg-primaryDark px-5 py-3 text-sm font-medium text-white transition-shadow hover:shadow-cta focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primaryDark focus-visible:ring-offset-2"
            >
              Lancer mon audit
            </Link>
            <a
              href="#comment-ca-marche"
              className="inline-flex items-center rounded-xl border border-gray-200 bg-white px-5 py-3 text-sm font-medium text-slate-700 hover:bg-gray-50"
            >
              Comment ça marche
            </a>
          </motion.div>
        </div>

        {/* Right — mini dashboard mockup */}
        <motion.div variants={fadeUp} className="md:pl-4">
          <MiniDashboard />
        </motion.div>
      </motion.div>
    </section>
  );
}
```

Notes:
- Header question ends with a narrow no-break space `&nbsp;?` (French typography).
- Ghost CTA targets `#comment-ca-marche` (matches Plan 03 HowItWorks id).
- Primary CTA targets `/audit` (REQ-10).
- No spring transitions (D-04: 0.4s ease-out only).
  </action>
  <verify>
    <automated>test -f components/landing/LandingHero.tsx && grep -E 'framer-motion|MiniDashboard|href="/audit"|font-serif' components/landing/LandingHero.tsx | wc -l | awk '$1 >= 4 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File begins with `"use client"`
    - `grep 'from "framer-motion"' components/landing/LandingHero.tsx` returns 1 match
    - `grep 'import MiniDashboard' components/landing/LandingHero.tsx` returns 1 match
    - `grep 'href="/audit"' components/landing/LandingHero.tsx` returns 1 match
    - `grep 'href="#comment-ca-marche"' components/landing/LandingHero.tsx` returns 1 match
    - `grep 'font-serif' components/landing/LandingHero.tsx` returns ≥ 1 match (H1 uses Fraunces)
    - `grep 'md:grid-cols-2' components/landing/LandingHero.tsx` returns 1 match (split layout)
    - `grep -E 'duration: 0\.4|ease: "easeOut"' components/landing/LandingHero.tsx` returns ≥ 1 match (D-04)
    - `grep -E 'spring|bounce' components/landing/LandingHero.tsx` returns 0 matches (D-04)
    - H1 contains an interrogative punctuation mark: `grep -E "coûtent-ils vraiment.{0,5}\\?" components/landing/LandingHero.tsx` returns 1 match
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Hero renders split 2-col with interrogative badge + Fraunces H1 + 2 CTAs + MiniDashboard; framer-motion stagger animates the mount at 0.4s ease-out, 80ms stagger.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client ← lucide-react / framer-motion | Local npm deps; versions pinned in package.json. |
| client → /audit navigation | Internal route; no form data in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Supply chain (Tampering) | framer-motion, lucide-react | accept | Versions already pinned; no new dep added this plan. |
| T-01-05 | Information Disclosure | hardcoded copy | accept | All copy is marketing; no PII. |
| T-01-06 | Tampering | anchor links `#comment-ca-marche`/`#pour-qui`/`#faq` | mitigate | Plans 03 & 04 MUST set matching `id` attributes — recorded in those plans' acceptance criteria. |
</threat_model>

<verification>
- Visual check (after Plan 05 wires the page): loading `/` shows sticky nav + interrogative badge + Fraunces H1 + MiniDashboard with 4 pastel cards in correct semantic colors
- DevTools: click "Lancer mon audit" from both nav and hero → navigates to `/audit`
- DevTools scroll: nav gains `shadow-sm` + bottom border at scrollY > 8
- Reduce-motion user-agent flag: hero still appears (opacity kept, translate cancelled by globals.css rule from Plan 01)
</verification>

<success_criteria>
- 3 new files exist under `components/landing/`
- Framer Motion scope limited to `LandingHero.tsx` (D-01)
- KPI sémantique fixe in MiniDashboard: Volume bleu, Signal émeraude, Taux orange, Argent violet
- Primary CTAs (nav + hero) both target `/audit`
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/phase-01-refonte-landing-v2/01-02-SUMMARY.md` recording:
- Component file paths + export names
- Any deviation from the sketch C markup (e.g., responsive tweaks)
- Framer-motion variants used + timing confirmation
</output>
