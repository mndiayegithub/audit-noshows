---
phase: 01-refonte-landing-v2
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - components/landing/StatsBar.tsx
  - components/landing/TargetGrid.tsx
  - components/landing/HowItWorksTimeline.tsx
  - components/landing/ValueProps.tsx
  - components/landing/ScorePill.tsx
autonomous: true
requirements: [REQ-06, REQ-07]
tags: [landing, stats, how-it-works, score]
must_haves:
  truths:
    - "StatsBar renders 3-4 factual numbers in Fraunces serif + vert sapin with vertical dividers"
    - "TargetGrid renders 4 target-cabinet cards (omnipratique / orthodontie / implantologie / groupes-réseaux) with pastille icons"
    - "HowItWorksTimeline renders 3 numbered steps with tricolor pastilles (Volume/Signal/Taux) connected by a vertical gray line"
    - "ValueProps renders check-list + floating ScorePill card (score 72/100 with mini SVG ring)"
    - "HowItWorks section has id='comment-ca-marche'; Target section has id='pour-qui' (anchor targets for nav)"
  artifacts:
    - path: components/landing/StatsBar.tsx
      provides: "Stats row on bg-white, Fraunces numbers #064E3B, vertical gray-200 dividers"
      contains: "font-serif"
    - path: components/landing/TargetGrid.tsx
      provides: "4-col target-cabinet grid with id='pour-qui'"
      contains: "id=\"pour-qui\""
    - path: components/landing/HowItWorksTimeline.tsx
      provides: "Vertical numbered timeline with id='comment-ca-marche'"
      contains: "id=\"comment-ca-marche\""
    - path: components/landing/ValueProps.tsx
      provides: "Check-list + floating ScorePill composition"
    - path: components/landing/ScorePill.tsx
      provides: "Compact score pill with mini SVG ring (72/100)"
      contains: "<svg"
  key_links:
    - from: components/landing/ValueProps.tsx
      to: components/landing/ScorePill.tsx
      via: "default import, rendered floating next to the value list"
      pattern: "import ScorePill"
    - from: components/landing/HowItWorksTimeline.tsx
      to: "KPI sémantique figée"
      via: "step 1 bg-kpiVolume, step 2 bg-kpiSignal, step 3 bg-kpiTaux"
      pattern: "kpi(Volume|Signal|Taux)"
---

<objective>
Build the middle of the landing — 4 RSC components covering sketches 002 B and 003 C: stats bar, target grid, how-it-works tricolor timeline, and value props with floating score pill. All components are RSC (no client hooks, no framer-motion — scroll fade-up is a single page-level utility applied in Plan 05). Sections set the `id` attributes that the nav anchors in Plan 02 point to.

Purpose: this block builds credibility (stats + pour qui) then explains the flow (timeline) and teases the deliverable (score pill). The tricolor timeline is the visual signature that locks the KPI sémantique (Volume/Signal/Taux) into the user's memory before they see the audit dashboard.
Output: 5 new components under `components/landing/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-01-refonte-landing-v2/01-SPEC.md
@.planning/phases/phase-01-refonte-landing-v2/01-CONTEXT.md
@.planning/phases/phase-01-refonte-landing-v2/01-01-SUMMARY.md
@.claude/skills/sketch-findings-landing-system-audit-noshows/SKILL.md
@.planning/sketches/002-landing-stats-target/README.md
@.planning/sketches/002-landing-stats-target/index.html
@.planning/sketches/003-landing-how-value/README.md
@.planning/sketches/003-landing-how-value/index.html
@new_design.md
@tailwind.config.ts

<interfaces>
<!-- Tokens from Plan 01 available as Tailwind utilities: -->
- `bg-primaryDark` (#064E3B), `text-primaryDark`
- `bg-accentGreen` (#10B981), `text-accentGreen`
- `bg-kpiVolume` / `text-kpiVolume-fg` (bleu Volume)
- `bg-kpiSignal` / `text-kpiSignal-fg` (émeraude Signal)
- `bg-kpiTaux`   / `text-kpiTaux-fg`   (orange Taux)
- `bg-kpiArgent` / `text-kpiArgent-fg` / `text-kpiArgent-deep`
- `font-serif` (Fraunces), `font-sans` (Inter)
- `rounded-2xl` (20px), `rounded-3xl` (28px)

<!-- Icons from lucide-react (already installed v1.7.0): -->
- Stethoscope, Smile, TeethOpen (fallback Smile), Building2, Check
- If any import name does not resolve at runtime in this lucide version, swap for a close-enough alternative and document in SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create StatsBar.tsx + TargetGrid.tsx (sketch 002 B)</name>
  <read_first>
    - .planning/sketches/002-landing-stats-target/index.html (variant B markup)
    - .claude/skills/sketch-findings-landing-system-audit-noshows/SKILL.md (§ landing_structure 2 + copy_guidelines: factuel, pas superlatif)
    - .planning/phases/phase-01-refonte-landing-v2/01-CONTEXT.md (§ specifics — 4 cabinets cibles)
  </read_first>
  <files>components/landing/StatsBar.tsx, components/landing/TargetGrid.tsx</files>
  <action>
Both files are RSC (no `"use client"`).

### StatsBar.tsx
Section on `bg-white` with bordure top/bottom `border-gray-200`. 3 chiffres (non-superlatifs) Fraunces serif vert sapin `#064E3B`, séparés par un `border-r border-gray-200` vertical sauf le dernier. Grid `md:grid-cols-3`, stacked on mobile.

```tsx
// components/landing/StatsBar.tsx
// RSC — pure DOM, no client hooks.

const STATS: { value: string; label: string }[] = [
  { value: "8 %",   label: "Taux de no-shows moyen secteur dentaire" },
  { value: "27 k€", label: "Manque à gagner annuel cabinet moyen" },
  { value: "3 min", label: "Temps pour recevoir votre audit" },
];

export default function StatsBar() {
  return (
    <section className="border-y border-gray-200 bg-white">
      <ul className="mx-auto grid max-w-6xl grid-cols-1 md:grid-cols-3">
        {STATS.map((s, i) => (
          <li
            key={s.label}
            className={[
              "px-8 py-10 text-center",
              i < STATS.length - 1 ? "md:border-r md:border-gray-200" : "",
            ].join(" ")}
          >
            <div className="font-serif text-4xl text-primaryDark md:text-5xl">{s.value}</div>
            <div className="mt-2 text-sm text-slate-600">{s.label}</div>
          </li>
        ))}
      </ul>
    </section>
  );
}
```

### TargetGrid.tsx
Section id `pour-qui` on `bg-gray-50` (inherits body). Eyebrow "Pour qui", titre Fraunces, then a 4-col grid of cards. Icon in a pastel pastille (neutral surface — per SKILL "radius 20 px, icône en pastille pastel variée"). Rotate through the 4 KPI pastels for surface variety (Volume/Signal/Taux/Argent) — this is surface-decorative, not semantic mapping.

```tsx
// components/landing/TargetGrid.tsx
// RSC — pure DOM.

import { Stethoscope, Smile, Sparkles, Building2 } from "lucide-react";
import type { ComponentType, SVGProps } from "react";

type Target = {
  Icon: ComponentType<SVGProps<SVGSVGElement>>;
  label: string;
  sub: string;
  pastilleBg: string;
  pastilleFg: string;
};

const TARGETS: Target[] = [
  { Icon: Stethoscope, label: "Omnipratique",    sub: "Cabinets généralistes",    pastilleBg: "bg-kpiVolume",  pastilleFg: "text-kpiVolume-fg" },
  { Icon: Smile,       label: "Orthodontie",     sub: "Cabinets spécialisés",     pastilleBg: "bg-kpiSignal",  pastilleFg: "text-kpiSignal-fg" },
  { Icon: Sparkles,    label: "Implantologie",   sub: "Cabinets chirurgicaux",    pastilleBg: "bg-kpiTaux",    pastilleFg: "text-kpiTaux-fg" },
  { Icon: Building2,   label: "Groupes & réseaux", sub: "Structures multi-sites", pastilleBg: "bg-kpiArgent",  pastilleFg: "text-kpiArgent-fg" },
];

export default function TargetGrid() {
  return (
    <section id="pour-qui" className="mx-auto max-w-6xl px-6 py-20">
      <p className="text-sm font-medium text-accentGreen">Pour qui</p>
      <h2 className="mt-2 font-serif text-3xl text-slate-900 md:text-4xl">
        Pensé pour les cabinets qui veulent chiffrer leurs no-shows.
      </h2>

      <div className="mt-10 grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
        {TARGETS.map(({ Icon, label, sub, pastilleBg, pastilleFg }) => (
          <div key={label} className="rounded-2xl border border-gray-200 bg-white p-5">
            <div className={`inline-flex h-10 w-10 items-center justify-center rounded-xl ${pastilleBg} ${pastilleFg}`}>
              <Icon aria-hidden className="h-5 w-5" />
            </div>
            <div className="mt-4 font-medium text-slate-900">{label}</div>
            <div className="mt-1 text-sm text-slate-600">{sub}</div>
          </div>
        ))}
      </div>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>test -f components/landing/StatsBar.tsx && test -f components/landing/TargetGrid.tsx && grep 'id="pour-qui"' components/landing/TargetGrid.tsx && grep 'text-primaryDark' components/landing/StatsBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist
    - `grep 'font-serif' components/landing/StatsBar.tsx` returns ≥ 1 match (Fraunces numbers)
    - `grep 'text-primaryDark' components/landing/StatsBar.tsx` returns ≥ 1 match (vert sapin)
    - `grep -E "border-r" components/landing/StatsBar.tsx` returns ≥ 1 match (vertical dividers)
    - `grep -E "\\+1000|∞|\\+\\d00 cabinets" components/landing/StatsBar.tsx` returns 0 matches (no superlatives)
    - `grep 'id="pour-qui"' components/landing/TargetGrid.tsx` returns 1 match
    - `grep -E "Omnipratique|Orthodontie|Implantologie|Groupes" components/landing/TargetGrid.tsx` returns 4 matches
    - `grep '"use client"' components/landing/StatsBar.tsx components/landing/TargetGrid.tsx` returns 0 matches (RSC)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    StatsBar shows 3 factual Fraunces numbers in vert sapin with vertical dividers; TargetGrid shows 4 cabinet-cible cards with pastel-pastille icons and carries id `pour-qui`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create HowItWorksTimeline.tsx — 3 tricolor steps with vertical connector (sketch 003 C)</name>
  <read_first>
    - .planning/sketches/003-landing-how-value/index.html (variant C markup)
    - .claude/skills/sketch-findings-landing-system-audit-noshows/SKILL.md (§ landing_structure 3 — tricolore Volume/Signal/Taux FIXE)
  </read_first>
  <files>components/landing/HowItWorksTimeline.tsx</files>
  <action>
RSC. Section id `comment-ca-marche`. 3 vertical steps: step 1 pastille Volume (bleu), step 2 Signal (émeraude), step 3 Taux (orange). Each pastille is a 40 px circle with the numéro en Fraunces ou Inter 600. A vertical connector `bg-gray-200` runs behind the pastilles (absolute positioned, offset to align with pastille center).

```tsx
// components/landing/HowItWorksTimeline.tsx
// RSC — pure DOM. Tricolor sémantique fixe: Volume → Signal → Taux.

const STEPS: { n: string; title: string; body: string; bg: string; fg: string }[] = [
  {
    n: "1",
    title: "Chargez votre export Doctolib",
    body: "Glissez votre CSV — nous supportons tous les exports standard.",
    bg: "bg-kpiVolume",
    fg: "text-kpiVolume-fg",
  },
  {
    n: "2",
    title: "On analyse vos no-shows",
    body: "Détection des créneaux à risque, des jours critiques et des motifs récurrents.",
    bg: "bg-kpiSignal",
    fg: "text-kpiSignal-fg",
  },
  {
    n: "3",
    title: "Recevez votre rapport",
    body: "Taux, CA perdu annualisé, graphiques et plan d'action — prêt en 60 secondes.",
    bg: "bg-kpiTaux",
    fg: "text-kpiTaux-fg",
  },
];

export default function HowItWorksTimeline() {
  return (
    <section id="comment-ca-marche" className="mx-auto max-w-4xl px-6 py-20">
      <p className="text-sm font-medium text-accentGreen">Comment ça marche</p>
      <h2 className="mt-2 font-serif text-3xl text-slate-900 md:text-4xl">
        Trois étapes, zéro inscription.
      </h2>

      <ol className="relative mt-12 space-y-10">
        {/* connector vertical — aligné sur le centre des pastilles (20px depuis la gauche) */}
        <span
          aria-hidden
          className="absolute left-5 top-5 bottom-5 w-px bg-gray-200"
        />

        {STEPS.map((s) => (
          <li key={s.n} className="relative flex gap-5">
            <span
              className={`relative z-10 flex h-10 w-10 flex-none items-center justify-center rounded-full ${s.bg} ${s.fg} font-serif text-lg`}
            >
              {s.n}
            </span>
            <div>
              <div className="font-medium text-slate-900">{s.title}</div>
              <p className="mt-1 text-sm text-slate-600">{s.body}</p>
            </div>
          </li>
        ))}
      </ol>
    </section>
  );
}
```

Sémantique fixe — do NOT permute:
- Step 1 = Volume (bleu `#DFF3FF`/`#2563EB`) — "Chargez l'export"
- Step 2 = Signal (émeraude `#DCF4E6`/`#059669`) — "On analyse"
- Step 3 = Taux (orange `#FCEACC`/`#EA580C`) — "Rapport"
  </action>
  <verify>
    <automated>test -f components/landing/HowItWorksTimeline.tsx && grep 'id="comment-ca-marche"' components/landing/HowItWorksTimeline.tsx && grep -cE 'kpiVolume|kpiSignal|kpiTaux' components/landing/HowItWorksTimeline.tsx | awk '$1 >= 3 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'id="comment-ca-marche"' components/landing/HowItWorksTimeline.tsx` returns 1 match
    - `grep 'bg-kpiVolume' components/landing/HowItWorksTimeline.tsx` returns ≥ 1 match (step 1)
    - `grep 'bg-kpiSignal' components/landing/HowItWorksTimeline.tsx` returns ≥ 1 match (step 2)
    - `grep 'bg-kpiTaux' components/landing/HowItWorksTimeline.tsx` returns ≥ 1 match (step 3)
    - `grep 'bg-kpiArgent' components/landing/HowItWorksTimeline.tsx` returns 0 matches (Argent reserved for money — not a workflow step)
    - `grep -E "bg-gray-200" components/landing/HowItWorksTimeline.tsx` returns ≥ 1 match (connector)
    - `grep '"use client"' components/landing/HowItWorksTimeline.tsx` returns 0 matches (RSC)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    HowItWorks renders 3 vertical steps with Volume/Signal/Taux pastilles in fixed order connected by a gray-200 vertical line; section carries id `comment-ca-marche`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create ScorePill.tsx + ValueProps.tsx (sketch 003 C value block)</name>
  <read_first>
    - .planning/sketches/003-landing-how-value/index.html (variant C — value list + floating pill markup)
    - .claude/skills/sketch-findings-landing-system-audit-noshows/SKILL.md (§ landing_structure 3 — floating score pill, not hero)
  </read_first>
  <files>components/landing/ScorePill.tsx, components/landing/ValueProps.tsx</files>
  <action>
Both RSC.

### ScorePill.tsx
Compact card `rounded-3xl` blanche bordée, radius 28 px. Mini SVG ring 80 px (stroke `#064E3B`, 72/100 progress), score `72/100` en Fraunces à droite du ring. Max width ~ 260 px. Pas centré en hero — c'est un accompagnement flottant.

```tsx
// components/landing/ScorePill.tsx
// RSC — pure SVG, no Chart.js.

export default function ScorePill() {
  const score = 72;
  const max = 100;
  const radius = 32;
  const circumference = 2 * Math.PI * radius;
  const progress = (score / max) * circumference;

  return (
    <div className="inline-flex items-center gap-4 rounded-3xl border border-gray-200 bg-white p-4 shadow-sm">
      <svg width="80" height="80" viewBox="0 0 80 80" aria-hidden>
        <circle cx="40" cy="40" r={radius} fill="none" stroke="#e5e7eb" strokeWidth="6" />
        <circle
          cx="40"
          cy="40"
          r={radius}
          fill="none"
          stroke="#064E3B"
          strokeWidth="6"
          strokeLinecap="round"
          strokeDasharray={`${progress} ${circumference}`}
          transform="rotate(-90 40 40)"
        />
        <text
          x="40"
          y="46"
          textAnchor="middle"
          fontFamily="var(--font-fraunces)"
          fontSize="20"
          fill="#064E3B"
          fontWeight="600"
        >
          {score}
        </text>
      </svg>
      <div>
        <div className="text-xs uppercase tracking-wide text-slate-500">Score cabinet</div>
        <div className="font-serif text-2xl text-slate-900">{score}/{max}</div>
      </div>
    </div>
  );
}
```

### ValueProps.tsx
Split 2-col desktop (stacked mobile). Left: 4 bullets avec `Check` lucide-react émeraude. Right: floating ScorePill au centre vertical.

```tsx
// components/landing/ValueProps.tsx
// RSC — pure DOM.

import { Check } from "lucide-react";
import ScorePill from "./ScorePill";

const REVELATIONS: string[] = [
  "Votre taux de no-shows vs. la moyenne du secteur dentaire",
  "Le CA perdu annualisé (déjà calculé, pas de re-multiplier)",
  "Les créneaux horaires et jours les plus à risque",
  "Un plan d'action priorisé en 3 leviers concrets",
];

export default function ValueProps() {
  return (
    <section className="mx-auto max-w-6xl px-6 py-20">
      <div className="grid gap-12 md:grid-cols-2 md:items-center">
        <div>
          <p className="text-sm font-medium text-accentGreen">Ce que révèle votre audit</p>
          <h2 className="mt-2 font-serif text-3xl text-slate-900 md:text-4xl">
            Un diagnostic chiffré, pas un tableau de bord générique.
          </h2>
          <ul className="mt-8 space-y-3">
            {REVELATIONS.map((text) => (
              <li key={text} className="flex items-start gap-3">
                <Check aria-hidden className="mt-0.5 h-5 w-5 flex-none text-accentGreen" />
                <span className="text-slate-700">{text}</span>
              </li>
            ))}
          </ul>
        </div>
        <div className="flex justify-center md:justify-end">
          <ScorePill />
        </div>
      </div>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>test -f components/landing/ScorePill.tsx && test -f components/landing/ValueProps.tsx && grep '<svg' components/landing/ScorePill.tsx && grep 'import ScorePill' components/landing/ValueProps.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist
    - `grep '<svg' components/landing/ScorePill.tsx` returns ≥ 1 match (pure SVG ring)
    - `grep '#064E3B' components/landing/ScorePill.tsx` returns ≥ 1 match (primary-dark stroke)
    - `grep -E "72/100|72/\\{max\\}" components/landing/ScorePill.tsx` returns ≥ 1 match
    - `grep 'font-serif' components/landing/ScorePill.tsx` returns ≥ 1 match
    - `grep 'import ScorePill' components/landing/ValueProps.tsx` returns 1 match
    - `grep 'from "lucide-react"' components/landing/ValueProps.tsx` returns 1 match
    - `grep 'text-accentGreen' components/landing/ValueProps.tsx` returns ≥ 2 matches (eyebrow + checks)
    - `grep '"use client"' components/landing/ScorePill.tsx components/landing/ValueProps.tsx` returns 0 matches
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    ScorePill renders a compact pure-SVG ring card showing 72/100 in Fraunces; ValueProps composes check-list + floating ScorePill in a 2-col layout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client ← lucide-react icons | Local dep; no network fetch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Spoofing | Stats values (8% / 27k€ / 3min) | accept | Values are honest averages per SKILL copy_guidelines; no false claims like "+1000 cabinets". |
| T-01-08 | Information Disclosure | hardcoded copy in TargetGrid/HowItWorks | accept | Marketing copy, no PII. |
| T-01-09 | Tampering | KPI sémantique (Volume/Signal/Taux) | mitigate | Acceptance criteria enforce color-to-step mapping via grep — regression blocked at plan level. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- After Plan 05 wires these into `app/page.tsx`, each section appears in order (stats → target → how → value) with the pastilles in the correct sémantique.
- Anchor nav links from Plan 02 (`#comment-ca-marche`, `#pour-qui`) scroll to the matching section.
</verification>

<success_criteria>
- 5 new components exist in `components/landing/`
- All RSC (no `"use client"`, no hooks)
- Sémantique KPI enforced: Volume bleu (step 1 + target #1), Signal émeraude (step 2 + target #2), Taux orange (step 3 + target #3), Argent violet reserved for money (not used here except as target surface accent)
- Sections expose the correct ids for nav anchors (#pour-qui, #comment-ca-marche)
</success_criteria>

<output>
After completion, create `.planning/phases/phase-01-refonte-landing-v2/01-03-SUMMARY.md` recording:
- Component file paths + export names
- Any icon substitution if a lucide-react import name was unavailable in v1.7.0
- Stats values used (to keep consistency with the rest of the landing copy)
</output>
