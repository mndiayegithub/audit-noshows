---
phase: 6
plan: 06-01
wave: 1
autonomous: true
---

# Plan 06-01 — Tests infra

## Tasks
1. Install `vitest`, `@vitest/coverage-v8`, `@playwright/test` in devDependencies. Add scripts `test`, `test:watch`, `test:e2e` to package.json.
2. Create `vitest.config.ts` (env node, alias `@/`).
3. Extract `lib/n8n-normalize.ts` (export `normalizeN8nResponse`); refactor `app/api/audit/route.ts` to import + call it.
4. Write 6 unit test files in `lib/__tests__/`: `score`, `audit-validation`, `safe-log`, `rate-limit`, `n8n-normalize`, `no-reannualization` (scans components/audit + app/audit/page.tsx for forbidden `ca_perdu* * 12` patterns).
5. Create `playwright.config.ts` (Chromium only, webServer dev:3000).
6. Create `e2e/audit-flow.spec.ts` + `e2e/fixtures/audit-response.json` + `e2e/fixtures/sample.csv`. Mock `/api/audit` + `/api/google-places` via `page.route()`. Assert ca_perdu_an display matches mock value (no re-annualization).
7. Create `.github/workflows/ci.yml`: lint + build + unit tests on push/PR (Playwright skipped).
8. Verify: `npm run test` exit 0, `npm run build` exit 0, `npm run lint` no new errors. Playwright install/run noted in SUMMARY (potentially deferred if browser download impractical in env).
