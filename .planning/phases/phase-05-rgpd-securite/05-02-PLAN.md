---
phase: 5
plan: 05-02
wave: 2
depends_on: [05-01]
files_modified:
  - app/politique-confidentialite/page.tsx (new)
  - app/mentions-legales/page.tsx (new)
  - components/Footer.tsx (new)
  - app/layout.tsx
  - app/page.tsx (or landing component)
  - app/audit/page.tsx
  - next.config.js (or .ts)
autonomous: true
requirements: [REQ-4]
---

# Plan 05-02 — Pages légales + Footer + mentions RGPD + security headers

## Tasks

### Task 1 — `app/politique-confidentialite/page.tsx`
<read_first>
- app/layout.tsx
- app/page.tsx (pour match DA)
</read_first>

<action>
Créer `app/politique-confidentialite/page.tsx` — React Server Component statique :
```tsx
export const metadata = { title: "Politique de confidentialité — PerfIAmatic Audit" };

export default function PrivacyPage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-16 text-slate-800">
      <h1 className="font-serif text-3xl font-bold mb-6 text-slate-900">Politique de confidentialité</h1>
      <p className="text-sm text-gray-500 mb-10">Dernière mise à jour : 24 avril 2026</p>
      {/* Sections (h2 + p) : Finalité, Données collectées, Base légale, Durée de conservation,
         Destinataires, Droits, DPO, Art. 9 RGPD (aucune donnée santé identifiante),
         Contact. Ton professionnel FR, ~1500-2000 chars. */}
    </main>
  );
}
```
Contenu :
- **Finalité** : Fournir un audit automatisé des no-shows dentaires à partir d'un export CSV de rendez-vous, sans collecte de données patient identifiantes.
- **Données collectées** : export CSV (date, statut, heure — **aucun nom patient**) ; nom du cabinet ; email optionnel pour recevoir le rapport.
- **Base légale** : consentement explicite de l'utilisateur (upload volontaire) + intérêt légitime du responsable de traitement.
- **Durée de conservation** : le CSV n'est pas persistant — il traverse nos serveurs pour analyse puis est purgé en mémoire à la fin de la requête. Email + nom cabinet : conservés 12 mois si rapport envoyé, supprimés sur demande.
- **Destinataires** : n8n (traitement backend, hébergé Hostinger UE), Google Places API (uniquement si l'utilisateur lance l'analyse optionnelle, nom cabinet transmis), Calendly (uniquement en cas de prise de RDV), SMTP email si option email cochée.
- **Droits** (accès, rectification, effacement, portabilité, opposition) : exerçables par email à mndiayepro97@gmail.com.
- **Art. 9 RGPD — données de santé** : les métadonnées de rendez-vous (date, statut) ne constituent pas des données de santé identifiantes dès lors qu'aucun nom patient n'est transmis. L'utilisateur garantit anonymiser son export avant upload.
- **DPO** : PerfIAmatic / Mansour Ndiaye — mndiayepro97@gmail.com.
</action>

<acceptance_criteria>
- File exists
- Contains substrings : "Finalité", "Base légale", "Art. 9", "Durée de conservation", "mndiayepro97@gmail.com"
- `npm run build` inclut la route `/politique-confidentialite`
</acceptance_criteria>

### Task 2 — `app/mentions-legales/page.tsx`
<read_first>
- app/politique-confidentialite/page.tsx (après Task 1, pour cohérence styling)
</read_first>

<action>
Créer `app/mentions-legales/page.tsx` — mêmes imports/styling que privacy.
Contenu :
- **Éditeur** : PerfIAmatic, Mansour Ndiaye — contact : mndiayepro97@gmail.com
- **Hébergeur app** : Vercel Inc., 340 S Lemon Ave #4133, Walnut, CA 91789, USA (hébergement edge UE via région fra1).
- **Hébergeur backend n8n** : Hostinger International Ltd., Lituanie (UE).
- **Propriété intellectuelle** : l'ensemble du contenu du site est protégé par le droit d'auteur ; reproduction interdite sans autorisation.
- **SIREN / statut juridique** : `[à compléter]`.
- **Médiateur de la consommation** : `[à compléter si applicable]`.
</action>

<acceptance_criteria>
- File exists
- Contains "Éditeur", "Hébergeur", "Vercel", "Hostinger", "mndiayepro97@gmail.com"
</acceptance_criteria>

### Task 3 — `components/Footer.tsx`
<read_first>
- app/layout.tsx
- app/page.tsx (DA tokens)
</read_first>

<action>
Créer `components/Footer.tsx` :
```tsx
import Link from "next/link";

export default function Footer() {
  const year = new Date().getFullYear();
  return (
    <footer className="border-t border-gray-200 bg-white py-8 mt-10">
      <div className="mx-auto max-w-6xl px-6 flex flex-col sm:flex-row items-center justify-between gap-3 text-sm text-gray-500">
        <div>© {year} PerfIAmatic · Audit No-Shows</div>
        <nav className="flex items-center gap-5">
          <Link href="/politique-confidentialite" className="hover:text-slate-900 transition">
            Politique de confidentialité
          </Link>
          <Link href="/mentions-legales" className="hover:text-slate-900 transition">
            Mentions légales
          </Link>
        </nav>
      </div>
    </footer>
  );
}
```
</action>

<acceptance_criteria>
- File exists
- Contains `politique-confidentialite`, `mentions-legales`, `new Date().getFullYear()`
</acceptance_criteria>

### Task 4 — Import Footer dans `app/layout.tsx`
<read_first>
- app/layout.tsx
</read_first>

<action>
Dans `app/layout.tsx`, importer `Footer` et l'ajouter à la fin du `<body>` (après `{children}`) :
```tsx
import Footer from "@/components/Footer";
// ...
<body>
  {children}
  <Footer />
</body>
```
</action>

<acceptance_criteria>
- `grep -n "Footer" app/layout.tsx` → ≥ 2 matches (import + usage)
</acceptance_criteria>

### Task 5 — Mention RGPD sur landing + lien sur /audit
<read_first>
- app/page.tsx
- app/audit/page.tsx (ligne ~257)
</read_first>

<action>
1. Dans `app/audit/page.tsx` ligne ~257, envelopper `"Conforme RGPD"` (ou le segment contenant) d'un `<Link href="/politique-confidentialite" className="underline decoration-dotted underline-offset-2 hover:text-slate-700">Conforme RGPD</Link>`. Import `Link from "next/link"` si absent.
2. Dans `app/page.tsx` (ou le composant de section Hero/CTA de la landing) : si ce n'est pas déjà dans le footer (qui couvre la mention via les liens), ajouter sous le CTA primaire une phrase fine : `<p className="mt-3 text-xs text-gray-500">Vos données sont traitées conformément au RGPD · Aucune donnée patient identifiante collectée. <Link href="/politique-confidentialite" className="underline decoration-dotted underline-offset-2">En savoir plus</Link></p>`. Si la structure de la landing rend ça trop invasif, se limiter à la mention via le footer (qui est maintenant présent). La présence du lien dans le footer suffit à satisfaire REQ-5.
</action>

<acceptance_criteria>
- `grep -n "politique-confidentialite" app/audit/page.tsx` → ≥ 1 match
- `grep -rn "politique-confidentialite" app/page.tsx components/` → ≥ 1 match (inclut Footer)
</acceptance_criteria>

### Task 6 — Security headers dans `next.config.*`
<read_first>
- next.config.js OR next.config.ts OR next.config.mjs (whichever exists)
</read_first>

<action>
Ajouter une fonction `async headers()` qui retourne :
```js
[{
  source: "/:path*",
  headers: [
    { key: "X-Content-Type-Options", value: "nosniff" },
    { key: "X-Frame-Options", value: "DENY" },
    { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
    { key: "Strict-Transport-Security", value: "max-age=63072000; includeSubDomains" },
  ],
}]
```
</action>

<acceptance_criteria>
- `grep -n "X-Content-Type-Options\|X-Frame-Options\|Strict-Transport-Security" next.config.*` → ≥ 3 matches
- `npm run build` exit 0
</acceptance_criteria>

## Must-haves
- 2 pages légales + footer présent partout
- Mention RGPD + lien depuis `/audit`
- 4 security headers dans Next.js config
- Build exit 0
