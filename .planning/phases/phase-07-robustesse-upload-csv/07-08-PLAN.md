---
phase: 07-robustesse-upload-csv
plan: 08
type: execute
wave: 4
depends_on: ["07-05", "07-07"]
files_modified:
  - e2e/audit-flow-ok.spec.ts
  - e2e/audit-flow-degraded.spec.ts
  - e2e/audit-flow-reject.spec.ts
autonomous: true
requirements: [REQ-7-6]
must_haves:
  truths:
    - "3 specs Playwright passent en CI : (a) golden path, (b) dégradé < 90 %, (c) refus dur < 50 %"
    - "Les 3 specs mockent /api/audit via page.route() (D-07) — aucun appel réel à n8n"
    - "Chaque spec consomme une fixture CSV du catalogue Plan 07-07 + une fixture JSON de réponse"
    - "Le test dégradé valide l'apparition du DegradedConfirmDialog + la bannière 'Audit partiel'"
    - "Le test refus valide l'apparition de la CSVErrorCard avec error_code INSUFFICIENT_DATA"
  artifacts:
    - path: "e2e/audit-flow-ok.spec.ts"
      provides: "Spec golden path: upload → preview → continue → résultats"
    - path: "e2e/audit-flow-degraded.spec.ts"
      provides: "Spec dégradé: upload → preview (badge dégradé) → modal confirm → résultats + bannière"
    - path: "e2e/audit-flow-reject.spec.ts"
      provides: "Spec refus dur: upload → preview → CSVErrorCard INSUFFICIENT_DATA"
  key_links:
    - from: "e2e/audit-flow-*.spec.ts"
      to: "e2e/fixtures/responses/*.json"
      via: "page.route + route.fulfill with body from fixture"
      pattern: "page\\.route\\("
    - from: "e2e/audit-flow-*.spec.ts"
      to: "e2e/fixtures/csv/*.csv"
      via: "page.setInputFiles for upload"
      pattern: "setInputFiles"
---

<objective>
Implémenter REQ #6 (3 specs Playwright e2e) avec D-07 (mock via `page.route()`). Couvre les 3 chemins critiques : golden path, mode dégradé, refus dur. Aucun appel à n8n — tous les tests sont déterministes < 1s/spec.

Purpose: garantir non-régression du flow upload sur les 3 scenarios métier ; le filet de sécurité avant chaque déploiement.

Output: 3 fichiers de spec, exécutables via `npm run test:e2e` (script existant en Phase 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/phase-07-robustesse-upload-csv/07-SPEC.md
@.planning/phases/phase-07-robustesse-upload-csv/07-CONTEXT.md
@app/audit/page.tsx
@components/audit/CSVPreview.tsx
@components/audit/DegradedConfirmDialog.tsx
@components/audit/CSVErrorCard.tsx
@e2e/fixtures/csv/
@e2e/fixtures/responses/
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec golden path (audit-flow-ok.spec.ts)</name>
  <files>e2e/audit-flow-ok.spec.ts</files>
  <read_first>
    - app/audit/page.tsx (post-Plan 07-05 — flow câblé)
    - components/audit/CSVPreview.tsx (sélecteurs visibles : "Continuer", "Vérifiez ce que nous avons compris")
    - e2e/fixtures/responses/audit-ok.json (Plan 07-07)
    - e2e/fixtures/csv/doctolib_6mois.csv (Plan 07-07)
    - playwright.config.ts (config existante Phase 6 — à confirmer ; si absent, ce plan suppose qu'elle est livrée par Phase 6)
  </read_first>
  <action>
    Créer `e2e/audit-flow-ok.spec.ts` :
    ```ts
    import { test, expect } from "@playwright/test";
    import path from "node:path";
    import fs from "node:fs";

    test.describe("Audit flow — golden path (REQ #6)", () => {
      test("upload Doctolib OK → preview → Continuer → rapport résultats", async ({ page }) => {
        // Mock /api/audit avec la fixture audit-ok.json
        const okBody = JSON.parse(fs.readFileSync(
          path.join(__dirname, "fixtures/responses/audit-ok.json"), "utf-8",
        ));
        await page.route("**/api/audit", async (route) => {
          await route.fulfill({
            status: 200,
            contentType: "application/json",
            body: JSON.stringify(okBody),
          });
        });

        await page.goto("/audit");

        // Remplir nom_cabinet (champ requis)
        await page.getByLabel(/nom du cabinet/i).fill("Cabinet Test");

        // Upload CSV
        const csvPath = path.join(__dirname, "fixtures/csv/doctolib_6mois.csv");
        await page.setInputFiles("input[type=file]", csvPath);

        // Le preview doit apparaître
        await expect(page.getByText(/Vérifiez ce que nous avons compris/i)).toBeVisible({ timeout: 5000 });

        // Le bouton Continuer est actif
        const continuerBtn = page.getByRole("button", { name: /^continuer$/i });
        await expect(continuerBtn).toBeEnabled();
        await continuerBtn.click();

        // Le rapport résultats s'affiche (titre du dashboard ou bannière succès)
        await expect(page.getByText(/Cabinet Test/)).toBeVisible({ timeout: 10000 });
        // PAS de bannière dégradée
        await expect(page.getByText(/Audit partiel/i)).not.toBeVisible();
      });
    });
    ```
    Attention: le sélecteur exact pour le champ "nom du cabinet" doit correspondre au label rendu par `app/audit/page.tsx`. Vérifier au moment de l'écriture (lire le fichier) — utiliser `getByLabel`, `getByPlaceholder` ou `getByRole("textbox", { name: ... })` selon ce qui est présent.
  </action>
  <verify>
    <automated>npx playwright test e2e/audit-flow-ok.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "page.route" e2e/audit-flow-ok.spec.ts` retourne 1
    - `grep -c "audit-ok.json" e2e/audit-flow-ok.spec.ts` retourne 1
    - `grep -c "doctolib_6mois.csv" e2e/audit-flow-ok.spec.ts` retourne 1
    - `grep -c "Vérifiez ce que nous avons compris" e2e/audit-flow-ok.spec.ts` retourne 1
    - `grep -c "Audit partiel" e2e/audit-flow-ok.spec.ts` retourne 1 (assertion not.toBeVisible)
    - `npx playwright test e2e/audit-flow-ok.spec.ts` exit 0
  </acceptance_criteria>
  <done>Spec golden path verte sans appel réel à n8n.</done>
</task>

<task type="auto">
  <name>Task 2: Spec dégradé (audit-flow-degraded.spec.ts)</name>
  <files>e2e/audit-flow-degraded.spec.ts</files>
  <read_first>
    - app/audit/page.tsx
    - components/audit/DegradedConfirmDialog.tsx (sélecteurs : "Données partiellement reconnues", "Continuer en mode dégradé")
    - components/audit/CSVPreview.tsx
    - e2e/fixtures/responses/audit-degraded.json
    - e2e/fixtures/csv/malformed_statuts_inconnus.csv
  </read_first>
  <action>
    Créer `e2e/audit-flow-degraded.spec.ts` :
    ```ts
    import { test, expect } from "@playwright/test";
    import path from "node:path";
    import fs from "node:fs";

    test.describe("Audit flow — mode dégradé (REQ #3 + #6)", () => {
      test("upload < 90 % reconnu → modal confirmation → bannière 'Audit partiel'", async ({ page }) => {
        const degradedBody = JSON.parse(fs.readFileSync(
          path.join(__dirname, "fixtures/responses/audit-degraded.json"), "utf-8",
        ));
        await page.route("**/api/audit", async (route) => {
          await route.fulfill({
            status: 200,
            contentType: "application/json",
            body: JSON.stringify(degradedBody),
          });
        });

        await page.goto("/audit");
        await page.getByLabel(/nom du cabinet/i).fill("Cabinet Dégradé");

        const csvPath = path.join(__dirname, "fixtures/csv/malformed_statuts_inconnus.csv");
        await page.setInputFiles("input[type=file]", csvPath);

        // Preview avec badge "Mode dégradé"
        await expect(page.getByText(/Mode dégradé/i)).toBeVisible({ timeout: 5000 });

        // Click Continuer → ouvre le DegradedConfirmDialog
        await page.getByRole("button", { name: /^continuer$/i }).click();

        // Modal Radix visible
        await expect(page.getByText(/Données partiellement reconnues/i)).toBeVisible();

        // Click "Continuer en mode dégradé"
        await page.getByRole("button", { name: /continuer en mode dégradé/i }).click();

        // Bannière "Audit partiel" dans les résultats
        await expect(page.getByText(/Audit partiel/i)).toBeVisible({ timeout: 10000 });
        await expect(page.getByText(/72/)).toBeVisible(); // ignored_count from fixture
      });
    });
    ```
  </action>
  <verify>
    <automated>npx playwright test e2e/audit-flow-degraded.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "page.route" e2e/audit-flow-degraded.spec.ts` retourne 1
    - `grep -c "audit-degraded.json" e2e/audit-flow-degraded.spec.ts` retourne 1
    - `grep -c "malformed_statuts_inconnus.csv" e2e/audit-flow-degraded.spec.ts` retourne 1
    - `grep -c "Mode dégradé" e2e/audit-flow-degraded.spec.ts` retourne at least 1
    - `grep -c "Données partiellement reconnues" e2e/audit-flow-degraded.spec.ts` retourne 1
    - `grep -c "Audit partiel" e2e/audit-flow-degraded.spec.ts` retourne 1
    - `npx playwright test e2e/audit-flow-degraded.spec.ts` exit 0
  </acceptance_criteria>
  <done>Spec dégradé verte, modal Radix testé, bannière finale validée.</done>
</task>

<task type="auto">
  <name>Task 3: Spec refus dur (audit-flow-reject.spec.ts)</name>
  <files>e2e/audit-flow-reject.spec.ts</files>
  <read_first>
    - app/audit/page.tsx
    - components/audit/CSVErrorCard.tsx (sélecteurs : "Données trop incomplètes pour produire un audit fiable", "Choisir un autre fichier")
    - components/audit/CSVPreview.tsx (badge "Sous le seuil minimum" quand willReject===true)
    - e2e/fixtures/responses/audit-reject.json
    - e2e/fixtures/csv/malformed_insufficient.csv
  </read_first>
  <action>
    Créer `e2e/audit-flow-reject.spec.ts` :
    ```ts
    import { test, expect } from "@playwright/test";
    import path from "node:path";
    import fs from "node:fs";

    test.describe("Audit flow — refus dur INSUFFICIENT_DATA (REQ #4 + #6)", () => {
      test("upload < 50 % reconnu → preview affiche refus + CSVErrorCard", async ({ page }) => {
        const rejectBody = JSON.parse(fs.readFileSync(
          path.join(__dirname, "fixtures/responses/audit-reject.json"), "utf-8",
        ));
        await page.route("**/api/audit", async (route) => {
          await route.fulfill({
            status: 400,
            contentType: "application/json",
            body: JSON.stringify(rejectBody),
          });
        });

        await page.goto("/audit");
        await page.getByLabel(/nom du cabinet/i).fill("Cabinet Refus");

        const csvPath = path.join(__dirname, "fixtures/csv/malformed_insufficient.csv");
        await page.setInputFiles("input[type=file]", csvPath);

        // Preview détecte willReject === true côté client (parseCSVForPreview)
        // Le bouton Continuer est désactivé OU absent ; la CSVErrorCard apparaît
        // Cas 1 — refus côté client direct (parseCSVForPreview classifie en willReject)
        // Cas 2 — refus côté API (le user a forcé le clic)
        // On teste le cas 2 : on click continue puis on vérifie l'erreur
        const continuerBtn = page.getByRole("button", { name: /^continuer$/i });
        // Si le bouton est désactivé (willReject côté client), c'est OK : la CSVErrorCard est déjà affichée
        const isEnabled = await continuerBtn.isEnabled().catch(() => false);
        if (isEnabled) {
          await continuerBtn.click();
        }

        // CSVErrorCard avec INSUFFICIENT_DATA
        await expect(page.getByText(/Données trop incomplètes pour produire un audit fiable/i)).toBeVisible({ timeout: 10000 });
        await expect(page.getByRole("button", { name: /Choisir un autre fichier/i })).toBeVisible();
      });
    });
    ```
  </action>
  <verify>
    <automated>npx playwright test e2e/audit-flow-reject.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "page.route" e2e/audit-flow-reject.spec.ts` retourne 1
    - `grep -c "audit-reject.json" e2e/audit-flow-reject.spec.ts` retourne 1
    - `grep -c "malformed_insufficient.csv" e2e/audit-flow-reject.spec.ts` retourne 1
    - `grep -c "INSUFFICIENT_DATA\\|Données trop incomplètes" e2e/audit-flow-reject.spec.ts` retourne au moins 1
    - `grep -c "Choisir un autre fichier" e2e/audit-flow-reject.spec.ts` retourne 1
    - `grep -c "status: 400" e2e/audit-flow-reject.spec.ts` retourne 1
    - `npx playwright test e2e/audit-flow-reject.spec.ts` exit 0
  </acceptance_criteria>
  <done>Spec refus dur verte, CSVErrorCard rendue, status 400 fulfill côté mock.</done>
</task>

</tasks>

<verification>
- `npx playwright test e2e/audit-flow-ok.spec.ts e2e/audit-flow-degraded.spec.ts e2e/audit-flow-reject.spec.ts` exit 0
- `npm run lint` exit 0
- `npm run build` exit 0
</verification>

<success_criteria>
- 3 specs e2e passantes en CI
- Tous les tests utilisent `page.route()` — `grep -L "page.route" e2e/audit-flow-*.spec.ts` ne retourne aucun fichier
- Aucun appel réel à n8n (pas de N8N_WEBHOOK_URL référencé dans les specs)
- Chaque spec consomme une fixture CSV du catalogue Plan 07-07
- Chaque spec consomme une fixture JSON de réponse Plan 07-07
</success_criteria>

<output>
Créer `.planning/phases/phase-07-robustesse-upload-csv/07-08-SUMMARY.md` après exécution.

À la fin de la phase complète (8 plans), écrire aussi un `.planning/phases/phase-07-robustesse-upload-csv/PHASE-SUMMARY.md` qui consolide les 6 REQs livrées.
</output>
