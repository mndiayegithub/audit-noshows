---
phase: 07-robustesse-upload-csv
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/api/audit/route.ts
  - lib/n8n-normalize.ts
  - lib/n8n-normalize.test.ts
  - types/audit.ts
autonomous: true
requirements: [REQ-7-2, REQ-7-3, REQ-7-4]
must_haves:
  truths:
    - "L'API /api/audit renvoie { error_code, error, details } pour les 5 codes verrouillés"
    - "L'API renvoie 200 + { degraded: true, reco_rate, ignored_count, sample_ignored } quand reco_rate ∈ [0.50, 0.90)"
    - "L'API renvoie 400 + INSUFFICIENT_DATA quand reco_rate < 0.50 OU nb_rdv_valides < 20"
    - "Les erreurs n8n techniques sont mappées sur l'un des 5 codes (ENCODING_ERROR, INVALID_DATE_FORMAT, EMPTY_AFTER_PARSING)"
  artifacts:
    - path: "app/api/audit/route.ts"
      provides: "Route API qui propage error_code typé"
      contains: "error_code"
    - path: "lib/n8n-normalize.ts"
      provides: "mapN8nErrorToCode + propagation degraded fields"
      exports: ["normalizeN8nResponse", "mapN8nErrorToCode"]
  key_links:
    - from: "app/api/audit/route.ts"
      to: "lib/audit-validation.ts"
      via: "evaluateRecognition + validateAuditPayload"
      pattern: "evaluateRecognition\\("
    - from: "app/api/audit/route.ts"
      to: "types/audit-errors.ts"
      via: "AuditErrorPayload type"
      pattern: "AuditErrorPayload"
---

<objective>
Étendre la route `/api/audit` pour qu'elle réponde au contrat d'erreur structuré (REQ #2) et propage les états dégradé/refus dur (REQ #3 + #4 backend). Étend `lib/n8n-normalize.ts` avec `mapN8nErrorToCode()` qui transforme les messages techniques n8n en `AuditErrorCode` typés.

Purpose: la couche serveur livre désormais le contrat que le frontend (07-03 à 07-06) consommera sans transformation supplémentaire.

Output: route migrée vers AuditErrorPayload, normalize étendu + testé, type AuditResponse augmenté avec champs `degraded`, `reco_rate`, `ignored_count`, `sample_ignored`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/phase-07-robustesse-upload-csv/07-SPEC.md
@.planning/phases/phase-07-robustesse-upload-csv/07-CONTEXT.md
@app/api/audit/route.ts
@lib/n8n-normalize.ts
@lib/audit-validation.ts
@lib/safe-log.ts
@types/audit.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Étendre lib/n8n-normalize.ts (mapN8nErrorToCode + propagation degraded)</name>
  <files>lib/n8n-normalize.ts, lib/n8n-normalize.test.ts, types/audit.ts</files>
  <read_first>
    - lib/n8n-normalize.ts (intégralement)
    - lib/n8n-normalize.test.ts (pattern de tests existants)
    - types/audit.ts (intégralement — étendre AuditResponse)
    - types/audit-errors.ts (créé Plan 07-01)
    - .planning/phases/phase-07-robustesse-upload-csv/07-SPEC.md (REQ #2 + #3)
    - .planning/phases/phase-07-robustesse-upload-csv/07-CONTEXT.md (D-05 mapping)
  </read_first>
  <behavior>
    - `mapN8nErrorToCode(rawError: string)` retourne :
      - "EMPTY_AFTER_PARSING" si le message contient "vide", "aucune ligne", "no row", "0 rdv"
      - "INVALID_DATE_FORMAT" si "date" ET ("invalide", "format", "parsable")
      - "ENCODING_ERROR" si "encoding", "encodage", "latin", "utf-8 invalid"
      - "INSUFFICIENT_DATA" si "trop peu", "moins de 20", "pas assez", "< 20"
      - "MISSING_COLUMNS" si "colonne", "header", "manquant"
      - fallback: "EMPTY_AFTER_PARSING" (cas non classifié)
    - `normalizeN8nResponse` étendu pour conserver les champs `degraded`, `reco_rate`, `ignored_count`, `sample_ignored` quand n8n les fournit (passthrough — ne pas inventer)
    - Test: 5+ cas pour mapN8nErrorToCode couvrant les 5 codes
    - Test: passthrough des champs dégradé via fixture wrapper {output:{success:true, stats:{...}, degraded:true, reco_rate:0.85, ignored_count:12, sample_ignored:[...]}}
  </behavior>
  <action>
    1. Étendre `types/audit.ts` `AuditResponse` (ne pas casser les usages existants — champs optionnels) :
       ```ts
       export interface AuditResponse {
         success: boolean;
         stats: AuditStats;
         rapport_texte?: string;
         pdf_url?: string;
         email_sent?: boolean;
         // Phase 7 — REQ #3
         degraded?: boolean;
         reco_rate?: number;          // 0..1
         ignored_count?: number;
         sample_ignored?: Array<{ ligne?: number; raison?: string; valeur?: string }>;
         // Phase 7 — REQ #2 (cas erreur)
         error?: string;
         error_code?: import("./audit-errors").AuditErrorCode;
         details?: Record<string, unknown>;
       }
       ```
    2. Dans `lib/n8n-normalize.ts`, ajouter et exporter `mapN8nErrorToCode(rawError: string): AuditErrorCode`. Implémenter via une chaîne de regex insensibles à la casse, ordre = priorité. **Aucun PII** dans les regex (la fn ne stocke jamais le message — elle classifie). Documenter en JSDoc que la fn est pure et idempotente.
    3. Étendre `normalizeN8nResponse` pour copier les 4 champs dégradé (`degraded`, `reco_rate`, `ignored_count`, `sample_ignored`) du wrapper output vers le top-level si présents. Conserver le comportement legacy si absents.
    4. Ajouter ≥ 8 tests Vitest dans `lib/n8n-normalize.test.ts` :
       - 5 tests mapN8nErrorToCode (un par code)
       - 1 test fallback "EMPTY_AFTER_PARSING" pour message non classifié
       - 2 tests passthrough degraded fields (un avec wrapper {output:{...}}, un avec direct)
    5. Ne pas modifier la fn `sanitizeRapportTexte` existante.
  </action>
  <verify>
    <automated>npx vitest run lib/n8n-normalize.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function mapN8nErrorToCode" lib/n8n-normalize.ts` retourne 1
    - `grep -c "degraded" lib/n8n-normalize.ts` retourne au moins 2 (déclaration + assignment)
    - `grep -c "sample_ignored" lib/n8n-normalize.ts` retourne au moins 1
    - `grep -c "error_code" types/audit.ts` retourne 1
    - `npx vitest run lib/n8n-normalize.test.ts` exit 0 avec ≥ 8 tests passants
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>mapN8nErrorToCode exporté + testé pour 5 codes ; AuditResponse étendu sans breaking change.</done>
</task>

<task type="auto">
  <name>Task 2: Migrer app/api/audit/route.ts vers AuditErrorPayload + propagation degraded/reject</name>
  <files>app/api/audit/route.ts</files>
  <read_first>
    - app/api/audit/route.ts (intégralement)
    - lib/audit-validation.ts (post-Plan 07-01: ValidationResult étendu, evaluateRecognition exporté)
    - lib/n8n-normalize.ts (post-Task 1: mapN8nErrorToCode + degraded passthrough)
    - lib/safe-log.ts (logger sans PII — Phase 5)
    - types/audit-errors.ts
    - .planning/phases/phase-07-robustesse-upload-csv/07-SPEC.md (REQ #2 acceptance: chaque code testable)
  </read_first>
  <action>
    1. Lire entièrement la route. Identifier les 4 zones d'extension :
       a. branche d'erreur de `validateAuditPayload` — quand `result.ok === false`, si la branche porte `error_code`, retourner status 400 avec body `{ success: false, error_code, error, details }` ; sinon (legacy email/nom_cabinet) garder status 400 avec `{ success: false, error: result.error }` (compatibilité descendante).
       b. réponse n8n parsée: si `normalizeN8nResponse` retourne `{ success: false, error: "..." }`, classifier via `mapN8nErrorToCode(error)` et renvoyer `{ success: false, error_code, error, details: { source: "n8n" } }` avec status 400.
       c. réponse n8n parsée success=true: appeler `evaluateRecognition(reco_rate, nb_rdv_valides)` quand n8n fournit ces champs (sinon outcome="ok" par défaut). Si outcome="reject", retourner 400 + payload INSUFFICIENT_DATA fourni par evaluateRecognition. Si outcome="degraded", retourner 200 avec body `{ ...auditResponse, degraded: true, reco_rate, ignored_count, sample_ignored }` (passthrough des champs n8n). Si outcome="ok", comportement legacy.
       d. erreur réseau / timeout n8n (catch): garder le 502/500 existant mais enrichir avec `error_code: "EMPTY_AFTER_PARSING"` et un message FR clair (ne pas leak la stack trace ni le N8N_WEBHOOK_URL). Utiliser `safeLog` Phase 5.
    2. Conserver `maxDuration = 60`, l'origin allowlist, le rate-limit (Phase 5). **Aucune** modification de ces blocs.
    3. Ne pas leak de PII dans les `details` retournés au client : seuls les champs métier (reco_rate, nb_rdv_valides, missing columns) sont permis. Pas de `csv_text`, pas d'`email`, pas de `nom_cabinet` dans `details`.
    4. Importer le type `AuditErrorPayload` depuis `@/types/audit-errors` pour annoter les retours d'erreur structurés.
    5. Ne pas créer de tests dédiés ici (couverts par les tests de lib/audit-validation.test.ts + lib/n8n-normalize.test.ts + e2e Playwright Plan 07-08). Mais s'assurer que le type-check est strict.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "evaluateRecognition" app/api/audit/route.ts` retourne au moins 1
    - `grep -c "mapN8nErrorToCode" app/api/audit/route.ts` retourne au moins 1
    - `grep -c "AuditErrorPayload" app/api/audit/route.ts` retourne au moins 1
    - `grep -c "error_code" app/api/audit/route.ts` retourne au moins 3
    - `grep -c "degraded" app/api/audit/route.ts` retourne au moins 2
    - `grep -E "csv_text|nom_cabinet|email" app/api/audit/route.ts | grep -i "details" | wc -l` retourne 0 (aucune PII dans details)
    - `npm run build` exit 0
    - `npm run lint` exit 0
  </acceptance_criteria>
  <done>Route propage les 5 error_code, INSUFFICIENT_DATA déclenché par evaluateRecognition, champs degraded passés au client, aucun PII leak.</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/n8n-normalize.test.ts lib/audit-validation.test.ts lib/audit-thresholds.test.ts` exit 0
- `npm run build` exit 0
- `npm run lint` exit 0
- Manuel : un curl avec FormData sans `csv` → 400 avec body contenant `error_code:"EMPTY_AFTER_PARSING"` (sera vérifié par l'e2e Plan 07-08)
</verification>

<success_criteria>
- Route migrée : tous les retours d'erreur CSV portent un `error_code` typé parmi les 5
- `evaluateRecognition` câblé sur la réponse n8n success
- Champs `degraded`, `reco_rate`, `ignored_count`, `sample_ignored` propagés en passthrough
- Aucun PII dans les `details` ni les logs (`safeLog` utilisé)
- Build + lint verts
</success_criteria>

<output>
Créer `.planning/phases/phase-07-robustesse-upload-csv/07-02-SUMMARY.md` après exécution.
</output>
