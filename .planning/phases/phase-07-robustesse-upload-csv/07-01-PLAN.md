---
phase: 07-robustesse-upload-csv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/audit-thresholds.ts
  - lib/audit-thresholds.test.ts
  - lib/audit-validation.ts
  - lib/audit-validation.test.ts
  - types/audit-errors.ts
autonomous: true
requirements: [REQ-7-2, REQ-7-4]
must_haves:
  truths:
    - "Les 3 seuils métier sont exposés dans un seul fichier source"
    - "validateAuditPayload renvoie un ValidationResult étendu avec error_code typé"
    - "Le code INSUFFICIENT_DATA est déclenché si reco_rate < 0.50 OU nb_rdv_valides < 20"
  artifacts:
    - path: "lib/audit-thresholds.ts"
      provides: "DEGRADED_THRESHOLD, REJECT_THRESHOLD, MIN_RDV_VALIDES constants"
      contains: "export const DEGRADED_THRESHOLD = 0.90"
    - path: "types/audit-errors.ts"
      provides: "AuditErrorCode union + AuditErrorPayload interface"
      contains: "MISSING_COLUMNS"
    - path: "lib/audit-validation.ts"
      provides: "validateAuditPayload returning typed ValidationResult"
  key_links:
    - from: "lib/audit-validation.ts"
      to: "lib/audit-thresholds.ts"
      via: "import { REJECT_THRESHOLD, MIN_RDV_VALIDES }"
      pattern: "from \"./audit-thresholds\""
---

<objective>
Établir la single-source-of-truth pour (a) les 3 seuils métier (D-06) et (b) le contrat d'erreur typé `{ error_code, error, details }` (REQ #2). Étend `lib/audit-validation.ts` pour produire un `ValidationResult` discriminé qui porte l'`error_code` au lieu d'un string libre, et ajoute la logique INSUFFICIENT_DATA basée sur les seuils (REQ #4 backend).

Purpose: pose les fondations utilisées par tous les plans downstream (route API, CSVPreview, error card, fixtures, e2e). Sans ce plan, aucun autre ne peut être contrat-fidèle.

Output: 3 nouveaux fichiers (thresholds, types d'erreurs, tests), 1 fichier étendu (audit-validation), tests Vitest verts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/phase-07-robustesse-upload-csv/07-SPEC.md
@.planning/phases/phase-07-robustesse-upload-csv/07-CONTEXT.md
@lib/audit-validation.ts
@lib/n8n-normalize.ts
@CLAUDE.md

<interfaces>
<!-- Extrait pertinent de lib/audit-validation.ts (existing pattern à étendre) -->

Pattern actuel: `validateAuditPayload(formData: FormData)` retourne
```ts
type ValidationResult =
  | { ok: true; values: { csv: string; nom_cabinet: string; ca_moyen: number; email?: string } }
  | { ok: false; error: string };
```

Cible: étendre la branche d'erreur pour porter `error_code` typé.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Créer lib/audit-thresholds.ts + tests</name>
  <files>lib/audit-thresholds.ts, lib/audit-thresholds.test.ts</files>
  <read_first>
    - .planning/phases/phase-07-robustesse-upload-csv/07-CONTEXT.md (D-06 verbatim)
    - .planning/phases/phase-07-robustesse-upload-csv/07-SPEC.md (REQ #3 + #4)
    - lib/audit-validation.ts (style de module à émuler)
    - lib/n8n-normalize.ts (pattern test Vitest existant à émuler)
  </read_first>
  <behavior>
    - Test: DEGRADED_THRESHOLD === 0.90
    - Test: REJECT_THRESHOLD === 0.50
    - Test: MIN_RDV_VALIDES === 20
    - Test: les 3 valeurs sont des `number` (pas string), invariants documentés en JSDoc
  </behavior>
  <action>
    Créer `lib/audit-thresholds.ts` strictement avec ce contenu (per D-06 verbatim, ne pas re-formuler les commentaires) :
    ```ts
    /**
     * Seuils métier audit no-shows — single source of truth (D-06 phase 7).
     * Consommé par: components/audit/CSVPreview.tsx, app/api/audit/route.ts, lib/audit-validation.ts.
     * NE PAS dupliquer ces valeurs ailleurs (drift dev/prod garanti).
     */
    export const DEGRADED_THRESHOLD = 0.90; // < 90% reconnus → confirmation utilisateur (REQ #3)
    export const REJECT_THRESHOLD = 0.50;   // < 50% reconnus → 400 INSUFFICIENT_DATA (REQ #4)
    export const MIN_RDV_VALIDES = 20;      // < 20 RDV valides → 400 INSUFFICIENT_DATA (REQ #4)
    ```
    Créer `lib/audit-thresholds.test.ts` avec 3 tests Vitest (`it` blocks): valeur exacte de chacune des 3 constantes, plus un test `typeof` qui assert `number`. Pas de logique : c'est un sanity-check pour empêcher un drift accidentel.
  </action>
  <verify>
    <automated>npx vitest run lib/audit-thresholds.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DEGRADED_THRESHOLD = 0.90" lib/audit-thresholds.ts` retourne 1
    - `grep -c "REJECT_THRESHOLD = 0.50" lib/audit-thresholds.ts` retourne 1
    - `grep -c "MIN_RDV_VALIDES = 20" lib/audit-thresholds.ts` retourne 1
    - `npx vitest run lib/audit-thresholds.test.ts` exit 0 avec 3+ tests passants
  </acceptance_criteria>
  <done>3 constantes exportées, tests Vitest verts, fichier minimaliste sans logique.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Créer types/audit-errors.ts (AuditErrorCode + AuditErrorPayload)</name>
  <files>types/audit-errors.ts</files>
  <read_first>
    - .planning/phases/phase-07-robustesse-upload-csv/07-SPEC.md (REQ #2 — 5 codes verbatim)
    - .planning/phases/phase-07-robustesse-upload-csv/07-CONTEXT.md (D-05 mapping code → titre/hint)
    - types/audit.ts (pattern d'export existant)
  </read_first>
  <behavior>
    - Le type `AuditErrorCode` est l'union exacte des 5 littéraux : `"MISSING_COLUMNS" | "INVALID_DATE_FORMAT" | "EMPTY_AFTER_PARSING" | "ENCODING_ERROR" | "INSUFFICIENT_DATA"`
    - L'interface `AuditErrorPayload` a les 3 champs : `error_code: AuditErrorCode`, `error: string` (FR), `details?: Record<string, unknown>`
    - `success: false` est inclus côté API response wrapper
  </behavior>
  <action>
    Créer `types/audit-errors.ts` avec exactement :
    ```ts
    /**
     * Contrat d'erreur structurée — Phase 7 REQ #2.
     * Codes verrouillés dans 07-SPEC.md (5 codes). Ne pas en ajouter sans amender la SPEC.
     */
    export type AuditErrorCode =
      | "MISSING_COLUMNS"
      | "INVALID_DATE_FORMAT"
      | "EMPTY_AFTER_PARSING"
      | "ENCODING_ERROR"
      | "INSUFFICIENT_DATA";

    export interface AuditErrorPayload {
      success: false;
      error_code: AuditErrorCode;
      error: string;        // Message FR à afficher (carte d'erreur D-05)
      details?: Record<string, unknown>;
    }

    /** Utilitaire helper pour produire une erreur typée (consommé par lib/audit-validation.ts et app/api/audit/route.ts). */
    export function makeAuditError(
      error_code: AuditErrorCode,
      error: string,
      details?: Record<string, unknown>,
    ): AuditErrorPayload {
      return { success: false, error_code, error, ...(details ? { details } : {}) };
    }
    ```
    Pas de tests dédiés (purement déclaratif) — la couverture vient de l'usage dans audit-validation.test.ts (Task 3).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MISSING_COLUMNS" types/audit-errors.ts` retourne au moins 1
    - `grep -c "INVALID_DATE_FORMAT" types/audit-errors.ts` retourne au moins 1
    - `grep -c "EMPTY_AFTER_PARSING" types/audit-errors.ts` retourne au moins 1
    - `grep -c "ENCODING_ERROR" types/audit-errors.ts` retourne au moins 1
    - `grep -c "INSUFFICIENT_DATA" types/audit-errors.ts` retourne au moins 1
    - `grep -c "export function makeAuditError" types/audit-errors.ts` retourne 1
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Type union, interface et helper exportés, type-check passe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Étendre lib/audit-validation.ts avec error_code + INSUFFICIENT_DATA</name>
  <files>lib/audit-validation.ts, lib/audit-validation.test.ts</files>
  <read_first>
    - lib/audit-validation.ts (intégralement — c'est la cible d'extension)
    - types/audit-errors.ts (créé Task 2)
    - lib/audit-thresholds.ts (créé Task 1)
    - .planning/phases/phase-07-robustesse-upload-csv/07-SPEC.md (REQ #2 acceptance: chaque code a un test)
  </read_first>
  <behavior>
    - Test: payload sans csv → `ok:false, error_code:"EMPTY_AFTER_PARSING"`
    - Test: csv > 2 Mo → `ok:false, error_code:"EMPTY_AFTER_PARSING"` ou nouveau code dédié si plus naturel (à préserver compatibilité Phase 5 — taille reste un cas d'erreur)
    - Test: CSV sans colonne `date` → `ok:false, error_code:"MISSING_COLUMNS", details.missing:["date"]`
    - Test: CSV sans colonne `statut|status` → `ok:false, error_code:"MISSING_COLUMNS", details.missing:["statut"]`
    - Test: email invalide → conserve message FR existant, error_code à choisir (réutiliser MISSING_COLUMNS si pas de code dédié, ou laisser sans error_code si email reste check non-CSV — décision: introduire `error_code:"INVALID_INPUT"` ? **NON** — la SPEC verrouille 5 codes. Email invalide reste une erreur 400 mais on lui assigne `error_code:"MISSING_COLUMNS"` est faux ; conserver une branche `ok:false, error: string` sans error_code pour les erreurs non-CSV est acceptable). **Décision arbitrée :** garder le ValidationResult discriminé existant pour les erreurs non-CSV (email, nom_cabinet) avec branche `{ ok:false, error: string }` ; étendre uniquement la branche CSV avec `{ ok:false, error_code, error, details }`. Documenter ce choix en JSDoc.
    - Test (REQ #4): helper exporté `evaluateRecognition(reco_rate: number, nb_rdv_valides: number)` retourne `{ outcome: "reject" | "degraded" | "ok", error?: AuditErrorPayload }` :
      - reco_rate < 0.50 → reject avec INSUFFICIENT_DATA
      - nb_rdv_valides < 20 → reject avec INSUFFICIENT_DATA
      - reco_rate < 0.90 (et passe les seuils reject) → degraded
      - sinon → ok
  </behavior>
  <action>
    1. Lire intégralement `lib/audit-validation.ts` (≤ 200 lignes attendu).
    2. Étendre le type `ValidationResult` :
       ```ts
       import type { AuditErrorCode, AuditErrorPayload } from "@/types/audit-errors";
       import { REJECT_THRESHOLD, MIN_RDV_VALIDES, DEGRADED_THRESHOLD } from "./audit-thresholds";

       export type ValidationResult =
         | { ok: true; values: ValidatedValues }
         | { ok: false; error: string }                          // erreurs hors-CSV (email, nom_cabinet)
         | { ok: false; error_code: AuditErrorCode; error: string; details?: Record<string, unknown> }; // erreurs CSV structurées
       ```
    3. Pour les checks CSV (taille, colonnes manquantes), retourner la branche structurée avec `error_code` adéquat :
       - taille > 2 Mo OU csv vide → `error_code: "EMPTY_AFTER_PARSING"`, message FR existant
       - colonne `date` manquante → `error_code: "MISSING_COLUMNS"`, `details: { missing: ["date"] }`
       - colonne statut manquante → `error_code: "MISSING_COLUMNS"`, `details: { missing: ["statut"] }`
       - les deux manquantes → `details: { missing: ["date", "statut"] }`
    4. Ajouter et exporter un helper pur `evaluateRecognition` :
       ```ts
       export type RecognitionOutcome =
         | { outcome: "ok" }
         | { outcome: "degraded"; reco_rate: number }
         | { outcome: "reject"; error: AuditErrorPayload };

       export function evaluateRecognition(reco_rate: number, nb_rdv_valides: number): RecognitionOutcome {
         if (reco_rate < REJECT_THRESHOLD || nb_rdv_valides < MIN_RDV_VALIDES) {
           return {
             outcome: "reject",
             error: {
               success: false,
               error_code: "INSUFFICIENT_DATA",
               error: `Données trop incomplètes pour produire un audit fiable (${Math.round(reco_rate * 100)} % reconnus, ${nb_rdv_valides} RDV valides — il faut au moins 50 % et 20 RDV).`,
               details: { reco_rate, nb_rdv_valides, min_reco_rate: REJECT_THRESHOLD, min_rdv_valides: MIN_RDV_VALIDES },
             },
           };
         }
         if (reco_rate < DEGRADED_THRESHOLD) return { outcome: "degraded", reco_rate };
         return { outcome: "ok" };
       }
       ```
    5. Créer `lib/audit-validation.test.ts` avec un test par scenario décrit dans `<behavior>` — au moins 8 tests. Utiliser `new FormData()` + `Blob` pour fabriquer les inputs. Aucun PII dans les fixtures (ASCII fictif uniquement, contrainte Phase 5).
    6. Conserver les messages FR existants verbatim (ne pas réécrire le wording legacy).
  </action>
  <verify>
    <automated>npx vitest run lib/audit-validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from \"@/types/audit-errors\"" lib/audit-validation.ts` retourne 1
    - `grep -c "from \"./audit-thresholds\"" lib/audit-validation.ts` retourne 1
    - `grep -c "export function evaluateRecognition" lib/audit-validation.ts` retourne 1
    - `grep -c "MISSING_COLUMNS" lib/audit-validation.ts` retourne au moins 1
    - `grep -c "INSUFFICIENT_DATA" lib/audit-validation.ts` retourne au moins 1
    - `npx vitest run lib/audit-validation.test.ts` exit 0 avec ≥ 8 tests passants
    - `npx tsc --noEmit` exit 0
    - `npm run lint` exit 0
  </acceptance_criteria>
  <done>ValidationResult discriminé étendu, helper evaluateRecognition exporté + testé, aucune régression sur les checks Phase 5 existants.</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/audit-thresholds.test.ts lib/audit-validation.test.ts` exit 0
- `npx tsc --noEmit` exit 0
- `npm run lint` exit 0
- `npm run build` exit 0 (attention: pas de breaking change pour `app/api/audit/route.ts` qui consomme déjà ValidationResult — ajouter des branches d'union ne casse pas, mais le type-check de la route révélera tout call-site à mettre à jour. Si la route ne compile plus à cause de la nouvelle branche d'union, c'est OK : le plan 07-02 la migrera.)
</verification>

<success_criteria>
- 3 constantes seuil exposées dans `lib/audit-thresholds.ts`
- 5 codes d'erreur déclarés dans `types/audit-errors.ts`
- `evaluateRecognition()` exporté et testé pour les 3 outcomes (ok / degraded / reject)
- Tests Vitest passent (≥ 11 tests cumulés sur les 2 fichiers test)
- Aucune valeur de seuil dupliquée ailleurs dans le repo (`grep -rn "0\.90\|0\.50" lib/ app/` ne révèle pas de seuil hardcodé hors `lib/audit-thresholds.ts`)
</success_criteria>

<output>
Créer `.planning/phases/phase-07-robustesse-upload-csv/07-01-SUMMARY.md` après exécution.
</output>
