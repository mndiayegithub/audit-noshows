---
phase: 07-robustesse-upload-csv
plan: 06
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - components/audit/CSVErrorCard.tsx
  - lib/mapErrorToCard.ts
  - lib/mapErrorToCard.test.ts
autonomous: true
requirements: [REQ-7-2]
must_haves:
  truths:
    - "Le composant <CSVErrorCard> rend une carte inline (pas modal) avec icône Alert, titre, message FR et hint actionnable"
    - "Les 5 codes d'erreur ont chacun un titre et un hint distincts (mapping verbatim D-05)"
    - "Le bouton 'Choisir un autre fichier' est ghost slate-500 (pas primary)"
    - "Le composant lit le payload AuditErrorPayload sans transformation supplémentaire"
  artifacts:
    - path: "components/audit/CSVErrorCard.tsx"
      provides: "Carte d'erreur inline pour les 5 codes structurés"
      min_lines: 50
    - path: "lib/mapErrorToCard.ts"
      provides: "Pure fn: AuditErrorCode → { title, hint } verbatim D-05"
      exports: ["mapErrorToCard"]
  key_links:
    - from: "components/audit/CSVErrorCard.tsx"
      to: "lib/mapErrorToCard.ts"
      via: "import + invocation in render"
      pattern: "mapErrorToCard\\("
---

<objective>
Implémenter la carte d'erreur inline (D-05) — REQ #2 frontend. Composant `<CSVErrorCard>` qui consomme un `AuditErrorPayload` et affiche le mapping titre/hint verrouillé pour chacun des 5 codes. Pure fn `mapErrorToCard()` séparée pour permettre les tests unitaires.

Purpose: l'utilisateur reçoit un message FR clair + une action concrète (renommer colonne, réenregistrer en UTF-8, etc.) — élimine les abandons sur erreur 500.

Output: 1 composant client, 1 pure fn + tests Vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/phase-07-robustesse-upload-csv/07-SPEC.md
@.planning/phases/phase-07-robustesse-upload-csv/07-CONTEXT.md
@types/audit-errors.ts
@tailwind.config.ts
@new_design.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Créer lib/mapErrorToCard.ts (pure fn) + tests Vitest</name>
  <files>lib/mapErrorToCard.ts, lib/mapErrorToCard.test.ts</files>
  <read_first>
    - .planning/phases/phase-07-robustesse-upload-csv/07-CONTEXT.md (D-05 mapping verbatim — 5 lignes)
    - types/audit-errors.ts (Plan 07-01)
  </read_first>
  <behavior>
    - Test: mapErrorToCard("MISSING_COLUMNS", { missing: ["date"] }) → titre = "Colonnes obligatoires manquantes", hint contient "Renommez vos colonnes en `date` et `statut`"
    - Test: mapErrorToCard("INVALID_DATE_FORMAT") → titre = "Format de date non reconnu", hint contient "JJ/MM/AAAA"
    - Test: mapErrorToCard("EMPTY_AFTER_PARSING") → titre = "CSV vide ou aucune ligne lisible", hint contient "fichier n&apos;est pas corrompu" OU "n'est pas corrompu"
    - Test: mapErrorToCard("ENCODING_ERROR") → titre = "Encodage non supporté", hint contient "UTF-8"
    - Test: mapErrorToCard("INSUFFICIENT_DATA", { reco_rate: 0.42, nb_rdv_valides: 14 }) → hint interpole "42 %" et "14 RDV"
    - Test: mapErrorToCard avec un code inconnu (cast force) → fallback titre/hint génériques (pas de throw)
  </behavior>
  <action>
    Créer `lib/mapErrorToCard.ts` avec mapping verbatim D-05 :
    ```ts
    import type { AuditErrorCode } from "@/types/audit-errors";

    export interface ErrorCardContent {
      title: string;
      hint: string;
    }

    export function mapErrorToCard(
      code: AuditErrorCode | string,
      details?: Record<string, unknown>,
    ): ErrorCardContent {
      switch (code) {
        case "MISSING_COLUMNS":
          return {
            title: "Colonnes obligatoires manquantes",
            hint: "Renommez vos colonnes en `date` et `statut`.",
          };
        case "INVALID_DATE_FORMAT":
          return {
            title: "Format de date non reconnu",
            hint: "Format attendu : JJ/MM/AAAA (ex : 12/03/2026).",
          };
        case "EMPTY_AFTER_PARSING":
          return {
            title: "CSV vide ou aucune ligne lisible",
            hint: "Vérifiez que votre fichier n'est pas corrompu.",
          };
        case "ENCODING_ERROR":
          return {
            title: "Encodage non supporté",
            hint: "Réenregistrez en UTF-8 (Excel : Fichier > Enregistrer sous > CSV UTF-8).",
          };
        case "INSUFFICIENT_DATA": {
          const recoRate = typeof details?.reco_rate === "number" ? details.reco_rate : 0;
          const nbValides = typeof details?.nb_rdv_valides === "number" ? details.nb_rdv_valides : 0;
          return {
            title: "Données trop incomplètes pour produire un audit fiable",
            hint: `${Math.round(recoRate * 100)} % reconnus, ${nbValides} RDV valides — il faut au moins 50 % et 20 RDV.`,
          };
        }
        default:
          return {
            title: "Une erreur est survenue",
            hint: "Vérifiez votre fichier et réessayez.",
          };
      }
    }
    ```
    Créer `lib/mapErrorToCard.test.ts` avec ≥ 6 tests Vitest (un par code + fallback). Vérifier exact match sur les titres (verrouillés D-05).
  </action>
  <verify>
    <automated>npx vitest run lib/mapErrorToCard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Colonnes obligatoires manquantes" lib/mapErrorToCard.ts` retourne 1
    - `grep -c "Format de date non reconnu" lib/mapErrorToCard.ts` retourne 1
    - `grep -c "CSV vide ou aucune ligne lisible" lib/mapErrorToCard.ts` retourne 1
    - `grep -c "Encodage non supporté" lib/mapErrorToCard.ts` retourne 1
    - `grep -c "Données trop incomplètes" lib/mapErrorToCard.ts` retourne 1
    - `grep -c "export function mapErrorToCard" lib/mapErrorToCard.ts` retourne 1
    - `npx vitest run lib/mapErrorToCard.test.ts` exit 0 avec ≥ 6 tests passants
  </acceptance_criteria>
  <done>Pure fn livrée + testée, mapping verbatim D-05 conforme.</done>
</task>

<task type="auto">
  <name>Task 2: Créer composant CSVErrorCard (rendu inline)</name>
  <files>components/audit/CSVErrorCard.tsx</files>
  <read_first>
    - lib/mapErrorToCard.ts (Task 1)
    - types/audit-errors.ts
    - tailwind.config.ts (palette: kpiTaux orange, kpiSignal emerald, primary #064E3B)
    - new_design.md (DA clinique-claire)
    - components/audit/AuditDashboard.tsx (style cohérent)
  </read_first>
  <action>
    Créer `components/audit/CSVErrorCard.tsx` (`"use client"`) :
    ```tsx
    "use client";

    import { AlertTriangle } from "lucide-react";
    import { mapErrorToCard } from "@/lib/mapErrorToCard";
    import type { AuditErrorCode } from "@/types/audit-errors";

    export interface CSVErrorCardProps {
      error: {
        error_code: AuditErrorCode | string;
        error?: string;
        details?: Record<string, unknown>;
      };
      onRetry: () => void;
    }

    export default function CSVErrorCard({ error, onRetry }: CSVErrorCardProps) {
      const { title, hint } = mapErrorToCard(error.error_code, error.details);
      return (
        <section
          role="alert"
          aria-live="polite"
          className="rounded-xl border border-amber-200 bg-amber-50/60 p-6"
        >
          <div className="flex items-start gap-3">
            <div className="flex-shrink-0 rounded-full bg-amber-100 p-2">
              <AlertTriangle className="h-5 w-5 text-amber-700" aria-hidden="true" />
            </div>
            <div className="flex-1">
              <h3 className="font-serif text-lg font-semibold text-slate-900">{title}</h3>
              {error.error && (
                <p className="mt-1 text-sm text-slate-700">{error.error}</p>
              )}
              <p className="mt-2 text-sm text-slate-600">{hint}</p>
              <div className="mt-4">
                <button
                  type="button"
                  onClick={onRetry}
                  className="rounded-lg px-4 py-2 text-sm font-medium text-slate-600 hover:bg-white hover:text-slate-900 focus:outline-none focus-visible:ring-2 focus-visible:ring-slate-300"
                >
                  Choisir un autre fichier
                </button>
              </div>
            </div>
          </div>
        </section>
      );
    }
    ```
    Style verrouillé per D-05 : carte inline (pas modal), palette `bg-amber-50/60 border-amber-200`, bouton ghost slate-500 (pas primary). JSX strict : `&apos;` si apostrophes (pas requis ici, mais à respecter si on en ajoute).
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\"use client\"" components/audit/CSVErrorCard.tsx` retourne 1
    - `grep -c "mapErrorToCard" components/audit/CSVErrorCard.tsx` retourne au moins 1
    - `grep -c "role=\"alert\"" components/audit/CSVErrorCard.tsx` retourne 1
    - `grep -c "aria-live" components/audit/CSVErrorCard.tsx` retourne 1
    - `grep -c "onRetry" components/audit/CSVErrorCard.tsx` retourne au moins 2
    - `grep -c "Choisir un autre fichier" components/audit/CSVErrorCard.tsx` retourne 1
    - `grep -E "bg-rose|bg-red" components/audit/CSVErrorCard.tsx | wc -l` retourne 0 (pas de rouge — D-05 specifie amber/kpiTaux)
    - `npm run build` exit 0
    - `npm run lint` exit 0
  </acceptance_criteria>
  <done>Carte d'erreur inline livrée, ARIA correct, palette amber/slate respectée, build + lint verts.</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/mapErrorToCard.test.ts` exit 0
- `npm run build` exit 0
- `npm run lint` exit 0
</verification>

<success_criteria>
- mapErrorToCard pure-fn livrée + 6 tests verts
- 5 codes mappés verbatim selon D-05
- `<CSVErrorCard>` standalone, ARIA `role="alert" aria-live="polite"`, bouton ghost
</success_criteria>

<output>
Créer `.planning/phases/phase-07-robustesse-upload-csv/07-06-SUMMARY.md` après exécution.
</output>
