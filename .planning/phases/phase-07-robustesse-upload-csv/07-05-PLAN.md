---
phase: 07-robustesse-upload-csv
plan: 05
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04", "07-06"]
files_modified:
  - app/audit/page.tsx
autonomous: true
requirements: [REQ-7-1, REQ-7-2, REQ-7-3, REQ-7-4]
must_haves:
  truths:
    - "Le user voit le preview après sélection (la dropzone est remplacée)"
    - "Le bouton Continuer du preview ouvre le DegradedConfirmDialog si degraded === true"
    - "Le bouton Continuer du preview lance le POST direct si degraded === false"
    - "Une erreur de parsing client (ENCODING_ERROR, MISSING_COLUMNS) affiche la CSVErrorCard à la place de la dropzone"
    - "Une erreur API (5 codes) affiche la CSVErrorCard à la place du preview"
    - "willReject === true désactive le bouton Continuer + montre la CSVErrorCard avec INSUFFICIENT_DATA"
    - "Le rapport résultats affiche une bannière 'Audit partiel : X lignes ignorées' si degraded === true"
  artifacts:
    - path: "app/audit/page.tsx"
      provides: "Flow upload câblé: dropzone → preview → (dialog?) → POST → résultats"
      contains: "CSVPreview"
  key_links:
    - from: "app/audit/page.tsx"
      to: "components/audit/CSVPreview.tsx"
      via: "import + render conditional sur file state"
      pattern: "<CSVPreview"
    - from: "app/audit/page.tsx"
      to: "components/audit/DegradedConfirmDialog.tsx"
      via: "import + render with degraded state"
      pattern: "<DegradedConfirmDialog"
    - from: "app/audit/page.tsx"
      to: "components/audit/CSVErrorCard.tsx"
      via: "import + render on error state"
      pattern: "<CSVErrorCard"
---

<objective>
Câbler tous les composants Phase 7 dans `app/audit/page.tsx` : la dropzone est remplacée par `<CSVPreview>` après sélection, le clic Continuer route soit vers le `<DegradedConfirmDialog>` (si `degraded`) soit vers le POST direct, les erreurs (parse client OU API) basculent sur `<CSVErrorCard>`, et le résultat affiche une bannière dégradée si `response.degraded === true`.

Purpose: livrer le flow complet à l'utilisateur — c'est ce plan qui matérialise REQ #1 + #3 + #4 visuellement.

Output: 1 fichier modifié (app/audit/page.tsx). Aucun composant nouveau ici.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/phase-07-robustesse-upload-csv/07-SPEC.md
@.planning/phases/phase-07-robustesse-upload-csv/07-CONTEXT.md
@app/audit/page.tsx
@components/audit/CSVPreview.tsx
@components/audit/DegradedConfirmDialog.tsx
@components/audit/CSVErrorCard.tsx
@components/audit/AuditDashboard.tsx
@types/audit.ts
@types/audit-errors.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Étendre la machine d'état + intégrer CSVPreview/Dialog/ErrorCard</name>
  <files>app/audit/page.tsx</files>
  <read_first>
    - app/audit/page.tsx (intégralement — 200+ lignes)
    - components/audit/CSVPreview.tsx (Plan 07-03)
    - components/audit/DegradedConfirmDialog.tsx (Plan 07-04)
    - components/audit/CSVErrorCard.tsx (Plan 07-06)
    - types/audit.ts (champs degraded/reco_rate/ignored_count étendus Plan 07-02)
    - types/audit-errors.ts (Plan 07-01)
  </read_first>
  <action>
    1. Étendre l'état `Etat` :
       ```ts
       type Etat = "formulaire" | "preview" | "loading" | "resultats" | "erreur";
       ```
       Ajouter aussi un state séparé pour l'erreur structurée (parse client OU API) :
       ```ts
       type StructuredError = { error_code: AuditErrorCode; error: string; details?: Record<string, unknown> } | null;
       const [structuredError, setStructuredError] = useState<StructuredError>(null);
       const [degradedDialogOpen, setDegradedDialogOpen] = useState(false);
       const [pendingDegradedFlag, setPendingDegradedFlag] = useState(false);  // marqué quand l'utilisateur a confirmé le mode dégradé
       ```
    2. Modifier le flow `onDrop` : après `setFile(acceptedFiles[0])`, basculer `setEtat("preview")` au lieu de rester sur "formulaire". La dropzone n'est rendue que si `etat === "formulaire"`. Si `etat === "preview"`, rendre `<CSVPreview file={file} ... />`. Si `structuredError !== null`, rendre `<CSVErrorCard error={structuredError} onRetry={handleResetUpload} />` à la place de la dropzone OU du preview selon le contexte d'origine.
    3. Wiring `<CSVPreview>` :
       ```tsx
       <CSVPreview
         file={file}
         onContinue={(degraded) => {
           if (degraded) {
             setDegradedDialogOpen(true);  // Plan 07-04 modal
           } else {
             handleSubmit(false);
           }
         }}
         onCancel={handleResetUpload}
         onError={(err) => { setStructuredError(err); setEtat("formulaire"); }}
       />
       ```
       avec `handleResetUpload = () => { setFile(null); setStructuredError(null); setEtat("formulaire"); }`.
    4. Wiring `<DegradedConfirmDialog>` rendu en parallèle (toujours monté, contrôlé par `degradedDialogOpen`). Récupérer `recoRate / ignoredCount / totalRows` depuis le `result.preview` actuel (lifter via callback ou re-parser — le plus simple: stocker un `previewSnapshot` state quand `<CSVPreview>` passe en ready ; ajouter une prop `onReady(snapshot)` au composant si pas déjà présente). **Décision arbitrée :** ajouter `onReady?: (snapshot: { recoRate: number; ignoredCount: number; totalRows: number; degraded: boolean; willReject: boolean }) => void` au composant `<CSVPreview>` (édition mineure permise sur Plan 07-03, mais préférable: faire en Plan 07-05 si pas anticipé). Si Plan 07-03 ne l'a pas exposé, l'ajouter ici.
       ```tsx
       <DegradedConfirmDialog
         open={degradedDialogOpen}
         onOpenChange={setDegradedDialogOpen}
         recoRate={previewSnapshot?.recoRate ?? 0}
         ignoredCount={previewSnapshot?.ignoredCount ?? 0}
         totalRows={previewSnapshot?.totalRows ?? 0}
         onConfirm={() => { setPendingDegradedFlag(true); handleSubmit(true); }}
         onCancel={handleResetUpload}
       />
       ```
    5. Migrer `handleSubmit` pour accepter `(degradedConfirmed: boolean)` :
       - log du flag dégradé (sans PII) via `console.info("[audit] submit", { degradedConfirmed })` ou idéalement via un import futur — pour Phase 7, console.info en dev est OK, le retirer si lint warn.
       - parser la response 400 : si `data.error_code` est présent, faire `setStructuredError({ error_code: data.error_code, error: data.error, details: data.details })` et `setEtat("erreur")` (la `<CSVErrorCard>` rend ce payload).
       - si response 200 + `data.degraded === true`, conserver le flag dans `resultats` pour la bannière.
    6. Bannière dégradée dans la branche `etat === "resultats"` : si `resultats.degraded === true`, afficher au-dessus du dashboard une bandeau :
       ```tsx
       {resultats.degraded && (
         <div className="mb-6 rounded-lg border border-amber-300 bg-amber-50 p-4 text-sm text-amber-800">
           <strong className="font-semibold">Audit partiel</strong> : {resultats.ignored_count ?? 0} ligne(s)
           ignorée(s) sur le fichier importé ({Math.round((resultats.reco_rate ?? 0) * 100)} % reconnues).
         </div>
       )}
       ```
       JSX strict : utiliser `&apos;` si apostrophes nécessaires.
    7. Conserver TOUT le code existant non lié à l'upload : les ETAPES_LOADING, le PDF download, le navbar, AuditDashboard rendering. Ne pas régresser.
    8. Imports propres : `import CSVPreview from "@/components/audit/CSVPreview"`, `import DegradedConfirmDialog from "@/components/audit/DegradedConfirmDialog"`, `import CSVErrorCard from "@/components/audit/CSVErrorCard"`, `import type { AuditErrorCode } from "@/types/audit-errors"`.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import CSVPreview" app/audit/page.tsx` retourne 1
    - `grep -c "import DegradedConfirmDialog" app/audit/page.tsx` retourne 1
    - `grep -c "import CSVErrorCard" app/audit/page.tsx` retourne 1
    - `grep -c "<CSVPreview" app/audit/page.tsx` retourne 1
    - `grep -c "<DegradedConfirmDialog" app/audit/page.tsx` retourne 1
    - `grep -c "<CSVErrorCard" app/audit/page.tsx` retourne 1
    - `grep -c "structuredError" app/audit/page.tsx` retourne au moins 3
    - `grep -c "Audit partiel" app/audit/page.tsx` retourne 1
    - `grep -c "preview" app/audit/page.tsx` retourne au moins 2 (state + render)
    - `npm run build` exit 0
    - `npm run lint` exit 0
  </acceptance_criteria>
  <done>Flow complet: dropzone → preview → (dialog if degraded) → POST → résultats avec bannière. Erreurs parse client + API affichent CSVErrorCard. Build + lint verts.</done>
</task>

</tasks>

<verification>
- `npm run build` exit 0
- `npm run lint` exit 0
- `npx tsc --noEmit` exit 0
- Vérification e2e fonctionnelle : Plan 07-08 (Playwright)
</verification>

<success_criteria>
- 4 états de l'UI fonctionnels : formulaire (dropzone) / preview (CSVPreview) / loading / resultats (avec bannière conditionnelle) / erreur (CSVErrorCard)
- Modal Radix s'ouvre uniquement quand `degraded === true`
- Reset upload propre (handleResetUpload nettoie file + structuredError + etat)
- Aucune régression : nom_cabinet, ca_moyen, email, PDF download, ETAPES_LOADING toujours fonctionnels
</success_criteria>

<output>
Créer `.planning/phases/phase-07-robustesse-upload-csv/07-05-SUMMARY.md` après exécution.
</output>
