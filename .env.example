# n8n – webhook prod (sans webhook-test)
N8N_WEBHOOK_URL=https://n8n.srv939707.hstgr.cloud/webhook/audit-flash

# Google Places API (server-side only — ne jamais préfixer NEXT_PUBLIC_)
# Restreindre la clé à l'API "Places" + referrer Vercel prod côté Google Cloud Console
GOOGLE_PLACES_API_KEY=

# Calendly (exposé côté client — OK en NEXT_PUBLIC_)
NEXT_PUBLIC_CALENDLY_URL=https://calendly.com/mndiayepro97/30min
